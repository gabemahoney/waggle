---
id: t3.nwq.vv.yc.zt
type: t3
title: 'Update tests/test_mcp_stdio_server.py: tool count 7 to 8 plus write_template error-wrapping'
up_dependencies:
- t3.nwq.vv.yc.st
down_dependencies:
- t3.nwq.vv.yc.7i
parent: t2.nwq.vv.yc
reference_materials: null
created_at: '2026-05-16T18:37:48.139071'
status: finished
schema_version: '0.1'
guid: nwqvvyczt27nfqh6vpween7izmx63sy1
---

## Context

After Epic 5 lands an eighth MCP tool, the tool-count assertion in `tests/test_mcp_stdio_server.py` (currently expecting 7 after Epic 4) must rise to 8. Error-wrapping coverage is extended to `write_template` mirroring the existing pattern for `spawn_worker`, `list_spawned_workers`, and `list_templates`.

## What Needs to Change

In `tests/test_mcp_stdio_server.py`:
- Update the registered-tool-count assertion (and any test method name that encodes the count) from 7 to 8.
- Add `"write_template"` to the expected-tools set / constant.
- Add a new error-wrapping test class (or test method) for `write_template`: patch the impl to raise an unexpected exception; call the tool via the FastMCP test surface; assert the result has `ok=False` and `err_name="ErrUnexpected"`. A second test in the same class can assert that a known error (e.g. `ErrTemplateNameUnsafe`) propagates unchanged through the wrapper.

No other test classes in this file are modified.

## Key Files

- `tests/test_mcp_stdio_server.py` — tool-count assertion + expected-tools constant + new error-wrapping test class.

## Acceptance Criteria

- The registered-tool-count assertion expects 8.
- The expected-tools set contains `"write_template"` (and all seven prior names).
- New error-wrapping tests for `write_template` cover both: unexpected exception → `ErrUnexpected`, and known error name propagates unchanged.
- `pytest tests/test_mcp_stdio_server.py` is green.
- No other test in the file is modified.
