---
id: t2.nwq.vv.xb
type: t2
title: 'Documentation: README write_template subsection + arch doc write-path extension'
up_dependencies:
- t2.nwq.vv.yc
parent: t1.nwq.vv
children:
- t3.nwq.vv.xb.bn
- t3.nwq.vv.xb.on
reference_materials: null
created_at: '2026-05-16T18:35:41.448790'
status: finished
schema_version: '0.1'
guid: nwqvvxb6p1jgym26ofcmnsppy2yb5qni
---

# Task 2 — Documentation: README write_template subsection + arch doc extension

## Context

Per SR-12.1, the README documents `write_template`'s three parameters, success return shape, the `force` flag, and each error condition with example error responses. Per SR-12.2, the arch doc `docs/architecture/spawn_options_and_templates.md` is extended to describe the `write_template` write path (validation → directory creation → defensive path check → atomic temp+rename) and the fact that the on-disk path written here is the same path the loader reads.

## What Needs to Change

- `README.md` — new `write_template` subsection within the existing "Templates" section. Covers parameters (`name`, `options`, `force`), success return shape (`{path, options}`), `force` flag behavior, and example error responses for each of `ErrTemplateNameUnsafe`, `ErrTemplateOptionsInvalid`, `ErrTemplateExists`, and `ErrUnexpected`. Includes a note that the resulting file is identical regardless of authoring surface (MCP authoring or hand-edit; the CLI surface added in a later Epic shares the same impl).
- `docs/architecture/spawn_options_and_templates.md` — new subsection covering the write path: validation (SR-7.4 name + SR-6.4 options) → directory auto-create → defensive parent-equality path check → atomic temp+rename. Note that the on-disk path is the same path the loader reads, so a freshly written template is immediately visible to the next `spawn_worker(template=<name>)` call (no reload required). Identify `write_template_impl` as the single shared implementation that the future CLI subcommand also delegates to (forward reference).

Out of scope: CLI subcommand usage examples (Epic 6).

## Why

Closes the SR-12 doc bar for Epic 5 — operators can author templates via MCP and reason about the failure modes without consulting source code.

## Success Criteria

- README "Templates" section has a `write_template` subsection with all three parameters, success return shape, `force` semantics, and an example error response for each of the four documented error names.
- The README note about identical-files-regardless-of-surface is present and correctly phrased (MCP or hand-edit; CLI shares the impl in a later Epic).
- The arch doc has a `write_template` write-path subsection covering validation, defensive path check, atomic temp+rename, and the loader-shared-path observation.
- No CLI invocation examples appear in either file (Epic 6 owns that).

## Dependencies

- Task 1 (docs describe shipped behavior).
