---
id: t3.nwq.c4.vr.ya
type: t3
title: 'Update tests/test_mcp_stdio_server.py: tool count 6 to 7 plus list_templates error-wrapping coverage'
up_dependencies:
- t3.nwq.c4.vr.u3
down_dependencies:
- t3.nwq.c4.vr.nu
parent: t2.nwq.c4.vr
reference_materials: null
created_at: '2026-05-16T18:30:53.074541'
status: finished
schema_version: '0.1'
guid: nwqc4vryat12jrzkxnw5qnxszbwqsvkm
---

## Context

After Epic 4 lands a new MCP tool, the tool-count assertion in `tests/test_mcp_stdio_server.py` (currently expecting 6) must rise to 7. The file's error-wrapping coverage already exists for `spawn_worker` and `list_spawned_workers`; extending it to `list_templates` keeps the pattern consistent.

## What Needs to Change

In `tests/test_mcp_stdio_server.py`:
- Update the registered-tool-count assertion (and any test method name that encodes the literal count) from 6 to 7.
- Update the set of expected tool names (typically a module-level constant like `_EXPECTED_TOOLS`) to include `"list_templates"`.
- Add a new error-wrapping test class for `list_templates` mirroring the pattern used by the other tools' error-wrapping tests: patch the impl function to raise an unexpected exception, call the tool via the FastMCP test surface, assert the result has `ok=False` and `err_name="ErrUnexpected"`.

No other test classes in this file are modified.

## Key Files

- `tests/test_mcp_stdio_server.py` — tool-count assertion + expected-tools set + new error-wrapping test class.

## Acceptance Criteria

- The registered-tool-count assertion expects 7 (not 6).
- The expected-tools set contains `"list_templates"` (and all six existing names).
- A new error-wrapping test exercises `list_templates` mapping an unexpected exception to `ErrUnexpected`.
- `pytest tests/test_mcp_stdio_server.py` is green.
- No other existing test in this file is modified.
