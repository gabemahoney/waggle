---
id: t3.nwq.sn.wj.jt
type: t3
title: Run full unit test suite after CLI write-template lands
up_dependencies:
- t3.nwq.sn.wj.ap
- t3.nwq.sn.wj.gf
- t3.nwq.sn.wj.u6
parent: t2.nwq.sn.wj
reference_materials: null
created_at: '2026-05-16T18:43:53.192445'
status: pupa
schema_version: '0.1'
guid: nwqsnwjjt1wkw8id7ez58j3uxhmabzop
---

## Context

Gate subtask. After the two engineer subtasks and the test-file subtask have landed, the full pytest suite must pass. This Epic also completes the work for Plan Bee `b.nwq`, so the gate covers regressions across all six Epics.

## What Needs to Change

- Run `pytest` against the full `tests/` directory.
- Diagnose and fix any failures at the call site (no weakening of assertions).
- Verify `tests/test_cli_write_template.py` and `tests/test_cli.py` both pass in isolation and as part of the full run.

## Key Files

- All files under `tests/` (read-only scan; edits only where failures surface).

## Acceptance Criteria

- `pytest tests/` exits 0 with 0 failures, 0 errors, 0 unintended skips.
- The new `tests/test_cli_write_template.py` passes in isolation and as part of the full run.
- The existing `tests/test_cli.py` is unmodified or its modifications are commit-justified (no regressions).
