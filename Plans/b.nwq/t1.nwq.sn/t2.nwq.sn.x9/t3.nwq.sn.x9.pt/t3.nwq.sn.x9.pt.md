---
id: t3.nwq.sn.x9.pt
type: t3
title: Update arch doc shared-impl note to present tense now that CLI exists
parent: t2.nwq.sn.x9
reference_materials: null
created_at: '2026-05-16T18:44:06.528230'
status: pupa
schema_version: '0.1'
guid: nwqsnx9pt96sihk9mhapzefikt2weu1m
---

## Context

Epic 5 added a forward-reference note in `docs/architecture/spawn_options_and_templates.md` stating that the CLI `write-template` subcommand and the `write_template` MCP tool both delegate to a single `write_template_impl`. With Epic 6 shipping the CLI, that forward reference should be updated to remove any "to be added later" hedging.

## What Needs to Change

In `docs/architecture/spawn_options_and_templates.md`:
- Locate the forward-reference note about `write_template_impl` being the shared delegation target.
- Update wording to present tense: remove any "future Epic", "to be added", or "will be added" hedge. State that both the MCP tool `write_template` and the CLI subcommand `claude-spawn write-template` delegate to the same `write_template_impl` function.
- Confirm the function name and file path (`src/claude_spawn/templates.py`) are accurate.

No new sections; no structural changes; word-count delta should be small.

## Key Files

- `docs/architecture/spawn_options_and_templates.md` — small wording update on the existing shared-impl note.

## Acceptance Criteria

- The shared-impl note is present tense (no "future" / "to be added" hedge).
- The function name and file path are correct as shipped.
- No other content in the file is changed by this subtask.
