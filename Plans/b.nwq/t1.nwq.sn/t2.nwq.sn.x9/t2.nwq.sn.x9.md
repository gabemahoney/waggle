---
id: t2.nwq.sn.x9
type: t2
title: 'Documentation: README write-template CLI section + arch doc note update'
up_dependencies:
- t2.nwq.sn.wj
parent: t1.nwq.sn
children:
- t3.nwq.sn.x9.io
- t3.nwq.sn.x9.pt
reference_materials: null
created_at: '2026-05-16T18:42:01.331420'
status: finished
schema_version: '0.1'
guid: nwqsnx9ixshviu7arubx5ujkdqijmp25
---

# Task 2 — Documentation: README CLI section + arch doc note

## Context

Per SR-12.1, the README documents `claude-spawn write-template` with both interactive and flag-driven mode examples, the repeatable-flag forms for map-valued options, `--force` behavior, and a flag-to-option mapping table. Epic 5 added a forward-reference note in `docs/architecture/spawn_options_and_templates.md` about the shared `write_template_impl`; Epic 6 confirms (and lightly updates) that note now that the CLI surface exists.

## What Needs to Change

- `README.md` — new CLI section (within or alongside the existing "Templates" section). Covers: a one-liner flag-driven invocation example; an interactive transcript example; the repeatable-flag forms for `--extra-env-entry`, `--claude-status-labels-entry`, `--permissions-{allow,deny,ask}`, and `--claude-arg`; `--force` behavior; a flag-to-option table mapping every CLI flag to its SR-1.1 option name; a cancellation note (Ctrl-C/EOF in interactive mode).
- `docs/architecture/spawn_options_and_templates.md` — update the forward-reference note from Epic 5 to remove the "to be added later" hedge, now that the CLI exists. Confirm the note accurately names `write_template_impl` as the single delegation target.

## Why

Closes the SR-12 doc bar for Epic 6 — operators can author templates from the command line without consulting source code.

## Success Criteria

- README has a `claude-spawn write-template` section with a complete flag-driven one-liner example, a multi-prompt interactive transcript, the repeatable-flag forms shown explicitly, the `--force` behavior described, and a flag-to-option table covering every CLI flag.
- The cancellation note is present.
- The arch doc's shared-impl note is updated to present tense (no "future Epic" or "to be added" hedge).

## Dependencies

- Task 1 (docs describe shipped behavior).
