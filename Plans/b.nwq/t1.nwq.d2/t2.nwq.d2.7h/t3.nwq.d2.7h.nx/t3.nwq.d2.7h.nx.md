---
id: t3.nwq.d2.7h.nx
type: t3
title: Create tests/test_engine_spawn_worker_readiness.py covering all four readiness paths
up_dependencies:
- t3.nwq.d2.7h.a1
down_dependencies:
- t3.nwq.d2.7h.7y
parent: t2.nwq.d2.7h
reference_materials: null
created_at: '2026-05-16T18:13:49.118678'
status: done
schema_version: '0.1'
guid: nwqd27hnxq7p9dk6tc5iqnnqr27chais
---

## Context

Epic 2's engineer subtask adds a polling loop, a module-level `_SPAWN_READINESS_TIMEOUT` constant, and two new error names to `spawn_worker_impl`. This subtask creates a dedicated test file covering all four readiness paths: timely registration (happy path with a bounded poll-count assertion), readiness timeout (with `kill-session` cleanup verification), best-effort kill (kill returns non-zero — `err_name` unchanged), and worker-exited-early (with `capture-pane` content verification).

## What Needs to Change

Create `tests/test_engine_spawn_worker_readiness.py`. The file patches the existing seams:
- `claude_spawn.spawn._tmux` (for `new-session`, `send-keys`, `has-session`, `capture-pane`, `kill-session`).
- `claude_spawn.claude_status._run` (for the poll loop) — drive via `tests.helpers.fake_claude_status`.

Per SR-11.2's mandated helper reuse, the file MUST use:
- `tests.helpers.fake_claude_status(triples)` — one triple per expected poll.
- `tests.helpers.fake_worker_record(instance_id=..., status="working", cwd="/tmp")` — to build worker records (do NOT hardcode label keys; use the `cwd=` keyword added in Epic 1 Task 2).
- `tests.helpers.fake_workers_response(records, skipped=())` — envelope wrapper.
- `tests.sample_payloads.WORKER_WORKING` and similar canonical shapes where applicable.

**Test cases (all in the same file):**

1. **Happy path — worker registers on the second poll.** Configure `fake_claude_status` with: (a) one triple returning a `fake_workers_response([])` (empty workers list — worker not yet registered); (b) one triple returning a `fake_workers_response([fake_worker_record(instance_id=<spawned id>, status="working", cwd="/tmp")])`. Capture `_tmux` calls. Assert:
   - Return value is exactly `{"instance_id": ..., "tmux_session_name": ...}` with no other keys (no `ok`, no `err_name`).
   - No `kill-session` call in captured `_tmux`.
   - The poll loop ran ≥1 and ≤2 iterations (verify by asserting that both `fake_claude_status` triples were consumed — no leftover triples and no extra unmocked calls). This matches Epic 2 AC #1's "ran ≥1 and ≤2 polls" requirement.

2. **Timeout path.** Patch `claude_spawn.spawn._SPAWN_READINESS_TIMEOUT` down to a small value (e.g. `0.05` seconds) so the test runs in well under a second. Configure `fake_claude_status` with empty-workers triples for as many polls as fit within the patched timeout. Assert:
   - Return is `{"ok": False, "err_name": "ErrSpawnReadinessTimeout", ...}`.
   - `err_description` contains both the `tmux_session_name` string AND the patched timeout value (so an operator reading the description can identify both).
   - A `kill-session -t <tmux_session_name>` call appears in captured `_tmux`.

3. **Best-effort kill on timeout.** Same setup as case 2, but configure `_tmux` to return a non-zero exit code (or raise) on the `kill-session` call. Assert:
   - Returned `err_name` is still `ErrSpawnReadinessTimeout` (kill failure does NOT change the error surface).

4. **Worker-exited-early.** Configure `_tmux` so a `has-session` probe partway through the poll returns non-zero (session gone), and a subsequent `capture-pane` returns the pane string `"FATAL: something bad
"`. Configure `fake_claude_status` with empty-workers triples for the polls that precede the early-exit detection. Assert:
   - Return is `{"ok": False, "err_name": "ErrSpawnWorkerExitedEarly", ...}`.
   - `err_description` contains the substring `"FATAL: something bad"`.
   - `capture-pane` appears in captured `_tmux` calls.

**Fast-test constraint:** every test in this file MUST complete in well under a second of wall time. Patch `_SPAWN_READINESS_TIMEOUT` at the module level rather than waiting for the real 15-second default.

## Key Files

- `tests/test_engine_spawn_worker_readiness.py` — new file.
- `tests/helpers.py` — uses `fake_claude_status`, `fake_worker_record`, `fake_workers_response` (no helper changes needed).
- `tests/sample_payloads.py` — use `WORKER_WORKING` or equivalent constants where helpful.

## Acceptance Criteria

- `pytest tests/test_engine_spawn_worker_readiness.py -v` passes with 0 failures, 0 errors.
- The entire file completes in well under a second of wall time (`pytest --timeout=2 tests/test_engine_spawn_worker_readiness.py` succeeds).
- Happy-path test asserts the exact two-key return shape AND the poll-count bound (≥1, ≤2 iterations when worker registers on the second poll).
- Timeout test asserts `err_name="ErrSpawnReadinessTimeout"`, description contains `tmux_session_name` AND timeout value, and a `kill-session` call is in `_tmux` history.
- Best-effort-kill test asserts `err_name` is unchanged when `kill-session` fails.
- Exited-early test asserts `err_name="ErrSpawnWorkerExitedEarly"` and description contains captured pane text.
- No test hardcodes label keys; worker records built via `fake_worker_record(cwd=...)`.
- File uses `fake_claude_status`, `fake_worker_record`, `fake_workers_response` per SR-11.2 — no inline dicts for canonical shapes.
