---
id: t3.nwq.d2.7h.a1
type: t3
title: Add spawn-readiness polling loop with timeout cleanup and worker-exited-early detection
down_dependencies:
- t3.nwq.d2.7h.nx
- t3.nwq.d2.7h.5h
- t3.nwq.d2.7h.7y
parent: t2.nwq.d2.7h
reference_materials: null
created_at: '2026-05-16T18:13:43.563333'
status: finished
schema_version: '0.1'
guid: nwqd27ha126dhntiqta4ew8nc56u5qq3
---

## Context

Epic 1 already replaced `spawn_worker_impl`'s signature and added validation + launch composition; the function currently returns the success dict `{instance_id, tmux_session_name}` immediately after the tmux `send-keys` call succeeds. SR-5 makes the return blocking: poll Claude Status's `instances` table until the spawned `instance_id` appears, or until the 15-second readiness timeout expires (SR-5.2). On timeout, clean up the unregistered tmux session before returning (SR-5.3). Detect worker-exited-early via interleaved `tmux has-session` probes and capture pane content for diagnostics on detected exit (SR-5.4).

## What Needs to Change

In `src/claude_spawn/spawn.py`:
- Add a module-level constant `_SPAWN_READINESS_TIMEOUT = 15` (seconds). The constant MUST be module-level per SR-5.2; do NOT expose it as a caller-facing parameter or a template field (SR-13).
- After the existing tmux `send-keys` block in `spawn_worker_impl`, replace the bare success return with a polling loop that:
  - Tracks elapsed time against `_SPAWN_READINESS_TIMEOUT`.
  - On each iteration, calls `claude_status.workers(label="claude_spawn_owned=1")` via the existing `_run` seam and scans for a record where `record["instance_id"] == instance_id` (exact-equality per SR-10.1 — do NOT match on `tmux_session_name`).
  - On match: returns `{"instance_id": instance_id, "tmux_session_name": tmux_session_name}`.
  - Interleaves a `tmux has-session -t <tmux_session_name>` probe via the existing `_tmux` seam. If the probe returns non-zero (session gone), attempt `tmux capture-pane` (best-effort — capture failure must not raise) and return `err_name=ErrSpawnWorkerExitedEarly` with the captured output included in `err_description`.
  - Poll interval short enough that happy-path return latency is bounded by actual registration time + at most one poll round-trip (e.g. ~100 ms via `time.sleep`).
- On timeout expiration: issue `tmux kill-session -t <tmux_session_name>` via `_tmux` (best-effort — kill failure must NOT change the returned `err_name`), then return `err_name=ErrSpawnReadinessTimeout` with `err_description` naming both the `tmux_session_name` and the timeout value (so an operator can identify the orphaned worker and the bound that triggered).

No new subprocess seam. Reuse the existing `_tmux` and `claude_status._run` patch targets so the test architecture stays simple.

## Key Files

- `src/claude_spawn/spawn.py` — module-level constant, polling loop after `send-keys` in `spawn_worker_impl`, both new error returns.

## Acceptance Criteria

- `_SPAWN_READINESS_TIMEOUT = 15` exists at module level in `spawn.py` and is not exposed through any caller-facing parameter or template field.
- The readiness poll calls `claude_status.workers(label="claude_spawn_owned=1")` and matches solely on `record["instance_id"] == instance_id`.
- On successful registration, the function returns `{"instance_id": ..., "tmux_session_name": ...}` with no `kill-session` call issued.
- On timeout: `tmux kill-session` is called, then return has `err_name=ErrSpawnReadinessTimeout` and description containing both the `tmux_session_name` and the timeout value.
- Kill failure on timeout does NOT alter the returned `err_name`.
- On worker-exited-early (session gone mid-poll): `tmux capture-pane` is attempted, then return has `err_name=ErrSpawnWorkerExitedEarly` and description includes the captured output.
- Capture failure on exited-early does not crash the function (best-effort).
- The existing `_tmux` and `claude_status._run` patch targets are sufficient to exercise all paths; no new seam is added.
