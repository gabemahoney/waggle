---
id: t3.nwq.d2.7h.7y
type: t3
title: Run full unit test suite and resolve remaining failures after readiness blocking lands
up_dependencies:
- t3.nwq.d2.7h.a1
- t3.nwq.d2.7h.nx
- t3.nwq.d2.7h.5h
parent: t2.nwq.d2.7h
reference_materials: null
created_at: '2026-05-16T18:13:58.182040'
status: done
schema_version: '0.1'
guid: nwqd27h7y49sut8kggb3qm6mge9nwo82
---

## Context

After Epic 2's engineer subtask + the new readiness test file + adapted Epic 1 happy-path tests have all landed, the full pytest suite must be run to surface any residual failures: tests in other files (e.g. `test_engine_list_spawned_workers.py`, `test_engine_send_input.py`, `test_engine_answer_question.py`) that incidentally call `spawn_worker_impl` and now hang on the readiness loop; `fake_claude_status` triple-count mismatches from happy-path call sites not covered by the adapt subtask; or import errors from `_SPAWN_READINESS_TIMEOUT` patching.

## What Needs to Change

- Run `pytest` from the repo root against the full `tests/` directory with a sensible per-test timeout (e.g. `pytest --timeout=10`).
- For each failure, diagnose root cause and fix at the call site. Do NOT weaken assertions to make a failure go away.
- Common categories to anticipate:
  - Other test files that call `spawn_worker_impl` happy-path without a `fake_claude_status` wrapper → apply the same wrapper pattern from the adapt subtask.
  - `fake_claude_status` raising on un-consumed triples → reduce or extend the triple count to match the actual poll iterations.
  - Module-level constant patches that need to be applied before `spawn_worker_impl` is called (not after).

## Key Files

- All files under `tests/` (read-only scan; edits only where failures surface).
- `src/claude_spawn/spawn.py` — confirm `_SPAWN_READINESS_TIMEOUT` is module-level and accessible for patching.

## Acceptance Criteria

- `pytest tests/` exits 0 with 0 failures, 0 errors, 0 unintended skips.
- No test is marked `xfail` or `skip` as a workaround for a real failure introduced by Epic 2.
- The new `tests/test_engine_spawn_worker_readiness.py` is included and passing.
