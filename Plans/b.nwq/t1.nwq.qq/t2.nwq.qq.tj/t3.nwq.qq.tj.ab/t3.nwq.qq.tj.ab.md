---
id: t3.nwq.qq.tj.ab
type: t3
title: 'Create tests/test_engine_spawn_worker_options.py: sanitize helper and default tmux_session_name'
up_dependencies:
- t3.nwq.qq.v9.7d
- t3.nwq.qq.tj.i3
down_dependencies:
- t3.nwq.qq.tj.4w
- t3.nwq.qq.tj.c3
- t3.nwq.qq.tj.bj
parent: t2.nwq.qq.tj
reference_materials: null
created_at: '2026-05-16T18:03:20.145085'
status: finished
schema_version: '0.1'
guid: nwqqqtjab5vmr1srtasd9toqkehiuof3
---

## Context

SR-11.6 places the sanitize-helper unit tests in `tests/test_engine_spawn_worker_options.py`. This subtask creates that new test file and seeds it with the sanitize SR-3.3 table and a default-`tmux_session_name` composition test. Sibling test subtasks (launch composition, overlay, validation) extend the same file.

## What Needs to Change

Create `tests/test_engine_spawn_worker_options.py`. Initial contents:

- A test class (e.g. `TestSanitizeFolderName`) with one assertion per SR-3.3 row exercising the helper directly:
  - `/home/horde/projects/waggle-project/waggle-main` → `waggle-main`
  - `/home/horde/projects/waggle-project/waggle-main/` → `waggle-main`
  - `/path/foo.bar.tar.gz` → `foo-bar-tar-gz`
  - `/path/with:colon` → `with-colon`
  - `/` → `root`
  - `.` → `root`
- A test class (e.g. `TestDefaultTmuxSessionName`) that calls `spawn_worker_impl` with a fixed `instance_id` and a known `cwd`, captures the `_tmux` argv via patched `_tmux`, and asserts the `-s` value matches `<sanitize(cwd)>-<instance_id[:8]>`.
- A second test in `TestDefaultTmuxSessionName` exercising the SR-3.4 "shorter than 8 chars" path: pass an `instance_id` of length < 8 and assert the full string is used as the suffix (no padding).

Tests must use `tests.helpers.fake_claude_status` for any Claude Status seam that the impl touches (e.g. if the test path involves the no-`instance_id`-collision check); they must NOT patch `_run` directly.

No test in this file may hardcode label keys; if needed, use `fake_worker_record(..., cwd=...)`.

## Key Files

- `tests/test_engine_spawn_worker_options.py` — new file; this subtask creates it and lands the sanitize + default-`tmux_session_name` slice.

## Acceptance Criteria

- All six SR-3.3 rows are asserted individually.
- The default-`tmux_session_name` test confirms `<sanitize(cwd)>-<instance_id[:8]>` composition.
- The short-`instance_id` test confirms no padding (full string used).
- `pytest tests/test_engine_spawn_worker_options.py::TestSanitizeFolderName tests/test_engine_spawn_worker_options.py::TestDefaultTmuxSessionName` is green after Task 1's helper and Task 3's signature change have landed.
