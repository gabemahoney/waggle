---
id: t3.nwq.qq.tj.hz
type: t3
title: Run full unit test suite and fix remaining failures after Task 3 signature change
up_dependencies:
- t3.nwq.qq.tj.s3
- t3.nwq.qq.tj.4w
- t3.nwq.qq.tj.c3
- t3.nwq.qq.tj.bj
parent: t2.nwq.qq.tj
reference_materials: null
created_at: '2026-05-16T18:03:38.970380'
status: finished
schema_version: '0.1'
guid: nwqqqtjhzbrmb8tp9q5n7jtpbzmrkuje
---

## Context

After all Task 3 engineer subtasks (T3-A through T3-D) and test subtasks have landed, the full pytest suite must be run to catch any cross-module breakage — e.g. stale assertions in `tests/test_engine_list_spawned_workers.py`, fixture regressions, import errors, or unintended ripple effects from `_REQUIRED_ENV_VARS` membership changes.

## What Needs to Change

- Run `pytest` against the full `tests/` directory.
- Investigate and fix any failures not already addressed by the prior Task 3 subtasks.
- If a test must be deleted because the behavior it covered was deliberately retired by Epic 1 (e.g. an assertion on the old `session_name` return key that has no equivalent in the new shape), delete it cleanly and note the removal in the commit.

## Key Files

- All files under `tests/` (read-only scan; edits only where failures surface).

## Acceptance Criteria

- `pytest` exits 0 with no failures and no unintended skips.
- The new `tests/test_engine_spawn_worker_options.py` test classes (`TestSanitizeFolderName`, `TestDefaultTmuxSessionName`, `TestLaunchComposition`, `TestSettingsOverlay`, `TestValidationErrors`) all pass.
- `grep -r "session_name" tests/ src/` shows only `tmux_session_name` references — no bare `session_name` keys in test assertions or live code.
