---
id: t3.nwq.qq.7v.wm
type: t3
title: Update docs/architecture/spawn_worker.md for new options, launch composition, errors, mermaid
parent: t2.nwq.qq.7v
reference_materials: null
created_at: '2026-05-16T18:05:03.073633'
status: finished
schema_version: '0.1'
guid: nwqqq7vwmbujhd1fnoysb672iaev5sbm
---

## Context

The `docs/architecture/spawn_worker.md` arch doc currently describes the 3-parameter signature, a 7-env-var tmux invocation referencing `CLAUDE_STATUS_LABEL_WAGGLE_REPO`, and a mermaid sequence with the old positional `claude --model {model}` command. Epic 1 replaces all of that. Depends on Task 3 landing so the data-flow text reflects shipped behavior.

## What Needs to Change

In `docs/architecture/spawn_worker.md`:

- **Parameters section:** replace the 3-row table with a 12-row table matching the new signature (Parameter | Type | Required | Default | Description). Mark `cwd` as required; show SR-1.3 defaults for the rest.
- **Flow section — env vars:** update the env-var list to the SR-4.2 set: `CLAUDE_STATUS_INSTANCE_ID`, `CLAUDE_STATUS_RELAY_MODE=on`, `CLAUDE_STATUS_AUQ_MODE=record`, `CLAUDE_STATUS_LABEL_CLAUDE_SPAWN_OWNED=1`, `_SESSION_NAME`, `_CWD` (renamed from `_REPO`), and conditional `_MODEL`. Note that `extra_env` and `claude_status_labels` entries are appended.
- **Flow section — start-directory:** explicitly call out that the working directory is set via tmux's start-directory flag on `new-session`, not a `cd` send-keys preamble.
- **Flow section — command line:** show the SR-4.4 composition: `claude [--model <m>] [--effort <l>] [--settings <p>] [claude_args…]` + `Enter`. State that `--model` and `--effort` are emitted only when supplied, and `--settings` is emitted only when an overlay path is resolved.
- **New: Settings overlay subsection:** add a short subsection describing the three overlay-synthesis paths at a high level (permissions-only → synthesized JSON overlay; `claude_settings`-only → caller file passed verbatim; composite → merged temp file with first-class `permissions` precedence). Include a one-line deferral note pointing to the future `docs/architecture/spawn_options_and_templates.md` for the templates flow (Epic 3 will create that file).
- **Errors table:** add the eight new Epic 1 error names alongside any existing entries: `ErrCwdMissing`, `ErrCwdNotFound`, `ErrCwdNotAPath`, `ErrClaudeSettingsNotFound`, `ErrInstanceIdCollision`, `ErrClaudeArgsSettingsConflict`, `ErrReservedEnvKey`, `ErrThinkingInvalid`.
- **Mermaid sequence diagram:** update to reflect the new env vars (the renamed `_CWD` label, conditional `_MODEL`), the start-directory flag on `new-session`, and the conditional command-line composition (do NOT show the old fixed `claude --model {model}` string).

Out of scope: do NOT create `docs/architecture/spawn_options_and_templates.md` (that's Epic 3). Do NOT describe the readiness wait (that's Epic 2). Do NOT describe templates, `list_templates`, or `write_template`.

## Key Files

- `docs/architecture/spawn_worker.md` — wholesale update of Parameters / Flow / Errors / Sequence Diagram; new Settings Overlay subsection.

## Acceptance Criteria

- `grep -i "\brepo\b" docs/architecture/spawn_worker.md` returns no matches (the file no longer references `repo` cloning).
- The parameters table has 12 rows; `cwd` is marked required.
- The env-var listing matches SR-4.2 exactly (six unconditional + conditional `_MODEL`).
- The errors table includes all eight new Epic 1 error names.
- The mermaid diagram renders without syntax errors and no longer shows the legacy fixed-string command.
- The deferral pointer for the templates flow is present.
