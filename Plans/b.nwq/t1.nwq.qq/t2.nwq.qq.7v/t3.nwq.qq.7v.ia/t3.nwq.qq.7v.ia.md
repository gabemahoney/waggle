---
id: t3.nwq.qq.7v.ia
type: t3
title: 'README: spawn_worker 12-option parameter table, fallback table, return shape'
parent: t2.nwq.qq.7v
reference_materials: null
created_at: '2026-05-16T18:04:50.430555'
status: finished
schema_version: '0.1'
guid: nwqqq7viau6ifc32szypvvivu5m3shi5
---

## Context

The current README's `spawn_worker` parameter table describes the legacy `(model, repo, session_name)` shape and the return shape includes an `ok` key. Epic 1 ships a 12-option signature, drops `ok`, and renames `session_name` → `tmux_session_name`. Operators reading the old table will pass arguments that fail at the MCP layer. Depends on Task 3 (T3-A through T3-D + tests) landing so the docs describe shipped behavior.

## What Needs to Change

In `README.md`:
- Replace the existing `spawn_worker` parameter table with a 5-column table covering all twelve options: Parameter | Type | Required | Default | Description. Order matches SR-1.1: `template`, `model`, `thinking`, `cwd`, `tmux_session_name`, `instance_id`, `claude_home`, `claude_settings`, `extra_env`, `claude_status_labels`, `claude_args`, `permissions`.
- Add a second compact table titled something like "Per-option fallback category" drawn from SR-1.3, with three rows: Required (`cwd`), Hardcoded-by-Claude-Spawn (`tmux_session_name`, `instance_id`, `extra_env`, `claude_status_labels`, `claude_args`, `permissions`), Inherits-Claude-Code-default (`model`, `thinking`, `claude_home`, `claude_settings`).
- Replace the return-shape snippet with `{"instance_id": str, "tmux_session_name": str}` — drop the `ok` key and the `session_name` key.

Out of scope: this subtask documents only the v1 surface for Epic 1. Do NOT add sections describing template loading, `list_templates`, `write_template`, the CLI subcommand, or the spawn-readiness blocking behavior — those belong to sibling Epics.

## Key Files

- `README.md` — `spawn_worker` parameter table, the new fallback-category table, and the return-shape snippet.

## Acceptance Criteria

- The parameter table has exactly twelve rows, in SR-1.1 order.
- The fallback-category table has three rows corresponding to the three SR-1.3 categories.
- The return-shape snippet matches `{instance_id, tmux_session_name}` with no `ok` and no `session_name` key.
- No mention of templates, readiness, `list_templates`, `write_template`, or the CLI in this section.
