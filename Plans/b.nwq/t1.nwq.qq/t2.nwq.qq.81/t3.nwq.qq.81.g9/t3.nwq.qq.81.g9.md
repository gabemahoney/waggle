---
id: t3.nwq.qq.81.g9
type: t3
title: Run full unit test suite and fix remaining failures after label rename
up_dependencies:
- t3.nwq.qq.81.ou
- t3.nwq.qq.81.gh
- t3.nwq.qq.81.x1
parent: t2.nwq.qq.81
reference_materials: null
created_at: '2026-05-16T18:00:07.426990'
status: finished
schema_version: '0.1'
guid: nwqqq81g9yxr2vfcqonioj73cp5xowmo
---

## Context

After the impl rename, helper rename, and test-assertion rename subtasks have all landed, the full pytest suite must be run to catch any remaining stale reference (e.g. another test file that hardcoded the old label key, a fixture in an unexpected place, or a cross-module import that breaks).

## What Needs to Change

- Run `pytest` against the full `tests/` directory.
- Investigate and fix any failures not already addressed by the prior Task 2 subtasks.
- If any test must be deleted because the behavior it covered was deliberately retired by the rename, delete it cleanly and note the removal.

## Key Files

- All files under `tests/` (read-only scan; edits only where failures surface).

## Acceptance Criteria

- `pytest` exits 0 with no failures and no unintended skips.
- `grep -r "claude_spawn_repo\|CLAUDE_SPAWN_REPO\|waggle_repo\|WAGGLE_REPO" src/ tests/` returns no matches.
