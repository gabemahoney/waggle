---
id: t3.nwq.qq.81.ou
type: t3
title: Rename label key at all emit and read sites in spawn.py
down_dependencies:
- t3.nwq.qq.81.x1
- t3.nwq.qq.81.g9
parent: t2.nwq.qq.81
reference_materials: null
created_at: '2026-05-16T17:59:53.736885'
status: finished
schema_version: '0.1'
guid: nwqqq81oudbhycwxk5geoky1sr4maakx
---

## Context

SR-10.2 renames the `claude_spawn_repo` label key to `claude_spawn_cwd` to reflect the semantic shift to working-directory. This subtask renames the impl-side emit and read sites; the test-helper rename is a sibling subtask that lands together to keep the suite green.

## What Needs to Change

In `src/claude_spawn/spawn.py`:
- Rename `CLAUDE_STATUS_LABEL_CLAUDE_SPAWN_REPO` → `CLAUDE_STATUS_LABEL_CLAUDE_SPAWN_CWD` at the env-var emit site in `spawn_worker_impl`.
- Update `_REQUIRED_ENV_VARS` to replace the old constant string with the new one.
- Audit `list_spawned_workers_impl` and `answer_question_impl` (and any other consumer in `spawn.py`) for `claude_spawn_repo` label lookups; rename to `claude_spawn_cwd`.

No other source files should require changes for this subtask.

## Key Files

- `src/claude_spawn/spawn.py` — emit site, `_REQUIRED_ENV_VARS`, label read sites.

## Acceptance Criteria

- `grep "claude_spawn_repo\|CLAUDE_SPAWN_REPO" src/` returns no matches.
- `_REQUIRED_ENV_VARS` membership includes `CLAUDE_STATUS_LABEL_CLAUDE_SPAWN_CWD` and excludes the old `_REPO` constant.
- `list_spawned_workers_impl` projects the worker's `cwd` from the new label key.
- After the sibling helper-rename subtask also lands, full `pytest` is green.
