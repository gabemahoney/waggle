---
id: t3.nwq.qq.81.x1
type: t3
title: Update test_engine_spawn_worker.py assertions for new label key
up_dependencies:
- t3.nwq.qq.81.ou
- t3.nwq.qq.81.gh
down_dependencies:
- t3.nwq.qq.81.g9
parent: t2.nwq.qq.81
reference_materials: null
created_at: '2026-05-16T18:00:01.728463'
status: finished
schema_version: '0.1'
guid: nwqqq81x15emzvkjgcbhdg6z76sspv86
---

## Context

The Epic 1 Task 2 label rename will break any existing test that asserts on the literal env-var name `CLAUDE_STATUS_LABEL_CLAUDE_SPAWN_REPO` or the `claude_spawn_repo` label key. After the impl rename and helper rename subtasks land, those broken assertions must be updated to the new name to restore the green suite.

## What Needs to Change

In `tests/test_engine_spawn_worker.py` (and any other test file under `tests/` that may hardcode the old label key strings):
- Rename any test that asserts on `CLAUDE_STATUS_LABEL_CLAUDE_SPAWN_REPO` (or `WAGGLE_REPO`) to assert on `CLAUDE_STATUS_LABEL_CLAUDE_SPAWN_CWD`.
- Update bodies that reference the old key string to use the new one.
- If any test iterates `_REQUIRED_ENV_VARS` and asserts on a specific membership, update it for the renamed entry.
- Use `fake_worker_record(..., cwd=...)` for any new assertion that needs to set the new label; do not hardcode label dicts.

## Key Files

- `tests/test_engine_spawn_worker.py` — primary location of stale label assertions.
- Any other test file that fails after the rename.

## Acceptance Criteria

- `grep "CLAUDE_SPAWN_REPO\|claude_spawn_repo\|WAGGLE_REPO\|waggle_repo" tests/` returns no matches.
- `pytest tests/test_engine_spawn_worker.py` is green after the impl and helper subtasks have also landed.
- Full `pytest` suite is green.
