---
id: t3.nwq.uu.tc.yj
type: t3
title: Run full unit test suite after templates loader and helpers land
up_dependencies:
- t3.nwq.uu.tc.6i
- t3.nwq.uu.tc.4k
- t3.nwq.uu.tc.zt
parent: t2.nwq.uu.tc
reference_materials: null
created_at: '2026-05-16T18:25:20.679709'
status: finished
schema_version: '0.1'
guid: nwquutcyjyvegn8tyuer233icfz5myqg
---

## Context

After Task 2's engineer subtask, the helper/payload subtask, and the load-test subtask have all landed, the full pytest suite must be run to surface any cross-module breakage — `tests/helpers.py` changes that ripple to existing test files, an unexpected import-time effect, or a missing seam in the new `templates.py`.

## What Needs to Change

- Run `pytest` against the full `tests/` directory.
- Investigate and fix any failures not already addressed by the prior Task 2 subtasks.
- If a previously-green test fails because it inadvertently depended on the helper's prior behavior, fix at the call site (do not weaken assertions).

## Key Files

- All files under `tests/` (read-only scan; edits only where failures surface).
- `src/claude_spawn/templates.py` — confirm it imports cleanly under the new Python floor.

## Acceptance Criteria

- `pytest tests/` exits 0 with 0 failures, 0 errors, 0 unintended skips.
- `pytest tests/test_engine_templates_load.py` passes in isolation and as part of the full run.
- `import claude_spawn.templates` succeeds at module import time without filesystem reads.
