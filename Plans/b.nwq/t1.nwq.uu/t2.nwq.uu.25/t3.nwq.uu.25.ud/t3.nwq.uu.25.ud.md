---
id: t3.nwq.uu.25.ud
type: t3
title: Run full unit test suite after template= resolution lands
up_dependencies:
- t3.nwq.uu.25.4j
- t3.nwq.uu.25.w2
parent: t2.nwq.uu.25
reference_materials: null
created_at: '2026-05-16T18:25:33.148190'
status: pupa
schema_version: '0.1'
guid: nwquu25ud7rh19wq64o4fxm43j7514pm
---

## Context

Gate subtask. After Task 3's engineer subtask and the integration-tests subtask have landed, the full pytest suite must pass.

## What Needs to Change

- Run `pytest` against the full `tests/` directory.
- Diagnose and fix any failures at the call site (no weakening of assertions). Common categories to anticipate:
  - Tests in other files that interact with the loader seams must use `fake_templates_dir`.
  - Tests that previously assumed `template=` parameter was a no-op may need updating.
  - Happy-path tests that didn't previously need a templates fixture and don't supply `template=` should remain unchanged (the no-template fast path makes no filesystem read).

## Key Files

- All files under `tests/`.
- `src/claude_spawn/spawn.py`, `src/claude_spawn/templates.py` — confirm clean imports and behavior.

## Acceptance Criteria

- `pytest tests/` exits 0 with 0 failures, 0 errors, 0 unintended skips.
- The new template integration cases in `tests/test_engine_spawn_worker_options.py` all pass.
- No test is marked `xfail`/`skip` as a workaround for a real failure introduced by Task 3.
