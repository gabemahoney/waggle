---
id: t3.fg8.vv.9m.ef
type: t3
title: Run pytest -q and verify no Epic-vv regressions
parent: t2.fg8.vv.9m
status: pupa
created_at: '2026-05-14T16:08:50.238835'
schema_version: '0.1'
guid: fg8vv9mefceiuc3x5fjwajqoezzpvka5
---

## Context

Sole Subtask under the Epic-level regression gate Task. Runs after Tasks 1, 2, 3 of Epic `t1.fg8.vv` have landed.

## What Needs to Change

No new files. Run `pytest -q` from the repo root. Triage any failures or collection errors. Apply minimal fixes scoped to NEW files only.

Acceptable fixes (in new files only):
- Narrowing imports in `mcp_stdio.py` so the module has no import-time side effects.
- Deferring side-effectful calls (e.g. accidental subprocess fork at module load).
- Fixing test fixture cleanup or interface mismatches in new test files.

Out-of-scope: any change to any pre-existing file in `src/` or `tests/` or elsewhere. No edits, no deletions, no skips, no xfails, no monkeypatches, no `# type: ignore` shims to silence pre-existing failures.

## Key Files

- New files introduced by Epic `t1.fg8.vv` (the stdio server module, new tmux helper, new test files, README addition) — may require adjustment

## Acceptance Criteria

- `pytest -q` exits 0.
- Test count ≥ pre-Epic count + new tests from Tasks 1, 2, 3; no pre-existing test deleted/skipped/xfailed.
- `python -c 'import waggle.mcp_stdio'` (or whatever path Engineer chose) succeeds with `subprocess.run`, `socket.socket`, and any daemon-era module name patched to raise on call — proving zero subprocess fork, zero socket bind, zero daemon import, zero capability-check fork at module import.
- The SR-2.2 capability check fires at `waggle mcp` startup, not at module import (verifiable by the assertion above).
- No file outside this Epic's new files has been modified (`git diff` against the pre-Epic base shows changes only to the new files).
- If any regression cannot be resolved within scope, the agent reports the exact failure output to the Team Lead and declares this Subtask blocked.
