---
id: t3.fg8.vv.ay.os
type: t3
title: Implement list_spawned_workers and register on stdio MCP server
down_dependencies:
- t3.fg8.vv.ay.7g
- t3.fg8.vv.ay.be
- t3.fg8.vv.ay.am
parent: t2.fg8.vv.ay
reference_materials: null
created_at: '2026-05-14T16:07:58.601488'
status: pupa
schema_version: '0.1'
guid: fg8vvayos2xstvjtkhxv8iyd6ttmv57e
---

## Context

SR-3.3: `list_spawned_workers` shells out to `claude-status workers --label waggle_owned=1` via the Epic-1 `waggle.claude_status.workers` client, projects each entry to `{instance_id, session_name}` sourcing `session_name` from `labels.waggle_session_name`, emits one JSON-per-line stderr log entry per excluded row, and recomputes on every call (no in-memory state).

## What Needs to Change

In the stdio-server module from Task 1, register `list_spawned_workers()` (no args). Body, wrapped by the Task-1 SR-7.1 wrapper:

- Calls `waggle.claude_status.workers(label="waggle_owned=1")`. The label string is hardcoded — the tool takes no orchestrator-supplied label parameter.
- On operation-failed from the client: returns that operation-failed result unchanged. The SR-7.1 wrapper's pass-through behavior covers this.
- On operation-success: extracts the parsed `{"workers": [...], "skipped": [...]}` envelope.
  - For each entry in `workers`: if `labels.waggle_session_name` is present, build `{"instance_id": entry["instance_id"], "session_name": entry["labels"]["waggle_session_name"]}` and append to the result list. If the label is missing, emit one JSON-per-line stderr log entry containing `instance_id`, `error` (a Waggle-supplied label like `"ErrMissingSessionNameLabel"` — pick one and use consistently), `error_description`. Exclude the row from the result.
  - For each entry in `skipped`: emit one JSON-per-line stderr log entry containing `instance_id`, `error` (the Claude Status `ErrName`), `error_description`. Exclude from result.
- Returns the bare `list[dict]`. The SR-7.1 wrapper handles envelope framing.

## Constraints

- Hardcoded label `"waggle_owned=1"` — no orchestrator-supplied label parameter.
- Stderr logging format: one JSON object per line (parseable). Field set: `instance_id`, `error`, `error_description`. Implementer chooses additional fields if helpful, but those three are mandatory.
- No Waggle module-level state. No caching. Each call invokes exactly one `claude-status workers` subprocess.
- No call to `claude_status.worker` or `claude_status.capabilities` from this body.

## Key Files

- `src/waggle/mcp_stdio.py` (or chosen path) — register `list_spawned_workers` and implement its body

## Acceptance Criteria

- The recorded `claude_status.workers` invocation argv contains `workers`, `--label`, `waggle_owned=1` as three separate argv entries.
- A two-row `workers` response projects to a list of two `{instance_id, session_name}` dicts; each has exactly those two keys.
- `session_name` is sourced from `labels.waggle_session_name` even when other fields in the row name a conflicting session.
- A row with missing `labels.waggle_session_name` is logged to stderr (one JSON-per-line entry with the three mandatory fields) and excluded.
- A response with non-empty `skipped` produces one JSON-per-line stderr entry per skipped row; excluded from result.
- `claude_status.workers` returning operation-failed propagates unchanged (no double-wrapping).
- Two consecutive calls record two distinct seam invocations (no caching).
