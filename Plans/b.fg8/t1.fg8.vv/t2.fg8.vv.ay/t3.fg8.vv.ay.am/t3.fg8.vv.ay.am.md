---
id: t3.fg8.vv.ay.am
type: t3
title: Add list_spawned_workers statelessness and SR-7.1 wrapping tests to tests/test_engine_list_spawned_workers.py
parent: t2.fg8.vv.ay
up_dependencies:
- t3.fg8.vv.ay.os
status: pupa
created_at: '2026-05-14T16:08:34.087609'
schema_version: '0.1'
guid: fg8vvayam9frvbrp16zn8an533fpu67g
---

## Context

The "restart survival" property is an integration property. The unit-test contribution is to verify zero module-level Waggle state — every call invokes one fresh `claude-status workers` subprocess. Combined with the SR-7.1 wrapper integration (operation-failed pass-through from the client) since both exercise repeated invocations.

## What Needs to Change

Append to `tests/test_engine_list_spawned_workers.py` statelessness + wrapper tests:

- Statelessness: invoke `list_spawned_workers()` twice with the same `fake_claude_status` stub; assert both returned lists are equal; assert `fake_claude_status` recorded exactly two seam invocations (no caching).
- Statelessness across stub changes: invoke once with a two-worker stub, then re-stage with a different one-worker stub, then invoke again; assert the second result reflects the new stub (proving no cache).
- Wrapper integration — `ErrStoreUnavailable`: stage a non-zero-exit `fake_claude_status` response with the canonical `ErrStoreUnavailable` stderr envelope from `sample_payloads`; invoke `list_spawned_workers()`; assert the result is operation-failed with `err_name == "ErrStoreUnavailable"` preserved verbatim from `claude_status`'s envelope; no exception escapes.
- Wrapper integration — `ErrSchemaMismatch`: same shape, different envelope; assert `err_name == "ErrSchemaMismatch"`.

All envelope strings sourced from `tests/sample_payloads.py`; no inline literals.

## Key Files

- `tests/test_engine_list_spawned_workers.py` — same file as sibling Subtasks; this Subtask appends statelessness + wrapper tests

## Acceptance Criteria

- The two-call statelessness test passes with `fake_claude_status`'s recorded invocation count == 2.
- The stub-change statelessness test demonstrates no cache layer.
- The `ErrStoreUnavailable` test passes with `err_name` verbatim from the envelope.
- The `ErrSchemaMismatch` test passes with `err_name` verbatim.
- Tests pass under `pytest -q`.
