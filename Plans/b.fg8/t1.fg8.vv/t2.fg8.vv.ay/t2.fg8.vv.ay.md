---
id: t2.fg8.vv.ay
type: t2
title: Implement list_spawned_workers tool
up_dependencies:
- t2.fg8.vv.em
down_dependencies:
- t2.fg8.vv.9m
parent: t1.fg8.vv
children:
- t3.fg8.vv.ay.os
- t3.fg8.vv.ay.7g
- t3.fg8.vv.ay.be
- t3.fg8.vv.ay.am
reference_materials: null
created_at: '2026-05-14T16:05:12.196693'
status: pupa
schema_version: '0.1'
guid: fg8vvayrqczcnyg8ttzb1wye6wmizfn9
---

## Context

`list_spawned_workers()` is the second production MCP tool. Per SR-3.3 it composes `waggle.claude_status.workers(label="waggle_owned=1")` (the verb shipped by Epic `t1.fg8.po`), projects each returned worker to `{instance_id, session_name}` (sourcing `session_name` from `labels.waggle_session_name`), and emits a structured JSON-per-line stderr log entry for every excluded row. The tool holds no Waggle in-memory state — every call invokes one fresh `claude-status workers` subprocess.

## What Needs to Change

- Register `list_spawned_workers()` on the stdio server from Task 1. Body wrapped in the Task-1 SR-7.1 wrapper.
- New tests in `tests/test_engine_list_spawned_workers.py` (split into argv+projection, skipped+missing-label exclusion, and statelessness+wrapping suites).

## Constraints

- The label filter `waggle_owned=1` is hardcoded; the tool signature takes no arguments per SR-3.3.
- For each entry in `workers`, project to `{instance_id: entry["instance_id"], session_name: entry["labels"]["waggle_session_name"]}`. If `waggle_session_name` is missing on a valid row, log one JSON-per-line stderr entry and EXCLUDE the row from the returned list (consistent with the skipped-row treatment).
- For each entry in the `skipped` array, emit one JSON-per-line stderr log entry containing `instance_id`, `error` (the `ErrName`), and `error_description`. Excluded from the returned list.
- When `claude_status.workers` returns operation-failed (e.g. `ErrStoreUnavailable`), the tool returns that operation-failed result unchanged (no double-wrapping by the SR-7.1 wrapper).
- Tool body returns the bare `list[dict]`; SR-7.1 wrapper handles envelope framing.
- No Waggle module-level state. No caching. Each call invokes exactly one `claude-status workers` subprocess.

## Acceptance Criteria

- The recorded `claude_status.workers` invocation argv contains `workers`, `--label`, and `waggle_owned=1` as three argv entries (verifiable via `fake_claude_status`).
- A two-row `workers` response projects to a list of two `{instance_id, session_name}` dicts; `session_name` is sourced from `labels.waggle_session_name` even when top-level fields disagree.
- Each returned dict has exactly the two keys (no leakage of other fields).
- A response with non-empty `skipped` emits exactly one JSON-parseable stderr line per skipped row containing `instance_id`, `error`, `error_description`; those rows do NOT appear in the returned list.
- A valid worker row missing `labels.waggle_session_name` is logged to stderr and excluded.
- `claude_status.workers` returning operation-failed propagates unchanged.
- Two consecutive calls record two distinct seam invocations (no caching).
- `pytest -q tests/test_engine_list_spawned_workers.py` is green.
