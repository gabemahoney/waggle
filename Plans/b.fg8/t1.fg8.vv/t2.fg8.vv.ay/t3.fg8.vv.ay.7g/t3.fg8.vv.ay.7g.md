---
id: t3.fg8.vv.ay.7g
type: t3
title: Add list_spawned_workers argv-filter and happy-path projection tests to tests/test_engine_list_spawned_workers.py
parent: t2.fg8.vv.ay
up_dependencies:
- t3.fg8.vv.ay.os
status: pupa
created_at: '2026-05-14T16:08:11.188600'
schema_version: '0.1'
guid: fg8vvay7gaoojs9nthxa3jmioanqu4yr
---

## Context

SR-3.3 contract: `list_spawned_workers` invokes `claude-status workers --label waggle_owned=1` (hardcoded label) and projects each entry to `{instance_id, session_name}` with `session_name` sourced from `labels.waggle_session_name`. This Subtask covers the argv-filter assertion and the happy-path projection. Exclusion paths and statelessness live in sibling Subtasks.

## What Needs to Change

Create `tests/test_engine_list_spawned_workers.py` with:

- Argv assertion: invoke `list_spawned_workers()` and assert via `fake_claude_status`'s recorded invocations that the seam was called with argv whose 0th element is `"workers"` and which contains `"--label"` and `"waggle_owned=1"` as separate adjacent entries. Confirm the label is the hardcoded literal `"waggle_owned=1"` — not configurable, no orchestrator-supplied variant.
- Happy-path projection: stage a multi-row `workers` response from `tests/sample_payloads.py` (at least two valid workers), invoke `list_spawned_workers()`, assert the returned list length matches the input row count and each entry has exactly the keys `instance_id` and `session_name` with values matching the canonical input.
- Source-of-truth assertion: stage a worker whose top-level fields suggest one `session_name` and whose `labels.waggle_session_name` is different; assert the label wins (verify by reading `result[0]["session_name"]`).
- No extra keys: assert each returned dict has exactly the two keys; e.g. `set(entry.keys()) == {"instance_id", "session_name"}`.

All test data from `tests/sample_payloads.py`; no inline JSON literals.

## Key Files

- `tests/test_engine_list_spawned_workers.py` — new file (this Subtask delivers argv + projection tests)
- `tests/helpers.py`, `tests/sample_payloads.py` — read-only

## Acceptance Criteria

- The argv assertion catches a refactor that changes the label literal.
- The happy-path projection test passes with at least two workers.
- The source-of-truth test catches a regression where `session_name` is sourced from a top-level field instead of `labels.waggle_session_name`.
- The no-extra-keys assertion catches accidental field leakage.
- All tests pass under `pytest -q`.
