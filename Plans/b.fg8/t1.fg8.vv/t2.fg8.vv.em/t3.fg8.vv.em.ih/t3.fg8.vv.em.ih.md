---
id: t3.fg8.vv.em.ih
type: t3
title: Append Stdio MCP launch model section to README.md
parent: t2.fg8.vv.em
up_dependencies:
- t3.fg8.vv.em.jz
status: pupa
created_at: '2026-05-14T16:06:09.912501'
schema_version: '0.1'
guid: fg8vvemih9182dt3pcsxqcji1jbe59dt
---

## Context

Initial README pass documents the stdio MCP launch model alongside the existing daemon-era documentation. The full daemon-removal rewrite belongs to Epic `t1.fg8.bp`. This Subtask only appends the new section.

## What Needs to Change

- Append a new section to `README.md` titled along the lines of "Stdio MCP launch model" describing:
  - `waggle mcp` launches one stdio MCP server per orchestrator session.
  - No daemon, no listener, no `waggle.service`.
  - The two MCP tools landed in this Epic: `spawn_worker` and `list_spawned_workers` (do NOT preview tools that don't exist yet — `terminate_worker`, `send_input`, `get_output`, `answer_question` come later in sibling Epics).
  - Dependency on Claude Status (handles permission relay, AUQ recording, state queries via `claude-status workers` / `worker` / `decide`).
- Do NOT edit, reorder, or delete the existing daemon section. The README's daemon-era language stays until Epic `t1.fg8.bp`.

## Key Files

- `README.md` — append a new section

## Acceptance Criteria

- A grep for the new section heading in `README.md` returns a hit.
- The new section names exactly two tools (`spawn_worker`, `list_spawned_workers`); does not mention `terminate_worker`, `send_input`, `get_output`, or `answer_question`.
- The existing daemon section is unchanged (`git diff` shows only an appended section, no deletions or in-place edits).
