---
id: t3.fg8.vv.iv.e6
type: t3
title: Add spawn_worker return-shape and session-name tests to tests/test_engine_spawn_worker.py
parent: t2.fg8.vv.iv
up_dependencies:
- t3.fg8.vv.iv.82
status: pupa
created_at: '2026-05-14T16:07:17.606439'
schema_version: '0.1'
guid: fg8vvive633uqc8vtwzbgfvhujti2bzh
---

## Context

SR-2.1 and SR-3.2 specify the `spawn_worker` return contract: `{instance_id, session_name}`; `instance_id` is a fresh UUID; default `session_name = waggle-<first 8 hex chars of instance_id>`; explicit `session_name` argument is preserved verbatim. This Subtask covers the shape-level invariants in isolation. Env-injection and launch-command tests live in sibling Subtasks.

## What Needs to Change

Create `tests/test_engine_spawn_worker.py` with tests covering:

- Return value of `spawn_worker(model="sonnet", repo="/tmp/x")` has exactly the keys `instance_id` and `session_name` — no extras leak.
- `instance_id` parses as a UUID (`uuid.UUID(result["instance_id"])` does not raise).
- Default `session_name` equals `"waggle-" + result["instance_id"].replace("-", "")[:8]` (or equivalent: the first 8 hex chars of the UUID, no dashes).
- `spawn_worker(model, repo, session_name="custom-name")` returns `session_name == "custom-name"` verbatim.
- Single-UUID consistency: the UUID embedded in the env-var injection (`CLAUDE_STATUS_INSTANCE_ID`) matches the UUID returned in the result. Patch the libtmux `new_session` call to record its env mapping; assert `env["CLAUDE_STATUS_INSTANCE_ID"] == returned_instance_id`.

Use `unittest.mock.patch` against `libtmux.Server` consistent with the existing `tests/test_tmux.py` pattern. Use `tests.helpers.fake_claude_status` to prove zero `claude-status` calls happen.

## Key Files

- `tests/test_engine_spawn_worker.py` — new file (this Subtask delivers return-shape and session-name tests)
- `tests/helpers.py`, `tests/sample_payloads.py` — read-only

## Acceptance Criteria

- `pytest -q tests/test_engine_spawn_worker.py::<this Subtask's tests>` is green.
- The single-UUID consistency test catches a refactor that generates two different UUIDs (one for return, one for env var).
- No pre-existing test file is modified.
- No `conftest.py` is added.
