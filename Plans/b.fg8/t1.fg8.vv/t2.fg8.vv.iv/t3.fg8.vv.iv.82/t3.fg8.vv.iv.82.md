---
id: t3.fg8.vv.iv.82
type: t3
title: Implement spawn_worker and register on stdio MCP server
up_dependencies:
- t3.fg8.vv.iv.op
down_dependencies:
- t3.fg8.vv.iv.e6
- t3.fg8.vv.iv.gi
- t3.fg8.vv.iv.px
parent: t2.fg8.vv.iv
reference_materials: null
created_at: '2026-05-14T16:07:05.465607'
status: pupa
schema_version: '0.1'
guid: fg8vviv82waz2yuuroywwpmqjjo5sotz
---

## Context

With the SR-7.1 wrapper (Task 1) and the env-injecting tmux helper (Subtask 2.1) in place, register `spawn_worker(model, repo, session_name=None)` on the stdio server per SR-3.2.

## What Needs to Change

In the stdio-server module from Task 1, register the `spawn_worker` MCP tool. The body, wrapped by the Task-1 SR-7.1 wrapper:

- Generates `instance_id` via `uuid.uuid4()`. Returns its string form.
- Default `session_name = "waggle-" + uuid.uuid4().hex[:8]` (the first 8 hex chars of the same UUID — single UUID is consistent across the return value and the env-var injection).
- Builds the env dict with all seven keys:
  - `CLAUDE_STATUS_INSTANCE_ID` = the UUID string
  - `CLAUDE_STATUS_RELAY_MODE` = `"on"`
  - `CLAUDE_STATUS_AUQ_MODE` = `"record"`
  - `CLAUDE_STATUS_LABEL_WAGGLE_OWNED` = `"1"`
  - `CLAUDE_STATUS_LABEL_WAGGLE_SESSION_NAME` = the resolved session_name
  - `CLAUDE_STATUS_LABEL_WAGGLE_MODEL` = `model.lower()`
  - `CLAUDE_STATUS_LABEL_WAGGLE_REPO` = the repo argument verbatim
- Calls `create_session_with_env(session_name, repo, env)` from `tmux.py` (Subtask 2.1).
- Sends `claude --model <lower-cased model>
` to `<session>:0.0` via the `tmux.py` send-keys path. The send-keys target is `<session>:0.0` explicitly — not the active pane.
- Returns the bare dict `{instance_id: <uuid-string>, session_name: <session_name>}`. The SR-7.1 wrapper handles envelope framing.

## Constraints

- The body does NOT import from `waggle.claude_status`. No polling. No placeholder row.
- Model lowercasing applies at BOTH sites: the `CLAUDE_STATUS_LABEL_WAGGLE_MODEL` value AND the `claude --model <model>` send-keys command. The orchestrator's input parameter is NOT mutated.
- `session_name` parameter, when provided, is used verbatim and overrides the default.
- Returns immediately after the send-keys completes; does not wait for any hook event.

## Key Files

- `src/waggle/mcp_stdio.py` (or chosen path from Task 1) — register `spawn_worker` and implement its body

## Acceptance Criteria

- `spawn_worker(model="sonnet", repo="/tmp/x")` returns an operation-success result wrapping `{"instance_id": <uuid-string>, "session_name": "waggle-<first8hex>"}`.
- The seven env vars are passed to ONE `new-session` call (from Subtask 2.1) — not split across `new-session` + `set-environment`.
- `CLAUDE_STATUS_LABEL_WAGGLE_MODEL` value is lower-cased.
- The send-keys command sent to `<session>:0.0` is `claude --model <lower-cased model>` followed by Enter.
- The body imports nothing from `waggle.claude_status`; patching `claude_status.worker` and `claude_status.workers` to raise on call does NOT affect the spawn path.
- An injected `LibTmuxException` or `OSError` at session-creation yields operation-failed via the SR-7.1 wrapper; no exception propagates.
