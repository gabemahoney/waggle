---
id: t2.fg8.fd.1s
type: t2
title: Epic-level regression gate (Epic t1.fg8.fd)
up_dependencies:
- t2.fg8.fd.da
parent: t1.fg8.fd
children:
- t3.fg8.fd.1s.16
reference_materials: null
created_at: '2026-05-14T16:15:40.789666'
status: pupa
schema_version: '0.1'
guid: fg8fd1sq9ndhe5rvf7rs8v8xudrc19tp
---

## Context

End-of-Epic regression gate. Runs after Task 1 of Epic `t1.fg8.fd` lands. Special attention: the new `answer_question` tool registration grows the stdio server's tool count beyond what Epics `t1.fg8.vv` and `t1.fg8.4n` registered; `tests/test_mcp_stdio_server.py`'s tool-count assertion must include `answer_question` (Task 1 owns that update — gate verifies).

## What Needs to Change

No new files. Run `pytest -q` from the repo root. Triage failures. Apply minimal fixes scoped to NEW files only — including `tests/test_mcp_stdio_server.py` only if Task 1 failed to grow the tool-set assertion (this file is Epic-2-introduced, not pre-existing daemon-era, so growing its assertions is in-scope for Epic 4).

## Constraints (strict)

- No modifications to pre-existing daemon-era files (`tests/test_tmux.py`, `tests/test_engine.py`, `tests/test_database.py`, daemon source modules, etc.).
- If a daemon-era test breaks, fix lives in NEW files only; if irreconcilable, report blocked.

## Acceptance Criteria

- `pytest -q` exits 0.
- Test count ≥ pre-Epic count + new tests; no daemon-era pre-existing test deleted/skipped/xfailed.
- `python -c 'import waggle.mcp_stdio'` (or chosen path) succeeds with all standard mocks — no subprocess fork, no socket bind, no daemon import.
- The stdio server's tool-set now contains `spawn_worker`, `list_spawned_workers`, `terminate_worker`, `send_input`, `get_output`, `answer_question` — exactly six tools.
- No daemon-era file modified.
