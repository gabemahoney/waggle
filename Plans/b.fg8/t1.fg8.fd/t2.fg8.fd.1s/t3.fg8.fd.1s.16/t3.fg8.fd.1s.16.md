---
id: t3.fg8.fd.1s.16
type: t3
title: Run pytest -q and verify no Epic-fd regressions
parent: t2.fg8.fd.1s
status: pupa
created_at: '2026-05-14T16:16:54.121976'
schema_version: '0.1'
guid: fg8fd1s165vuocc4eothk64m9df6bk1h
---

## Context

Sole Subtask under the Epic-level regression gate Task. Runs after Task 1 of Epic `t1.fg8.fd` has landed.

## What Needs to Change

No new files. Run `pytest -q` from the repo root. Triage any failures. Apply minimal fixes scoped to NEW files only.

Acceptable in-scope adjustments:
- Inert imports in new Waggle source.
- Interface mismatches in new test files.
- The Epic-2 introduced `tests/test_mcp_stdio_server.py` tool-set assertion update (the new `answer_question` tool brings the registered count from 5 to 6 — Task 1 owns this update; gate verifies).

Out-of-scope: any change to pre-existing daemon-era files (`tests/test_tmux.py`, `tests/test_engine.py`, `tests/test_database.py`, daemon source modules, etc.).

## Acceptance Criteria

- `pytest -q` exits 0.
- The stdio server's tool registry now contains exactly six tools: `spawn_worker`, `list_spawned_workers`, `terminate_worker`, `send_input`, `get_output`, `answer_question` — verifiable via the updated `tests/test_mcp_stdio_server.py` assertion.
- Test count ≥ pre-Epic count + new Epic-4 tests; no daemon-era pre-existing test deleted/skipped/xfailed.
- No daemon-era file modified.
- If irreconcilable, agent reports blocked.
