---
id: t3.fg8.fd.da.a3
type: t3
title: Add answer_question lookup and refusal tests (steps 1-3) to tests/test_engine_answer_question.py
parent: t2.fg8.fd.da
up_dependencies:
- t3.fg8.fd.da.8n
status: pupa
created_at: '2026-05-14T16:16:19.915634'
schema_version: '0.1'
guid: fg8fddaa3pkvzfve6htccpbyahvoq5ez
---

## Context

Steps 1-3 of SR-3.5: workers query, no-match refusal, multi-question refusal. All early returns before any tmux interaction. Shared mock setup (Claude Status seam staged, libtmux mock untouched).

## What Needs to Change

Create `tests/test_engine_answer_question.py` with lookup-and-refusal tests:

- Happy-path lookup (workers-query assertion only): stage one row with `pending.kind == "ask_user_question"` and `pending.request_id == 7` (single-question variant from `sample_payloads`); call `answer_question(question_id=7, answer="yes")`; assert recorded `fake_claude_status` invocation argv includes `--label waggle_owned=1`. Test stops at the workers-query assertion — downstream coverage belongs to sibling Subtasks.
- No-match by request_id: stage workers without the matching id (e.g. only `request_id=5`); assert operation-failed with no-pending-request err_name; libtmux capture-pane mock and send-keys mock NEVER called.
- Permission-request kind: stage row with matching `request_id` but `pending.kind == "permission_request"`; assert operation-failed with no-pending-request err_name; no libtmux interaction.
- pending is None: stage row with `pending is None`; assert operation-failed with no-pending-request err_name; no libtmux interaction.
- Multi-question refusal: stage row with single-question `pending.kind == "ask_user_question"` matching `request_id` but `pending.questions` length 2 (multi-question variant from `sample_payloads`); assert operation-failed with `err_name == "ErrMultiQuestionUnsupported"`; libtmux capture-pane and send-keys mocks NEVER called.

All test data from `tests/sample_payloads.py`. Use `tests/helpers.py:fake_claude_status`.

## Key Files

- `tests/test_engine_answer_question.py` — new file (this Subtask delivers lookup-and-refusal tests)

## Acceptance Criteria

- All listed cases pass under `pytest -q`.
- Negative-match cases assert libtmux mocks were never called (mock call count == 0).
- No inline JSON literals; all data from `sample_payloads`.
