---
id: t3.fg8.fd.da.m3
type: t3
title: Add answer_question validation, normalization, and send tests (steps 4-6) to tests/test_engine_answer_question.py
parent: t2.fg8.fd.da
up_dependencies:
- t3.fg8.fd.da.8n
status: pupa
created_at: '2026-05-14T16:16:32.860670'
schema_version: '0.1'
guid: fg8fddam392si9wx1w87rcbu8uwdkmui
---

## Context

Steps 4-6 of SR-3.5: capture pane 0:0 with 50-line scrollback; whitespace-normalize both pane and question; substring-match; on success send `answer + Enter` in a single `send-keys` call.

## What Needs to Change

Append to `tests/test_engine_answer_question.py`:

- **Happy path:** stage single-question workers response with question `"Continue?"` and `request_id=7`; stage capture-pane to return text containing `"Continue?"`; call `answer_question(question_id=7, answer="yes")`. Assert (a) capture-pane called targeting window 0, pane 0 with scrollback 50; (b) send-keys called targeting window 0, pane 0 with text `"yes"` AND Enter delivered in the same call (verify via the recorded `enter=True` flag); (c) operation-success.
- **Line-wrap normalization:** question `"Should we deploy now?"`; capture-pane returns `"Should we
deploy now?"`; assert match succeeds and send-keys called.
- **Internal-whitespace-run normalization:** question `"Are you sure?"`; capture-pane returns `"Are  you   sure?"` (multiple spaces); assert match succeeds. Repeat with tabs `"Are	you	sure?"`.
- **Leading/trailing whitespace tolerance:** question `"  Continue?  "` vs. pane `"Continue?"` (or vice versa); assert match succeeds.
- **Race refusal:** question `"Continue?"`; capture-pane returns text without it (e.g. `"$ ls -la
file.txt"`); assert operation-failed with `err_name == "ErrQuestionNoLongerVisible"`; send-keys NEVER called.
- **Send-keys target:** every happy-path test asserts recorded send-keys target is window 0, pane 0 explicitly.

Use `tests/helpers.py:fake_tmux_pane` for capture-pane staging where appropriate. All test data from `sample_payloads`.

## Key Files

- `tests/test_engine_answer_question.py` — same file as the lookup-and-refusal Subtask; this Subtask appends validation/normalization/send tests

## Acceptance Criteria

- All listed cases pass under `pytest -q`.
- The send-keys-with-Enter assertion verifies a single send-keys call with `enter=True` (not two calls).
- Line-wrap, tab, multi-space, and leading/trailing whitespace cases each pass.
- The race-refusal case asserts the send-keys mock call count is 0.
