---
id: t3.fg8.fd.da.ew
type: t3
title: Add answer_question SR-7.1 wrapper, no-call-leakage, and TOCTOU non-mitigation tests
parent: t2.fg8.fd.da
up_dependencies:
- t3.fg8.fd.da.8n
status: pupa
created_at: '2026-05-14T16:16:45.278982'
schema_version: '0.1'
guid: fg8fddaew3rg6k99j8cbwgkjh71dj92j
---

## Context

Three cross-cutting properties: SR-7.1 wrapper integration; no-call-leakage (only `workers` from `claude_status`); TOCTOU non-mitigation (one capture-pane invocation per call — no retry, no re-check).

## What Needs to Change

Append to `tests/test_engine_answer_question.py`:

- **libtmux capture-pane raise:** stage happy-path workers response, force capture-pane to raise (`LibTmuxException`); assert operation-failed via SR-7.1; no exception escapes.
- **libtmux send-keys raise:** stage happy-path workers + successful capture-pane, force send-keys to raise; assert operation-failed; no exception escapes.
- **Claude Status workers failure propagation:** stage `claude_status.workers` to return operation-failed with `err_name == "ErrStoreUnavailable"` (envelope from `sample_payloads`); assert `answer_question` returns operation-failed with the same `err_name` (propagated unchanged); libtmux capture-pane and send-keys mocks NEVER called.
- **No-call-leakage:** patch `waggle.claude_status.worker`, `waggle.claude_status.capabilities`, `waggle.claude_status.decide` each to raise `AssertionError`; run a happy-path `answer_question` invocation; assert it succeeds (i.e. none of the three patched functions was called).
- **TOCTOU non-mitigation on refusal:** stage happy-path lookup + race-refusal capture-pane; assert `fake_claude_status` recorded exactly ONE `workers` invocation and libtmux capture-pane mock called exactly ONCE — no retry, no re-checking.
- **TOCTOU non-mitigation on success:** stage happy-path success; assert capture-pane mock called exactly once (not twice).

## Key Files

- `tests/test_engine_answer_question.py` — same file as sibling Subtasks; this Subtask appends wrapper/leakage/TOCTOU tests

## Acceptance Criteria

- All listed cases pass under `pytest -q`.
- The combined test file (all three Subtasks) passes.
- No-call-leakage assertions cover all three forbidden functions.
- TOCTOU assertions verify exact invocation counts (== 1).
