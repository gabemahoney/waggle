---
id: t3.fg8.po.bi.1c
type: t3
title: Run full unit test suite and verify no Task-2 regressions
parent: t2.fg8.po.bi
up_dependencies:
- t3.fg8.po.bi.2o
- t3.fg8.po.bi.cj
status: pupa
created_at: '2026-05-14T15:53:41.027705'
schema_version: '0.1'
guid: fg8pobi1chond87k4d6y6teomuch118i
---

## Context

Adding `src/waggle/claude_status.py` and `tests/test_claude_status_client.py` is purely additive — the Epic body states "No removals from existing code in this Epic." Pre-existing daemon-era tests (`test_engine.py`, `test_database.py`, `test_rest.py`, `test_cma_client.py`, etc.) remain in place until sibling Epics `t1.fg8.bp` and `t1.fg8.2g` delete them. This Subtask is the integration gate.

**Scope rule (strict):** the fix for any regression caused by Task-2 additions lives in the NEW files. No modifications of any kind are permitted to pre-existing files — no editing, no deleting, no skipping, no xfailing, no monkeypatching imports, no `# type: ignore` shims. Pre-existing-test cleanup belongs to sibling Epics. If a regression cannot be reconciled by editing only the new files, the agent reports the failure verbatim to the Team Lead and declares this Subtask blocked. The agent does NOT mark the Subtask complete in that case.

## What Needs to Change

No new files. The agent runs `pytest -q` from the repo root after Subtasks 1–4 of this Task have landed, triages any failures or collection errors, and applies the minimal fix scoped to the new files only.

Acceptable fixes:
- Narrowing imports in `src/waggle/claude_status.py` so the module has no import-time side effects.
- Deferring a side-effectful call (e.g. accidental subprocess fork) so import alone forks no subprocess.
- Adjusting an interface mismatch in `tests/test_claude_status_client.py`.

Out-of-scope: any change to any pre-existing file in `src/` or `tests/` or anywhere else.

## Key Files

- `src/waggle/claude_status.py` — may require adjustment to ensure inert import (no subprocess fork, no PATH lookup, no file I/O at module-load time)
- `tests/test_claude_status_client.py` — may require adjustment if a test inadvertently pollutes shared state

## Acceptance Criteria

- `pytest -q` exits 0 with no failures and no errors.
- The reported test count is at least the pre-Epic count plus the new tests from Subtasks 3 and 4 of this Task; no pre-existing test has been deleted, skipped, or xfailed as a workaround.
- `python -c "import waggle.claude_status"` succeeds with `subprocess.run` patched to raise on call (proving zero subprocess invocations at module import time).
- No file outside the two new files introduced in this Task has been modified.
- If any regression cannot be resolved within scope, the agent reports the exact failure output to the Team Lead and declares this Subtask blocked.
