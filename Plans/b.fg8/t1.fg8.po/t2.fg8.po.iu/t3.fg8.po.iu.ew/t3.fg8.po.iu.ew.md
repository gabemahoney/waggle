---
id: t3.fg8.po.iu.ew
type: t3
title: Run full unit test suite and verify no Task-1 regressions
up_dependencies:
- t3.fg8.po.iu.1b
- t3.fg8.po.iu.9b
parent: t2.fg8.po.iu
reference_materials: null
created_at: '2026-05-14T15:51:48.899718'
status: worker
schema_version: '0.1'
guid: fg8poiuewdd3p13vxd8vdzoxiu6b8pcv
---

## Context

This Task is purely additive — the Epic body states "No removals from existing code in this Epic." Pre-existing daemon-era tests remain in place until sibling Epics `t1.fg8.bp` and `t1.fg8.2g` delete them. This Subtask is the integration gate that confirms the four new files (`tests/sample_payloads.py`, `tests/helpers.py`, `tests/test_sample_payloads.py`, `tests/test_helpers_smoke.py`) do not regress any previously-green pre-existing test and do not cause collection-time failures.

**Scope rule (strict):** the fix for any regression caused by Task-1 additions lives in the NEW files. No modifications of any kind are permitted to pre-existing files — no editing, no deleting, no skipping, no xfailing, no monkeypatching imports. Pre-existing-test cleanup is owned by sibling Epics. If a regression cannot be reconciled by editing only the new files, the agent reports the failure verbatim to the Team Lead and declares this Subtask blocked. The agent does NOT mark the Subtask complete in that case.

## What Needs to Change

No new files. The agent runs `pytest -q` from the repo root after the four prior Subtasks have landed, triages any failures, and applies minimal fixes scoped to the new files only. Acceptable fixes: narrowing imports in `tests/helpers.py`; deferring side-effectful operations; correcting a constant name or shape mismatch in `tests/sample_payloads.py`. Out-of-scope: any change to any pre-existing file.

## Key Files

- `tests/sample_payloads.py` — may require adjustment if shape mismatches surface
- `tests/helpers.py` — may require adjustment if interface mismatches surface
- `tests/test_sample_payloads.py` and `tests/test_helpers_smoke.py` — may require adjustment

## Acceptance Criteria

- `pytest -q` exits 0.
- The reported test count is at least the pre-Epic count plus the new tests added in Subtasks 3 and 4; no pre-existing test has been deleted, skipped, or xfailed.
- No file outside the four new files introduced in this Task has been modified.
- If any regression cannot be resolved within scope, the agent reports the exact failure output to the Team Lead and declares this Subtask blocked.
