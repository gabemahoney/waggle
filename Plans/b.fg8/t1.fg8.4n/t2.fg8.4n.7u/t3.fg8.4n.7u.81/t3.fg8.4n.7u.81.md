---
id: t3.fg8.4n.7u.81
type: t3
title: Add tests/test_engine_terminate_worker.py
parent: t2.fg8.4n.7u
up_dependencies:
- t3.fg8.4n.7u.tj
status: pupa
created_at: '2026-05-14T16:12:59.282564'
schema_version: '0.1'
guid: fg84n7u81azv5yjqfg38geosgmmbygz3
---

## Context

Validate `terminate_worker`: correct kill-session invocation, SR-7.1 wrapper integration on missing session, and absence of Claude Status polling (critical to the stateless refactor).

## What Needs to Change

Create `tests/test_engine_terminate_worker.py` covering:
- `terminate_worker(session_name="s")` records exactly one kill-session call with the bare session name `"s"`.
- A libtmux exception (e.g. missing session) yields operation-failed per SR-7.1; no exception escapes.
- Patching `waggle.claude_status.worker` to raise `AssertionError("terminate must not poll claude-status")` and asserting it is never called.
- Same for `waggle.claude_status.workers`.
- Patching `time.sleep` and `asyncio.sleep` and asserting zero calls (no polling/waiting).

Use the libtmux mock pattern from `tests/test_tmux.py`. No `conftest.py`.

## Key Files

- `tests/test_engine_terminate_worker.py` — new file

## Acceptance Criteria

- All listed cases pass under `pytest -q tests/test_engine_terminate_worker.py`.
- No-poll assertions cover both `worker`, `workers` and the sleep functions.
- No pre-existing test file modified.
- No `conftest.py` added.
