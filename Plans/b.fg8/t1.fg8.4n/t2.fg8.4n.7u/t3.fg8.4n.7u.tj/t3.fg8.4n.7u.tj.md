---
id: t3.fg8.4n.7u.tj
type: t3
title: Implement terminate_worker and register on stdio MCP server
up_dependencies:
- t3.fg8.4n.7u.r6
down_dependencies:
- t3.fg8.4n.7u.81
parent: t2.fg8.4n.7u
reference_materials: null
created_at: '2026-05-14T16:12:47.846667'
status: pupa
schema_version: '0.1'
guid: fg84n7utj9in3kifq1rmeuns5w172tjk
---

## Context

With the name-keyed kill-session helper in place (Subtask 3.1), register `terminate_worker(session_name)` on the Epic `t1.fg8.vv` stdio server. SR-3.4: Claude Status records the worker as `ended` via its lifecycle hooks — Waggle does NOT poll, wait, or insert placeholders.

## What Needs to Change

In the stdio-server module, register `terminate_worker(session_name)` wrapped by Epic `t1.fg8.vv`'s SR-7.1 wrapper. The body:
- Calls `kill_session_by_name(session_name)` from `tmux.py` (Subtask 3.1).
- Returns a bare success value (wrapper handles envelope framing).
- Does NOT call any `waggle.claude_status` verb. No polling. No `time.sleep` / `asyncio.sleep`.

## Key Files

- `src/waggle/mcp_stdio.py` (or chosen path from Epic `t1.fg8.vv`) — register `terminate_worker`

## Acceptance Criteria

- Tool registered on the stdio server.
- Body invokes the kill helper with the bare `(session_name,)` argument.
- Returns a bare success value; wrapper frames into operation-success.
- A libtmux-level error yields operation-failed via the wrapper.
- No import from `waggle.claude_status` in the `terminate_worker` body.
- No `time.sleep` / `asyncio.sleep` in the path.
