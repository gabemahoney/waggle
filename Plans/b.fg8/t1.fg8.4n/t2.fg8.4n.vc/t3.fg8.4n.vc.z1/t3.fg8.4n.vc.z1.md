---
id: t3.fg8.4n.vc.z1
type: t3
title: Run pytest -q and verify no Epic-4n regressions
parent: t2.fg8.4n.vc
status: pupa
created_at: '2026-05-14T16:13:09.336646'
schema_version: '0.1'
guid: fg84nvcz1db9h3vee6tgph4a4b5mtfsc
---

## Context

Sole Subtask under the Epic-level regression gate Task. Runs after Tasks 1, 2, 3 of Epic `t1.fg8.4n` have landed. Special attention: the Task-2 capture-pane-helper additions sit alongside the existing `_capture_pane_sync` — the gate must verify `tests/test_tmux.py` still passes without modification (no regression in active-pane default behavior for daemon-era callers).

## What Needs to Change

No new files. Run `pytest -q` from the repo root. Triage any failures. Apply minimal fixes scoped to NEW files only.

Acceptable fixes (new files only):
- Inert imports in new `tmux.py` helpers (no module-load side effects beyond what existing `tmux.py` already does).
- Interface mismatches in new test files.
- Tool-body adjustments in the stdio server module.

Out-of-scope: any change to pre-existing files including `tests/test_tmux.py`, the existing `_send_keys_sync` / `_capture_pane_sync` / `_kill_session_sync` paths, the daemon, `server.py`, etc.

## Acceptance Criteria

- `pytest -q` exits 0.
- `pytest -q tests/test_tmux.py` is green without modification.
- Test count ≥ pre-Epic count + new tests from Tasks 1–3; no pre-existing test deleted/skipped/xfailed.
- `python -c 'from waggle import tmux'` succeeds with no side effects under the standard mocking.
- No file outside this Epic's new additions modified.
- If irreconcilable, agent reports blocked; does NOT mutate any pre-existing file.
