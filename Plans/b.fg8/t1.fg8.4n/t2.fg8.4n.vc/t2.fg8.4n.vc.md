---
id: t2.fg8.4n.vc
type: t2
title: Epic-level regression gate (Epic t1.fg8.4n)
up_dependencies:
- t2.fg8.4n.g9
- t2.fg8.4n.uh
- t2.fg8.4n.7u
parent: t1.fg8.4n
children:
- t3.fg8.4n.vc.z1
reference_materials: null
created_at: '2026-05-14T16:11:25.544915'
status: pupa
schema_version: '0.1'
guid: fg84nvcskghofujv9nq6mihk6grd4m3z
---

## Context

End-of-Epic regression gate. All three tool Tasks share the `tmux.py` helper import surface and the stdio server registration surface. One gate covers all additive regressions.

## What Needs to Change

No new files. Run `pytest -q` from the repo root. Triage failures, apply minimal fixes scoped to new files only.

## Constraints (strict)

- **No modifications of any kind to pre-existing files** — including `tests/test_tmux.py`. The new tmux helpers from this Epic are ADDITIONS; they must not break the existing helpers' behavior or test coverage.
- Acceptable fixes (new files only): inert imports in new modules, interface mismatches in new tests, scoping issues in new helpers.
- If a pre-existing test breaks, fix the new files; if irreconcilable, agent reports blocked and does NOT mutate any pre-existing file.

## Acceptance Criteria

- `pytest -q` exits 0.
- `pytest -q tests/test_tmux.py` is green without modification (catches accidental regression in existing capture-pane / send-keys / kill-session paths).
- Test count ≥ pre-Epic count + Tasks 1–3 new tests; no pre-existing test deleted/skipped/xfailed.
- `python -c 'from waggle import tmux'` succeeds with `subprocess.run`, `socket.socket`, daemon-era modules patched to raise — proving no side effects.
- No file outside this Epic's new additions modified.
