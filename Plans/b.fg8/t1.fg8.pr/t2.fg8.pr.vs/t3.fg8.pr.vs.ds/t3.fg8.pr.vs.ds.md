---
id: t3.fg8.pr.vs.ds
type: t3
title: Delete hooks/settings.json.template and clean install.sh references
down_dependencies:
- t3.fg8.pr.vs.5b
parent: t2.fg8.pr.vs
reference_materials: null
created_at: '2026-05-14T16:19:32.795546'
status: pupa
schema_version: '0.1'
guid: fg8prvsdsae6ges89ynihwxs11gmvjms
---

## Context

SR-5.2 mandates deletion of `hooks/settings.json.template` and any Waggle-specific hook bodies. `install.sh` may reference them — clean those references but leave the daemon-era logic (deleted in Epic `t1.fg8.bp`).

## What Needs to Change

- Delete `hooks/settings.json.template`. If `hooks/` becomes empty, delete the directory.
- Search for and delete any other Waggle-specific Claude Code hook body files.
- Audit `install.sh`: remove references to the deleted template files. Leave daemon-era branches; annotate them with `# TODO(t1.fg8.bp): remove daemon-era logic when daemon is deleted`.
- No replacement template file. Claude Status owns hooks now.

## Key Files

- `hooks/settings.json.template` — deleted
- `hooks/` — deleted if empty
- `install.sh` — references cleaned (or deleted entirely if no surviving content)

## Acceptance Criteria

- `find hooks/ -name 'settings.json.template'` returns nothing.
- `grep -REn 'settings\.json\.template' . --exclude-dir=.git` returns nothing.
- If `install.sh` survives, it has no references to deleted templates; daemon-era logic carries the TODO comment.
- No new template/hook-body file introduced.
