---
id: t2.fg8.pr.1u
type: t2
title: Epic-level regression gate (Epic t1.fg8.pr)
up_dependencies:
- t2.fg8.pr.vs
- t2.fg8.pr.e1
parent: t1.fg8.pr
children:
- t3.fg8.pr.1u.8v
reference_materials: null
created_at: '2026-05-14T16:19:23.768759'
status: pupa
schema_version: '0.1'
guid: fg8pr1ujv5s3xfjws57tu3u15rfhbnwn
---

## Context

End-of-Epic regression gate. `tests/test_sting.py` is the EXPLICIT carve-out — its rewrite is sanctioned by the Epic body. All OTHER pre-existing files remain untouched.

## What Needs to Change

No new files. Run `pytest -q` from the repo root. Triage failures. Apply minimal fixes scoped to new files OR `tests/test_sting.py` (within the rewrite's intended scope) OR `src/waggle/sting.py` (its body is intentionally changing).

## Constraints (strict, with the documented carve-out)

- The ONLY pre-existing test file modifiable by this Epic is `tests/test_sting.py` — and only within the rewrite's scope (no restoring daemon-era assertions).
- No modifications to any OTHER pre-existing file: `tests/test_tmux.py`, `tests/test_engine.py`, `tests/test_database.py`, daemon source modules, etc.
- If a regression elsewhere is irreconcilable, agent reports blocked.

## Acceptance Criteria

- `pytest -q` exits 0.
- `git diff --name-only` against the pre-Epic baseline shows test-file changes ONLY to `tests/test_sting.py` (plus the new `tests/test_install_hooks.py`).
- The rewritten `tests/test_sting.py` contains zero references to `database`, `db_path`, `queue`, `daemon`, `registered_caller`, `set_state`, `register_caller`.
- `python -c 'from waggle import sting'` succeeds with all standard mocks; no daemon-era module import.
- `python -c 'from waggle import cli'` succeeds with no subprocess fork at import.
- No file outside this Epic's documented scope modified.
