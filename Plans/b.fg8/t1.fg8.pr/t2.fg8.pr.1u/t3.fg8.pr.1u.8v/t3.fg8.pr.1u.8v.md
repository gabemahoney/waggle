---
id: t3.fg8.pr.1u.8v
type: t3
title: Run pytest -q and verify no Epic-pr regressions
parent: t2.fg8.pr.1u
status: pupa
created_at: '2026-05-14T16:20:34.693045'
schema_version: '0.1'
guid: fg8pr1u8vzxmf2ggtq4mvje8btcw9vwo
---

## Context

End-of-Epic regression gate with the documented `tests/test_sting.py` carve-out. All other pre-existing test files remain untouched.

## What Needs to Change

No new files. Run `pytest -q` from the repo root. Triage. Apply minimal fixes scoped to: new files; `tests/test_sting.py` (rewrite scope only — no daemon-era assertions); `src/waggle/sting.py` (intentionally rewritten); `src/waggle/cli.py` (additive `install` subcommand only).

## Constraints (strict, with carve-out)

- `tests/test_sting.py` is the ONLY pre-existing test file modifiable.
- No modifications to any other pre-existing test file.
- No restoring daemon-era assertions to `test_sting.py` to silence a failure.
- If irreconcilable, agent reports blocked.

## Acceptance Criteria

- `pytest -q` exits 0.
- `git diff --name-only` against pre-Epic baseline shows test changes ONLY to `tests/test_sting.py` plus new `tests/test_install_hooks.py`.
- `grep -E 'database|db_path|queue|daemon|registered_caller|set_state|register_caller' tests/test_sting.py` returns nothing.
- `python -c 'from waggle import sting'` succeeds; no daemon-era module import.
- `python -c 'from waggle import cli'` succeeds; no subprocess fork at module import.
- No file outside Epic 5's documented scope modified.
