---
id: t3.fg8.2g.54.9x
type: t3
title: Write docs/migration.md, link from README, final docs/architecture pass
parent: t2.fg8.2g.54
status: pupa
created_at: '2026-05-14T16:26:57.142391'
schema_version: '0.1'
guid: fg82g549x1ko52bm53mwxvja3hjxc354
---

## Context

Write `docs/migration.md` and final pass on `docs/architecture/` per SR-12.

## What Needs to Change

- Write `docs/migration.md` with the six operator steps (stop daemon, uninstall, delete DB, install new, run `waggle install`, restart orchestrators) plus the orchestrator-API migration note (calls move from Waggle to `claude-status` direct).
- Add a "Migration" section to `README.md` linking to `docs/migration.md`.
- Final pass on `docs/architecture/`: keep only stdio MCP / tmux / Claude Status integration material; remove any remaining daemon-era diagrams.

## Key Files

- `docs/migration.md` — new file
- `README.md` — append Migration section with link
- `docs/architecture/` — final cleanup pass

## Acceptance Criteria

- `docs/migration.md` exists with at minimum the six operator steps.
- README contains a Migration section linking to `docs/migration.md`.
- `docs/architecture/` contains no daemon-era language (`grep -REn "systemd|REST|HTTP daemon|SSE|register_caller|approve_permission" docs/architecture/` returns nothing).

