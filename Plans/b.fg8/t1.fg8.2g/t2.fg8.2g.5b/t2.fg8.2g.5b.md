---
id: t2.fg8.2g.5b
type: t2
title: Delete SQLite layer (schema.sql, database.py, test_database.py)
down_dependencies:
- t2.fg8.2g.tv
parent: t1.fg8.2g
children:
- t3.fg8.2g.5b.e2
reference_materials: null
created_at: '2026-05-14T16:26:12.177051'
status: pupa
schema_version: '0.1'
guid: fg82g5bfs2fh4c112139rrj2n79gbasn
---

## Context

Per SR-9.1, delete Waggle's SQLite persistence layer entirely. With the daemon source already deleted in Epic 6, nothing references the schema or the database module.

## What Needs to Change

- Delete `src/waggle/schema.sql`.
- Delete `src/waggle/database.py`.
- Remove every `import sqlite3` from `src/waggle/` (audit via grep before/after).
- Delete `tests/test_database.py`.
- If the `db_path` or `registered_caller` fixtures still live in any surviving test file (they should have been removed alongside their consumers in Epic 6 — confirm), remove them now.

## Constraints

- After this Task, Waggle source must not import any SQLite driver (no `sqlite3`, no `sqlalchemy`, no `aiosqlite`).
- No backup, no migration helper. Hard cutover per the PRD non-goal.

## Acceptance Criteria

- `find src/waggle/ -name schema.sql -o -name database.py` returns nothing.
- `grep -REn "import sqlite3|from sqlite3|import sqlalchemy|import aiosqlite" src/waggle/` returns nothing.
- `find tests/ -name test_database.py` returns nothing.
- `pytest -q` is green.
