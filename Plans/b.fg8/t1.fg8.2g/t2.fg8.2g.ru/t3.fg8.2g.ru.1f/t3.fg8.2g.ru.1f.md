---
id: t3.fg8.2g.ru.1f
type: t3
title: Reduce or delete config.py and update/remove tests/test_config.py
parent: t2.fg8.2g.ru
status: pupa
created_at: '2026-05-14T16:26:57.049931'
schema_version: '0.1'
guid: fg82gru1fh34q4kjsx6pp7yoz1hz7xg3
---

## Context

Reduce or delete `config.py` per SR-8.1. No `~/.waggle/config.json` read anywhere.

## What Needs to Change

- Audit `src/waggle/config.py`. Keep only env-var helpers; remove daemon-era keys and any file-config reading code.
- If nothing meaningful remains, delete `config.py` entirely and remove its imports from any caller.
- Update `tests/test_config.py`: rewrite to cover only the surviving surface, or delete entirely if `config.py` is gone.

## Key Files

- `src/waggle/config.py` — reduce or delete
- `tests/test_config.py` — update or delete

## Acceptance Criteria

- `grep -REn "db_path|queue_path|http_port|relay_timeout_seconds|max_workers|cma_endpoint|admin_email" src/waggle/` returns nothing.
- `grep -REn "config\.json|\.waggle/config" src/waggle/` returns nothing.
- A test (in the survival case for `config.py`) or absence (in the deletion case) confirms no `~/.waggle/` directory is required at startup.
- `pytest -q` is green.

