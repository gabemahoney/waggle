---
id: t2.fg8.2g.qn
type: t2
title: Delete state_parser.py, its tests, and pane-snapshot fixtures
down_dependencies:
- t2.fg8.2g.tv
parent: t1.fg8.2g
children:
- t3.fg8.2g.qn.qi
reference_materials: null
created_at: '2026-05-14T16:26:12.269728'
status: pupa
schema_version: '0.1'
guid: fg82gqn5p95undcz117gfhuvorbso1xt
---

## Context

Per SR-6.2, `state_parser.py` and its pane-snapshot fixtures are deleted. State classification is Claude Status's responsibility now; `tmux.is_llm_running` and other detection helpers were already removed in Epic 6 Task 3.

## What Needs to Change

- Delete `src/waggle/state_parser.py`.
- Delete `tests/test_state_parser.py`.
- Delete `tests/fixtures/pane_snapshots/` directory and all its contents.
- If `tests/fixtures/` becomes empty after the pane-snapshot deletion, delete the directory.
- Audit: `grep -REn "state_parser|StateParser" src/ tests/` should return nothing after the deletions.

## Constraints

- No replacement state-classification logic in Waggle. Orchestrator uses `claude-status worker <id>` to read state.

## Acceptance Criteria

- `find src/waggle/ -name state_parser.py` returns nothing.
- `find tests/ -name test_state_parser.py` returns nothing.
- `find tests/fixtures/pane_snapshots -type f` returns nothing.
- `grep -REn "state_parser|StateParser" src/ tests/` returns nothing.
- `pytest -q` is green.
