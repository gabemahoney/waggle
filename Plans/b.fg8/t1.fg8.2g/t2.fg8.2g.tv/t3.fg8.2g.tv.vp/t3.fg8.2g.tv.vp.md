---
id: t3.fg8.2g.tv.vp
type: t3
title: Run final pytest -q and verify all Epic-2g acceptance criteria
parent: t2.fg8.2g.tv
status: pupa
created_at: '2026-05-14T16:26:57.246301'
schema_version: '0.1'
guid: fg82gtvvpr4yqgaurydpfjwowztshxfh
---

## Context

Sole Subtask under the final regression gate. After Tasks 1–4 of Epic 7 land, Waggle is fully stateless.

## What Needs to Change

No new files. Run `pytest -q` from the repo root. Triage failures; apply minimal fixes in NEW code only.

## Acceptance Criteria

- `pytest -q` exits 0.
- `grep -REn "sqlite3|sqlalchemy" src/waggle/` returns nothing.
- `find src/waggle/ -name schema.sql -o -name database.py -o -name state_parser.py` returns nothing.
- `find tests/fixtures/pane_snapshots -type f` returns nothing.
- `rm -rf ~/.waggle && waggle mcp --help` exits 0.
- A test executes a synthetic end-to-end run: install Claude Status (stubbed), run `waggle install` (mocked subprocess), launch `waggle mcp`, exercise the six tools through the FastMCP stdio surface against staged Claude Status and tmux mocks; all assertions pass.
- `docs/migration.md` exists.
- No daemon-era artifact restored.
- If irreconcilable, agent reports blocked.

