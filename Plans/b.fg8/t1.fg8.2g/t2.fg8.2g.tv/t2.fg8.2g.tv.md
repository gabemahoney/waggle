---
id: t2.fg8.2g.tv
type: t2
title: Epic-level regression gate (Epic t1.fg8.2g — final)
up_dependencies:
- t2.fg8.2g.5b
- t2.fg8.2g.qn
- t2.fg8.2g.ru
- t2.fg8.2g.54
parent: t1.fg8.2g
children:
- t3.fg8.2g.tv.vp
reference_materials: null
created_at: '2026-05-14T16:26:25.175044'
status: pupa
schema_version: '0.1'
guid: fg82gtvfskxq3kd61gi9bu5fmnzgs27c
---

## Context

Final Epic-level regression gate. This is the last Epic; after it lands, Waggle is shipped as a stateless, ephemeral CLI + stdio MCP server.

## What Needs to Change

No new files. Run `pytest -q` from the repo root after Tasks 1–4 of this Epic land. Triage failures; apply minimal fixes in code introduced in earlier Epics if needed.

## Acceptance Criteria (covering Epic body)

- `grep -REn "sqlite3|sqlalchemy" src/waggle/` returns nothing.
- `find src/waggle/ -name schema.sql -o -name database.py -o -name state_parser.py` returns nothing.
- `find tests/fixtures/pane_snapshots -type f` returns nothing.
- `pytest -q` exits 0; test count plausibly reflects the simplified surface (no daemon, no DB, no state parser, six MCP tools).
- `rm -rf ~/.waggle && waggle mcp --help` exits 0 (verifying no `~/.waggle/` dependency).
- On a fresh VM following only the README + `docs/migration.md`: install Claude Status, install Waggle, run `waggle install`, run `waggle mcp`, spawn a worker, send input, get output, answer a question, terminate the worker — full end-to-end.
- `docs/migration.md` exists with operator steps.

## Constraints

- No regression of prior Epics' deliverables.
- No restoration of any deleted artifact.
- If irreconcilable, agent reports blocked.
