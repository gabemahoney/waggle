---
id: t3.fg8.bp.er.nv
type: t3
title: Run pytest -q and verify Epic-bp acceptance criteria
parent: t2.fg8.bp.er
status: pupa
created_at: '2026-05-14T16:24:32.201969'
schema_version: '0.1'
guid: fg8bpernvyuwjodxo2pyjrvavgwsvdja
---

## Context

End-of-Epic regression gate. Covers the full Epic acceptance criteria.

## What Needs to Change

No new files. Run `pytest -q` from the repo root. Triage failures; apply minimal fixes in NEW code only (the surviving stdio-server module, the renamed tmux helpers, etc.).

## Constraints

- No restoration of any deleted daemon-era artifact.
- No `# type: ignore`, no skips, no xfails to silence failures.
- If irreconcilable, agent reports blocked verbatim.

## Acceptance Criteria

- `pytest -q` exits 0.
- All Epic-level acceptance criteria from the Task ticket body pass (the find/grep assertions in the Task ticket).
- `waggle mcp --help`, `waggle install --help`, `waggle sting --help` each exit 0.
- `waggle serve`, `waggle set-state`, `waggle permission-request`, `waggle ask-relay` each exit non-zero with argparse "unknown command".
- The stdio MCP server's tool registry contains exactly the six tools: `spawn_worker`, `list_spawned_workers`, `terminate_worker`, `send_input`, `get_output`, `answer_question`.
- No daemon-era file restored.

