---
id: t2.fg8.bp.er
type: t2
title: Epic-level regression gate (Epic t1.fg8.bp)
up_dependencies:
- t2.fg8.bp.hs
- t2.fg8.bp.qz
- t2.fg8.bp.sd
- t2.fg8.bp.rc
- t2.fg8.bp.h6
parent: t1.fg8.bp
children:
- t3.fg8.bp.er.nv
reference_materials: null
created_at: '2026-05-14T16:23:33.024892'
status: pupa
schema_version: '0.1'
guid: fg8bperi5npcviaga9wdjbwwv85r3v26
---

## Context

End-of-Epic regression gate. This Epic is the largest scope change: source deletions, CLI subcommand removals, tmux helper collapse, dependency pruning, README rewrite. The gate confirms the Epic's acceptance criteria are all met.

## What Needs to Change

No new files. Run `pytest -q` from the repo root after all Tasks 1–5 of this Epic land. Triage failures; apply minimal fixes in NEW code (the surviving stdio-server module, the new tmux helpers, etc.) — NOT in any restored daemon-era artifact.

## Acceptance Criteria (covering the full Epic body)

- `find src/ -name 'daemon.py' -o -name 'rest.py' -o -name 'server.py' -o -name 'inbound_processor.py' -o -name 'outbound_processor.py' -o -name 'queue.py' -o -name 'recovery.py' -o -name 'state_monitor.py' -o -name 'cma_client.py' -o -name 'mailer.py' -o -name 'auth.py' -o -name 'middleware.py'` returns nothing.
- `find . -name 'waggle.service' -not -path './.git/*'` returns nothing.
- `grep -REn 'starlette|uvicorn|persist_queue|persist-queue|FastAPI|HTTPException|Starlette|SSHAuthMiddleware' src/ pyproject.toml` returns nothing.
- `waggle serve`, `waggle set-state`, `waggle permission-request`, `waggle ask-relay` each exit non-zero with argparse "unknown command".
- `waggle mcp --help`, `waggle install --help`, `waggle sting --help` each exit 0.
- `grep -REn 'TODO\(t1\.fg8\.bp\)' src/` returns nothing (all parking-lot comments resolved).
- `pytest -q` exits 0.
- `grep -REn 'systemd|waggle.service|REST|HTTP daemon' README.md` returns nothing.
- README documents the six surviving MCP tools by name.
- `find docs/architecture/ -name 'relay.md' -o -name 'waggle_set_state.md'` returns nothing.
- The stdio MCP server's tool registry contains exactly the six tools.

## Constraints

- No daemon-era restoration. If a test that was deleted in this Epic is needed to pass, that's a signal the deletion was wrong — surface to Team Lead.
- No `# type: ignore`, no skips, no xfails introduced to silence failures.
- If irreconcilable, agent reports blocked verbatim.
