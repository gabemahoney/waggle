---
id: t3.fg8.bp.h6.8c
type: t3
title: Rewrite README.md for stdio MCP launch model
parent: t2.fg8.bp.h6
status: pupa
created_at: '2026-05-14T16:24:32.014068'
schema_version: '0.1'
guid: fg8bph68ctm9fgvm8e5de6rktcwgu4kc
---

## Context

Rewrite `README.md` to drop daemon-era language and document the stdio MCP launch model.

## What Needs to Change

Rewrite `README.md`:
- Remove all daemon-era language: REST/HTTP, systemd, `waggle.service`, `waggle serve`, persist-queue, CMA, mailer, SSH auth middleware, `register_caller`, `approve_permission`.
- Add: stdio MCP launch model — `waggle mcp` is launched per orchestrator session.
- Add: dependency on Claude Status (relay mode for permissions, record mode for AUQs, consumer-CLI for state reads).
- Add: install path — `waggle install` invokes `claude-status install-hooks`.
- Add: the six MCP tools by name with one-sentence descriptions: `spawn_worker`, `list_spawned_workers`, `terminate_worker`, `send_input`, `get_output`, `answer_question`.
- Add: migration story for orchestrators — state reads via `claude-status workers` / `worker`; permission approvals via `claude-status decide` (NOT via Waggle).
- Link to `docs/migration.md` (body lands in Epic 7; stub link is fine).

## Constraints

- Tool list exactly six — no preview, no removed.
- No HTTP/REST/systemd/`waggle.service`/`waggle serve` mentions.

## Acceptance Criteria

- `grep -REn "systemd|waggle\.service|REST|HTTP daemon|SSE|register_caller|approve_permission" README.md` returns nothing.
- README names all six MCP tools.
- A reader cold can follow it to install Claude Status, run `waggle install`, and run `waggle mcp`.

