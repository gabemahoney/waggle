---
id: t2.fg8.bp.qz
type: t2
title: Delete daemon source modules, waggle.service, and corresponding tests
up_dependencies:
- t2.fg8.bp.hs
down_dependencies:
- t2.fg8.bp.sd
- t2.fg8.bp.rc
- t2.fg8.bp.h6
- t2.fg8.bp.er
parent: t1.fg8.bp
children:
- t3.fg8.bp.qz.hx
reference_materials: null
created_at: '2026-05-14T16:21:50.717237'
status: pupa
schema_version: '0.1'
guid: fg8bpqzq78cweqj9moy2m6mefqnyjc8x
---

## Context

With `cli.py` decoupled (Task 1), delete the daemon-era source modules and their tests in one atomic commit per SR-1.2.

## What Needs to Change

**Source deletions:**
- `src/waggle/daemon.py`
- `src/waggle/server.py` (HTTP/FastMCP-bound — the new stdio server lives in `mcp_stdio.py`)
- `src/waggle/rest.py`
- `src/waggle/inbound_processor.py`
- `src/waggle/outbound_processor.py`
- `src/waggle/queue.py`
- `src/waggle/recovery.py`
- `src/waggle/state_monitor.py`
- `src/waggle/cma_client.py`
- `src/waggle/mailer.py`
- `src/waggle/auth.py`
- `src/waggle/middleware.py`
- `src/waggle/retry.py` (only if no surviving module imports it — confirm via grep)
- `waggle.service` (repo root)

**Test deletions (per SR-11.4):**
- `tests/test_rest.py`
- `tests/test_auth.py`
- `tests/test_middleware.py`
- `tests/test_queue.py`
- `tests/test_restart_recovery.py`
- `tests/test_state_monitor.py`
- `tests/test_cma_client.py`
- `tests/test_mailer.py`
- `tests/test_tls.py`
- `tests/test_set_state.py` (if not already deleted in Task 1)
- `tests/test_integration_cma.py`
- `tests/test_integration_ssh_spawn.py`
- `tests/test_relay_cli.py` (if not already deleted in Task 1)
- `tests/test_relay_engine.py`
- `tests/test_relay_integration.py`
- `tests/test_mcp_integration.py` — delete entirely (sibling `tests/test_mcp_stdio_server.py` from Epic 2 covers the new stdio surface)

**Test audits (keep surviving cases, delete the rest):**
- `tests/test_engine.py` — audit each test case. Cases exercising surviving behavior (paths still in use after this Epic) survive. Cases exercising deleted handlers (, , , , queue/recovery paths, etc.) are deleted.
- `tests/test_cli.py` — same audit. Cases for , ,  survive; cases for deleted subcommands deleted.
- `tests/test_package_entry_point.py` — rewrite or delete and replace. The new entry points are `waggle mcp`, `waggle install`, `waggle sting`. If this file's content was about the daemon entry point, replace it; otherwise update.

## Constraints

- All deletions land in ONE commit per atomicity — `pytest -q` is green afterward.
- No `# type: ignore` shims, no monkeypatched imports, no skipped tests in the audit pass. Either keep cleanly or delete.

## Acceptance Criteria

- `find src/ -name 'daemon.py' -o -name 'rest.py' -o -name 'server.py' -o -name 'inbound_processor.py' -o -name 'outbound_processor.py' -o -name 'queue.py' -o -name 'recovery.py' -o -name 'state_monitor.py' -o -name 'cma_client.py' -o -name 'mailer.py' -o -name 'auth.py' -o -name 'middleware.py'` returns nothing.
- `find . -name 'waggle.service' -not -path './.git/*'` returns nothing.
- `grep -REn 'SSHAuthMiddleware|register_caller|approve_permission' src/ tests/` returns nothing (or only in surviving stdio-server test surfaces that document their non-registration).
- `pytest -q` is green.
- The new stdio MCP server from Epic 2 continues to register exactly the six tools (`spawn_worker`, `list_spawned_workers`, `terminate_worker`, `send_input`, `get_output`, `answer_question`).
