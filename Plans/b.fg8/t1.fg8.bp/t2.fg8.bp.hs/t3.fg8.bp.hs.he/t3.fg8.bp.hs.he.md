---
id: t3.fg8.bp.hs.he
type: t3
title: Remove serve/set-state/permission-request/ask-relay subcommands from cli.py
parent: t2.fg8.bp.hs
status: pupa
created_at: '2026-05-14T16:24:31.634954'
schema_version: '0.1'
guid: fg8bphsheyhcs92pzh7jbnwfn6cag54g
---

## Context

Remove the four daemon-era CLI subcommands so `cli.py` is decoupled from the modules deleted in Task 2.

## What Needs to Change

In `src/waggle/cli.py`:
- Remove the subparser registrations and dispatch handlers for `serve`, `set-state`, `permission-request`, `ask-relay`.
- Remove any helper imports those handlers used IF no other code path still imports them (verify by grep before removing).

Audit `tests/test_cli.py`:
- Delete test cases exercising the four removed subcommands.
- Keep cases for `mcp`, `install`, `sting`.

If `tests/test_set_state.py` or `tests/test_relay_cli.py` cause collection failures due to the removed handlers, delete them in this commit (otherwise they're deleted in Task 2).

## Key Files

- `src/waggle/cli.py` — remove subcommands
- `tests/test_cli.py` — audit and clean

## Acceptance Criteria

- `waggle serve`, `waggle set-state`, `waggle permission-request`, `waggle ask-relay` each exit non-zero with argparse "unknown command".
- `waggle mcp --help`, `waggle install --help`, `waggle sting --help` each exit 0.
- `pytest -q` is green.

