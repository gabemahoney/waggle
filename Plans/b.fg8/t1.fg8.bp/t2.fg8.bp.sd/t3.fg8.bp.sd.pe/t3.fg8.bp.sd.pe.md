---
id: t3.fg8.bp.sd.pe
type: t3
title: Collapse duplicate tmux.py helpers and remove parking-lot TODO comments
parent: t2.fg8.bp.sd
status: pupa
created_at: '2026-05-14T16:24:31.823584'
schema_version: '0.1'
guid: fg8bpsdpeqamas1dn24q1qzrxfv7oqgu
---

## Context

The new tmux.py helpers from Epics 2, 3, 4 carry `# TODO(t1.fg8.bp): collapse with existing ... after daemon removal` comments. With daemon source gone, collapse the duplicates.

## What Needs to Change

In `src/waggle/tmux.py`:
- Delete: `_create_session_sync`, `create_session`, `_capture_pane_sync`, `capture_pane`, `_send_keys_sync`, `send_keys`, `_kill_session_sync`, `kill_session`, `is_llm_running`, `_check_llm_running_sync`, and any other state-detection helper.
- Optionally rename the surviving new helpers to drop suffixes (e.g. `create_session_with_env` → `create_session`); if renamed, update ALL call sites in the six MCP tool bodies (Epics 2–4).
- Remove all `# TODO(t1.fg8.bp): collapse ...` comments.

In `tests/test_tmux.py`:
- Delete test cases for the removed daemon-era helpers.
- Surviving cases for the kept (or renamed) helpers must pass.

## Constraints

- This is pure code-collapse. No new behavior introduced.
- Renames must propagate to ALL call sites in the new MCP tool bodies.
- Codebase green after.

## Acceptance Criteria

- `grep -RE "_create_session_sync|_capture_pane_sync|_send_keys_sync|_kill_session_sync|is_llm_running|_check_llm_running_sync" src/waggle/` returns nothing.
- `grep -REn "TODO\(t1\.fg8\.bp\)" src/` returns nothing.
- `pytest -q tests/test_tmux.py` is green.
- Full `pytest -q` is green.

