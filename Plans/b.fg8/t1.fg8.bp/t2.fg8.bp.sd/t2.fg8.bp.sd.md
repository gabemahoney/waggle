---
id: t2.fg8.bp.sd
type: t2
title: Collapse duplicate tmux.py helpers
up_dependencies:
- t2.fg8.bp.qz
down_dependencies:
- t2.fg8.bp.er
parent: t1.fg8.bp
children:
- t3.fg8.bp.sd.pe
reference_materials: null
created_at: '2026-05-14T16:22:31.037893'
status: pupa
schema_version: '0.1'
guid: fg8bpsdbrr69b6gdmvg7pzgng6gf65ef
---

## Context

Epics 2, 3, 4 each added new tmux.py helpers alongside daemon-era helpers, with TODO comments pointing at this Epic. Now the daemon-era helpers (`_create_session_sync`, `_capture_pane_sync`, `_send_keys_sync`, `_kill_session_sync`, and related `is_llm_running` / `_check_llm_running_sync` if still present) are dead code — collapse the duplication.

## What Needs to Change

In `src/waggle/tmux.py`:

- Delete daemon-era helpers no longer used after Task 2:
  - `_create_session_sync` and `create_session` (replaced by Epic 2's `_create_session_with_env_sync` / `create_session_with_env`)
  - `_capture_pane_sync` and `capture_pane` (replaced by Epic 3's `_capture_pane_explicit_sync` / `capture_pane_explicit`)
  - `_send_keys_sync` and `send_keys` (replaced by Epic 3's `_send_keys_no_enter_sync` / `send_keys_no_enter` and Epic 4's `_send_keys_with_enter_sync` / `send_keys_with_enter`)
  - `_kill_session_sync` and `kill_session` (replaced by Epic 3's `_kill_session_by_name_sync` / `kill_session_by_name`)
- Optionally rename the new helpers to drop suffixes (e.g. `create_session_with_env` → `create_session`) once the daemon-era originals are gone. If renamed, update all call sites (the six MCP tool bodies from Epics 2–4) and remove the `# TODO(t1.fg8.bp): collapse ...` comments.
- Delete `is_llm_running` and any other state-detection helpers from `tmux.py` (state classification is Claude Status's responsibility now — SR-6.2). Their `state_parser.py` consumer is deleted in Epic 7.

Update `tests/test_tmux.py`:
- Delete test cases covering the removed daemon-era helpers.
- Surviving cases for the renamed-or-kept helpers must continue to pass.

## Constraints

- Codebase green after this Task.
- Any rename must update ALL call sites in the new MCP tool bodies (Epics 2, 3, 4).
- Do not introduce new behavior; this is pure collapse.

## Acceptance Criteria

- `grep -RE "_create_session_sync|_capture_pane_sync|_send_keys_sync|_kill_session_sync|is_llm_running|_check_llm_running_sync" src/waggle/` returns nothing.
- `grep -RE "TODO\(t1\.fg8\.bp\)" src/` returns nothing (all the parking-lot comments are resolved).
- `pytest -q tests/test_tmux.py` is green.
- The six MCP tool bodies (spawn_worker, list_spawned_workers, terminate_worker, send_input, get_output, answer_question) still work end-to-end (full `pytest -q` is green).
