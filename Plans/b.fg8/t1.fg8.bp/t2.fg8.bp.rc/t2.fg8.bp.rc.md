---
id: t2.fg8.bp.rc
type: t2
title: Strip starlette, uvicorn, persist-queue from pyproject.toml
up_dependencies:
- t2.fg8.bp.qz
down_dependencies:
- t2.fg8.bp.er
parent: t1.fg8.bp
children:
- t3.fg8.bp.rc.ku
reference_materials: null
created_at: '2026-05-14T16:22:47.042080'
status: pupa
schema_version: '0.1'
guid: fg8bprcu2en9nxb7avpui945n3qssr6j
---

## Context

With the daemon source modules deleted (Task 2), prune their runtime dependencies from `pyproject.toml` per SR-9.2.

## What Needs to Change

In `pyproject.toml`:
- Remove `starlette` from `[tool.poetry.dependencies]`.
- Remove `uvicorn` from `[tool.poetry.dependencies]`.
- Remove `persist-queue` from `[tool.poetry.dependencies]`.
- Audit `httpx`: `grep -RE "import httpx|from httpx" src/` after Task 2; if no surviving source imports it, remove from dependencies. If something still imports it (e.g. by accident), surface to Team Lead.
- Keep `libtmux`. Keep `fastmcp` (its stdio transport is what Epic 2 uses).

Run `poetry lock` (or equivalent) to regenerate `poetry.lock` matching the new dependency set. Commit the updated lock file.

## Constraints

- Do NOT add any new dependency.
- Do NOT touch dev dependencies (`pytest`, `pytest-asyncio`) — those remain.

## Acceptance Criteria

- `grep -E "^(starlette|uvicorn|persist-queue)" pyproject.toml` returns nothing.
- `grep -REn "import (starlette|uvicorn|persist_queue|persistqueue)|from (starlette|uvicorn|persist_queue|persistqueue)" src/` returns nothing.
- `pip install -e .` (or `poetry install`) succeeds on a clean venv without pulling the removed packages.
- `pytest -q` is green.
