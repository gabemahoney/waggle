---
id: t3.fg8.bp.rc.ku
type: t3
title: Remove starlette/uvicorn/persist-queue from pyproject.toml and regenerate lock
parent: t2.fg8.bp.rc
status: pupa
created_at: '2026-05-14T16:24:31.917625'
schema_version: '0.1'
guid: fg8bprckuei93r9gc6n7eovyt5u729xi
---

## Context

Prune runtime dependencies that the deleted daemon source modules required.

## What Needs to Change

In `pyproject.toml` under `[tool.poetry.dependencies]`:
- Remove `starlette`.
- Remove `uvicorn`.
- Remove `persist-queue`.
- Audit `httpx`: after Task 2's source deletions, run `grep -REn "import httpx|from httpx" src/`. If nothing imports it, remove it from dependencies; if something still does, surface to Team Lead (likely a missed deletion).

Regenerate `poetry.lock` to match the new dependency set; commit the updated lock.

## Constraints

- Do NOT add any new dependency.
- Do NOT touch dev dependencies.
- Keep `libtmux` and `fastmcp`.

## Acceptance Criteria

- `grep -E "^(starlette|uvicorn|persist-queue)" pyproject.toml` returns nothing.
- `grep -REn "import (starlette|uvicorn|persist_queue|persistqueue)|from (starlette|uvicorn|persist_queue|persistqueue)" src/` returns nothing.
- `poetry install` on a clean venv succeeds without pulling the removed packages.
- `pytest -q` is green.

