---
id: t3.fkb.tf.ev.we
type: t3
title: Update pyproject.toml metadata and src/claude_spawn/__init__.py docstring
up_dependencies:
- t3.fkb.tf.ev.49
down_dependencies:
- t3.fkb.tf.ev.x5
parent: t2.fkb.tf.ev
reference_materials: null
created_at: '2026-05-16T13:28:34.919614'
status: finished
schema_version: '0.1'
guid: fkbtfevwezwmtt9qw7tbnnwhbdkjxjd7
---

## Context
The Poetry distribution metadata declares the package name, layout, and console-script entry. Subtask A has moved the source directory to `src/claude_spawn/`; `pyproject.toml` still points at the legacy `waggle` name. This subtask updates the metadata and the package's `__init__.py` docstring per SRD SR-1.1 and SR-1.2.

The current `pyproject.toml` `[tool.poetry] description` is `"Stateless stdio MCP server for managing Claude Code worker agents in tmux sessions"` — it already contains no "waggle" token, so Subtask B verifies rather than edits the description. The current `src/waggle/__init__.py` docstring is `"""Waggle - Database Layer for Agent State Management."""` (a stale, factually wrong description) — replace with Claude-Spawn-voiced text reflecting the package's actual purpose. Preserve `__version__ = "0.1.0"`.

## What Needs to Change
- `pyproject.toml [tool.poetry]`: set `name = "claude-spawn"`; set `packages = [{include = "claude_spawn", from = "src"}]`. Verify (do not blindly rewrite) that `description` contains no case-insensitive "waggle" substring; if it does, re-word generically.
- `pyproject.toml [tool.poetry.scripts]`: replace with exactly one entry `claude-spawn = "claude_spawn.cli:main"`. The legacy `waggle` key must not appear.
- `src/claude_spawn/__init__.py`: rewrite the docstring with the Claude Spawn voice and an accurate one-line description. Preserve the `__version__ = "0.1.0"` attribute verbatim.
- Do NOT touch any other `pyproject.toml` section (dependencies, pytest config, build-system) and do NOT touch any other source file.

## Key Files
- `pyproject.toml` — name, packages, scripts, verify description
- `src/claude_spawn/__init__.py` — docstring rewrite, preserve `__version__`

## Acceptance Criteria
- `pyproject.toml` shows `name = "claude-spawn"`.
- `pyproject.toml` shows `packages = [{include = "claude_spawn", from = "src"}]`.
- `pyproject.toml` `[tool.poetry.scripts]` table contains exactly one entry: `claude-spawn = "claude_spawn.cli:main"`. No `waggle` key remains.
- `grep -i waggle pyproject.toml` returns no matches.
- After `poetry install`, the `claude-spawn` executable is on `PATH`; `which waggle` returns nothing.
- `claude-spawn --help` exits 0.
- `python -c "import claude_spawn; print(claude_spawn.__version__)"` prints `0.1.0`.
- `src/claude_spawn/__init__.py` docstring contains no case-insensitive "waggle" substring.
