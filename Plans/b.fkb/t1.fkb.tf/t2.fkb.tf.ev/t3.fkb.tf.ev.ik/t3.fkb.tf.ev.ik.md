---
id: t3.fkb.tf.ev.ik
type: t3
title: Rewrite prose mentions of Waggle in src/claude_spawn/ docstrings, log/error strings, comments — excluding deferred data values
up_dependencies:
- t3.fkb.tf.ev.49
down_dependencies:
- t3.fkb.tf.ev.x5
parent: t2.fkb.tf.ev
reference_materials: null
created_at: '2026-05-16T13:28:38.413792'
status: finished
schema_version: '0.1'
guid: fkbtfevikhzmf6q4uwfbqo1ecjrqjmjf
---

## Context
After Subtasks A–C, the only "Waggle"/"waggle" tokens remaining in `src/claude_spawn/` (excluding the explicitly deferred data values) live in module/class/function docstrings, log-message format strings, exception-message format strings, and inline source comments. Per PRD ("Every CLI help string, error message, and log message must use the new name. Every `__init__.py`, module docstring, and class docstring must use the new name."), these prose mentions are renamed in this Epic.

The sharp boundary: identifier strings inside *prose* are renamed; identifier strings that ARE the data value (label keys, env-var prefixes, MCP server name, tmux prefix, regex contents) are NOT.

## What Needs to Change
Update prose mentions of "Waggle" / "waggle" in module docstrings, class docstrings, function docstrings, log-message format strings, exception-message format strings, and comments across `src/claude_spawn/` to use "Claude Spawn" / `claude-spawn` per the PRD identifier table. Specific surfaces include but are not limited to:

- `mcp_stdio.py` module docstring; tool docstrings referencing "Waggle-owned workers", "Waggle process restarts", "waggle mcp", "waggle.server".
- `spawn.py` module docstring; `answer_question_impl` step-list line about "Fetch waggle-owned workers via Claude Status"; `list_spawned_workers_impl` docstring about "Query Claude Status for waggle-owned workers"; stderr log line emitted on skipped rows (drop the `waggle ` token from `"waggle list_spawned_workers: ..."`).
- `sting.py` module docstring; `_detect_mcp` docstring referring to "waggle MCP entry"; `check_claude_status_health` docstring referring to `waggle.claude_status.capabilities()`; **user-facing error string `"Install claude-status and run 'waggle install'."` flips to `'claude-spawn install'`**; `handle_sting` docstring.
- `claude_status.py` module docstring (lines naming "This module is the only path through which Waggle interacts...", "Waggle-originated", "import waggle.claude_status performs no subprocess fork...", patch-target prose).
- `installer.py` module docstring; **user-facing error message that says `"re-run 'waggle install'"` flips to `'claude-spawn install'`**; `handle_install` docstring; `_remove_legacy_hook_template` comment that references "the old Waggle hook template file".

## EXPLICITLY DO NOT modify the following data-value literals (deferred to later Epics):
- Label-key strings `waggle_owned`, `waggle_session_name`, `waggle_model`, `waggle_repo` anywhere → Epic 3 (`t1.fkb.zr`).
- Env-var name fragments `CLAUDE_STATUS_LABEL_WAGGLE_*` in `_REQUIRED_ENV_VARS` → Epic 3.
- `--label waggle_owned=1` argv string passed to `claude-status workers` → Epic 3.
- Tmux session-name prefix literal `"waggle-"` (the `f"waggle-{instance_id[:8]}"` form in `spawn.py`) → Epic 4 (`t1.fkb.y2`).
- MCP `FastMCP("waggle-stdio")` argument in `mcp_stdio.py` → Epic 5 (`t1.fkb.si`).
- Regex pattern `(?i)(?:^|[-_])waggle(?:$|[-_])` and the symbol names `_WAGGLE_PATTERN`, `_key_matches_waggle`, `_has_waggle_in_mcp_servers` in `sting.py` → Epic 6 (`t1.fkb.jg`).

The CLI/argparse help-text changes are owned by Subtask C — do not duplicate. Test files are owned by Subtask E — do not touch.

## Key Files
- `src/claude_spawn/mcp_stdio.py` — module docstring + tool docstring prose (NOT `FastMCP("waggle-stdio")`)
- `src/claude_spawn/spawn.py` — module + impl docstrings + stderr log line (NOT label keys, NOT env-var names, NOT tmux prefix literal)
- `src/claude_spawn/sting.py` — module + function docstrings + user-facing `'waggle install'` error string (NOT `_WAGGLE_PATTERN` family)
- `src/claude_spawn/claude_status.py` — module docstring
- `src/claude_spawn/installer.py` — module + function docstrings + user-facing `'waggle install'` error string

## Acceptance Criteria
- `grep -RIn -i "waggle" src/claude_spawn/` returns ONLY matches from the deferred surfaces enumerated above (label keys, env-var names, tmux prefix literal, `FastMCP("waggle-stdio")`, `_WAGGLE_*` symbol family). No prose, docstring, log, error, or comment matches remain.
- User-facing error messages for the install/sting paths reference `claude-spawn install`, not `waggle install`.
- The `_remove_legacy_hook_template` function still works (untouched logic); only its comment changes.
- `poetry run pytest` continues to pass (no test assertions on prose strings should break — tests assert on error envelopes, not prose).
