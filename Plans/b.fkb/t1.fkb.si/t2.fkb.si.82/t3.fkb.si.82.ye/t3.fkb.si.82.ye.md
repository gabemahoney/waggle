---
id: t3.fkb.si.82.ye
type: t3
title: Run full unit-test suite; verify 213 collected, 212/1/1 green (count +1 from new test)
up_dependencies:
- t3.fkb.si.82.ww
- t3.fkb.si.82.uj
parent: t2.fkb.si.82
reference_materials: null
created_at: '2026-05-16T13:38:35.680495'
status: done
schema_version: '0.1'
guid: fkbsi82ye7jbmvw2g1emm1vym4msytdf
---

## Context
After Subtasks A (source) and B (new test), the MCP server's advertised name and its docstring example are renamed, and a new unit test asserts the server name. This subtask verifies the result. Note: this Epic adds ONE new test method (Subtask B), so the post-Epic test count rises from 212 to 213.

## What Needs to Change
No source or test edits.
- Run `poetry run pytest --collect-only` from the repo root; confirm clean collection.
- Run `poetry run pytest` from the repo root; confirm the expected pass/skip/fail counts.
- Confirm the single expected failure is the pre-existing environmental `tests/spikes/test_ask_relay_spike.py::TestAskRelayEndToEnd::test_sentinel_injected_via_hook` and no other test regresses.

## Key Files
- (verification only — no code changes expected unless a new failure surfaces)

## Acceptance Criteria
- `poetry run pytest --collect-only` exits 0 and reports exactly 213 tests collected (212 baseline + 1 new test added by Subtask B).
- `poetry run pytest` final summary reads `212 passed, 1 skipped, 1 failed`.
- The single failing test is the pre-existing environmental `tests/spikes/test_ask_relay_spike.py::TestAskRelayEndToEnd::test_sentinel_injected_via_hook` — no new failures.
- `poetry run pytest tests/test_mcp_stdio_server.py -v` shows the new server-name assertion passing.
- If a new failure surfaces, it is either fixed in this subtask (test-side edit only) or escalated to the team lead.
