---
id: t3.fkb.jg.y2.1q
type: t3
title: Run full unit-test suite; verify 213 collected, 212/1/1 green
up_dependencies:
- t3.fkb.jg.y2.a4
- t3.fkb.jg.y2.3g
parent: t2.fkb.jg.y2
reference_materials: null
created_at: '2026-05-16T13:41:01.949821'
status: done
schema_version: '0.1'
guid: fkbjgy21q1jnkwyo9ai7yn431agicqbp
---

## Context
After Subtasks A (source) and B (tests), the sting MCP-detection pattern + helpers + matching test class are renamed and rewritten per SR-7.2. This subtask is the verification gate.

## What Needs to Change
No source or test edits.
- Run `poetry run pytest --collect-only` from the repo root; confirm clean collection.
- Run `poetry run pytest` from the repo root; confirm the expected pass/skip/fail counts.
- Confirm the single expected failure is the pre-existing environmental `tests/spikes/test_ask_relay_spike.py::TestAskRelayEndToEnd::test_sentinel_injected_via_hook` and no other test regresses.

## Key Files
- (verification only — no code changes expected unless a new failure surfaces)

## Acceptance Criteria
- `poetry run pytest --collect-only` exits 0 and reports exactly 213 tests collected (unchanged from end of Epic 5 — same number of pattern + detect-mcp methods, just renamed/rewritten).
- `poetry run pytest` final summary reads `212 passed, 1 skipped, 1 failed`.
- The single failing test is the pre-existing environmental `tests/spikes/test_ask_relay_spike.py::TestAskRelayEndToEnd::test_sentinel_injected_via_hook` — no new failures.
- `poetry run pytest tests/test_sting.py::TestClaudeSpawnPattern -v` shows positive and negative match cases pass.
- `poetry run pytest tests/test_sting.py::TestDetectMcp -v` shows MCP-detection cases pass against `claude-spawn`-rooted fixture keys.
- If a new failure surfaces, it is either fixed in this subtask (test-side edit only) or escalated to the team lead.
