---
id: t3.fkb.jg.y2.3g
type: t3
title: Rewrite TestWagglePattern as TestClaudeSpawnPattern; update TestDetectMcp fixtures
down_dependencies:
- t3.fkb.jg.y2.1q
parent: t2.fkb.jg.y2
reference_materials: null
created_at: '2026-05-16T13:40:57.811743'
status: finished
schema_version: '0.1'
guid: fkbjgy23g4fxpxiv22vhpfcqjoctobp4
---

## Context
The Engineer is renaming `_WAGGLE_PATTERN` → `_CLAUDE_SPAWN_PATTERN`, `_key_matches_waggle` → `_key_matches_claude_spawn`, `_has_waggle_in_mcp_servers` → `_has_claude_spawn_in_mcp_servers`, and updating the regex contents. The test file `tests/test_sting.py` must flip imports, rename the pattern-matching test class, rewrite its test cases per SR-7.2, and update the `TestDetectMcp` fixture JSON to use `claude-spawn`-rooted `mcpServers` keys. Same number of test methods before and after — pure replacement.

## What Needs to Change
- Update the `from claude_spawn.sting import (...)` block to import the renamed symbols: `_CLAUDE_SPAWN_PATTERN`, `_has_claude_spawn_in_mcp_servers`, `_key_matches_claude_spawn`. The other names in the import block (e.g. `_detect_mcp`, `check_claude_status_health`, `handle_sting`) are unchanged.
- Rename the class `TestWagglePattern` → `TestClaudeSpawnPattern`. Replace every `_WAGGLE_PATTERN` reference inside the class body with `_CLAUDE_SPAWN_PATTERN`. Rewrite the five test methods with new input strings per SR-7.2:
  - `test_pattern_matches_exact` — assert `.search("claude-spawn")` and `.search("claude_spawn")` are non-None.
  - `test_pattern_matches_with_suffix` — assert `.search("claude_spawn_server")` is non-None (the SR-7.2 positive list does not include `claude-spawn-mcp` as a literal, but the regex permits any `[-_]<rest>` suffix; choose strings from the SR-7.2 positive case list).
  - `test_pattern_matches_with_prefix` — assert `.search("my-claude-spawn")` is non-None (and optionally `my_claude_spawn` if added).
  - `test_pattern_no_match_embedded` — assert `.search("claudespawn")`, `.search("myclaudespawn")`, `.search("claude-spawnfish")`, `.search("claude-spawned")` all return None.
  - `test_pattern_case_insensitive` — assert `.search("CLAUDE-SPAWN")` and `.search("Claude-Spawn")` are non-None.
- Update `TestDetectMcp`:
  - `test_detect_from_claude_json` — change the `mcpServers` fixture key from `"waggle"` to a `claude-spawn`-rooted key (e.g. `"claude-spawn"`).
  - `test_detect_from_settings_json` — change the `mcpServers` fixture key from `"waggle-server"` to a `claude-spawn`-rooted key (e.g. `"claude-spawn-server"`).
  - Rename the method `test_no_waggle_key_returns_false` → `test_no_claude_spawn_key_returns_false`. Keep the negative test's `"other-tool"` fixture key (it's already non-matching for both patterns).
  - The two neutral methods (`test_not_found_when_no_config`, `test_malformed_json_returns_false`) are unchanged.
- DO NOT touch other test classes in the file (`TestCheckClaudeStatusHealth`, `TestHandleSting`).
- The total number of test methods in the file must be unchanged — five methods in the renamed pattern class plus five in `TestDetectMcp` (matching the pre-rename count).

## Key Files
- `tests/test_sting.py` — imports, pattern test class rewrite, `TestDetectMcp` fixture keys and one method rename.

## Acceptance Criteria
- `grep -n "_WAGGLE_PATTERN\|_key_matches_waggle\|_has_waggle_in_mcp_servers" tests/test_sting.py` returns no matches.
- `grep -n "TestWagglePattern" tests/test_sting.py` returns no matches.
- `grep -n "class TestClaudeSpawnPattern" tests/test_sting.py` returns exactly one match.
- The pattern test class exercises the six positive cases (`claude-spawn`, `claude_spawn`, `my-claude-spawn`, `CLAUDE-SPAWN`, `claude_spawn_server`, `Claude-Spawn`) and the four negative cases (`claudespawn`, `claude-spawnfish`, `myclaudespawn`, `claude-spawned`).
- `TestDetectMcp.test_detect_from_claude_json` and `test_detect_from_settings_json` use `claude-spawn`-rooted `mcpServers` keys.
- `TestDetectMcp` contains a method named `test_no_claude_spawn_key_returns_false` (and no method named `test_no_waggle_key_returns_false`).
- The file's other test classes (`TestCheckClaudeStatusHealth`, `TestHandleSting`) are unmodified.
- `python -m py_compile tests/test_sting.py` succeeds.
