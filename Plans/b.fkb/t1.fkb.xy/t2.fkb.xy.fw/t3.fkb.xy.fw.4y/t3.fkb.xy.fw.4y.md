---
id: t3.fkb.xy.fw.4y
type: t3
title: Update docs/architecture/*.md (3 files; flip WAGGLE_ env-var symbols but preserve known inaccuracy)
down_dependencies:
- t3.fkb.xy.fw.jg
parent: t2.fkb.xy.fw
reference_materials: null
created_at: '2026-05-16T13:42:57.672701'
status: finished
schema_version: '0.1'
guid: fkbxyfw4yxp1xsrds954xmn9j1kmnnhn
---

## Context
`docs/architecture/` contains three internal architecture documents describing the spawn-worker, send-input, and terminate-worker subsystems. They use "Waggle" / "waggle" / `waggle` for project-name prose and identifier strings. After Epics 1–6, the production code uses Claude Spawn names. This subtask updates the architecture docs in lockstep.

`docs/architecture/spawn_worker.md` has a known pre-existing inaccuracy: it lists env-var names like `WAGGLE_INSTANCE_ID` that do not match the actual `CLAUDE_STATUS_*`-prefixed env vars the code injects. **The inaccuracy is NOT corrected in this subtask** — it is a follow-up bee. The only edit to those env-var entries is the `WAGGLE_` → `CLAUDE_SPAWN_` token flip so the doc no longer references Waggle.

## What Needs to Change
- `docs/architecture/spawn_worker.md`:
  - Replace every "Waggle" / "waggle" / `waggle` occurrence with the appropriate Claude Spawn form (`Claude Spawn` for prose, `claude_spawn` for module paths, `claude-spawn` for the binary, `claude-spawn-stdio` for the MCP server name).
  - In the env-var list, flip `WAGGLE_INSTANCE_ID` → `CLAUDE_SPAWN_INSTANCE_ID` (and any sibling `WAGGLE_*` symbolic names → `CLAUDE_SPAWN_*`). Do NOT correct the underlying factual inaccuracy that these symbolic names don't match the actual `CLAUDE_STATUS_*` env vars — leave that for a follow-up bee.
- `docs/architecture/send_input.md`: replace every "Waggle" / "waggle" / `waggle` occurrence with the appropriate Claude Spawn form.
- `docs/architecture/terminate_worker.md`: same.
- DO NOT introduce new sections or rewrite logic descriptions. Only name/identifier token swaps.

## Key Files
- `docs/architecture/spawn_worker.md` — full pass; flip env-var symbolic names but don't fix the inaccuracy.
- `docs/architecture/send_input.md` — full pass.
- `docs/architecture/terminate_worker.md` — full pass.

## Acceptance Criteria
- `grep -i "waggle" docs/architecture/` returns zero matches across all three files.
- `grep -n "WAGGLE_" docs/architecture/spawn_worker.md` returns zero matches.
- `grep -n "CLAUDE_SPAWN_INSTANCE_ID" docs/architecture/spawn_worker.md` returns at least one match (the renamed env-var symbol — still factually inaccurate but no longer Waggle-named).
- Each doc's overall structure (section headings, line count ±3) is preserved.
