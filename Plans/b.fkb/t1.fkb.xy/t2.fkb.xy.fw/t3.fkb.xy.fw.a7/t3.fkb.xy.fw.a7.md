---
id: t3.fkb.xy.fw.a7
type: t3
title: Update docs/spikes/*.md (2 files; preserve SUPERSEDED annotations)
down_dependencies:
- t3.fkb.xy.fw.jg
parent: t2.fkb.xy.fw
reference_materials: null
created_at: '2026-05-16T13:42:57.908621'
status: finished
schema_version: '0.1'
guid: fkbxyfwa75sbn6itmijis22behd5dvb5
---

## Context
`docs/spikes/` contains two spike documents: `ask-relay-spike.md` and `claude-channels-spike.md`. They describe historical or superseded design exploration and may carry `SUPERSEDED` annotations. They use "Waggle" / "waggle" / `waggle` in prose and identifier references. This subtask updates the spikes in the Claude Spawn voice, preserving the `SUPERSEDED` annotations in spirit (the annotations themselves only change in their name token, not their meaning).

## What Needs to Change
- `docs/spikes/ask-relay-spike.md`: replace every "Waggle" / "waggle" / `waggle` occurrence with the appropriate Claude Spawn form. Preserve any `SUPERSEDED` annotations — only the name token within them changes (e.g. "SUPERSEDED by Waggle v2" → "SUPERSEDED by Claude Spawn v2"). Prose like "the original Waggle ask-relay protocol" reworks to "the original Claude Spawn ask-relay protocol".
- `docs/spikes/claude-channels-spike.md`: same handling — replace tokens; preserve `SUPERSEDED` annotations in spirit.
- DO NOT introduce new sections, rewrite design decisions, or modify code-fence contents beyond the name-token swaps.

## Key Files
- `docs/spikes/ask-relay-spike.md` — full pass; preserve `SUPERSEDED` annotations.
- `docs/spikes/claude-channels-spike.md` — full pass; preserve `SUPERSEDED` annotations.

## Acceptance Criteria
- `grep -i "waggle" docs/spikes/` returns zero matches across both files.
- Any `SUPERSEDED` annotation lines that existed pre-task still exist post-task (only the name token within them has changed).
- Each doc's overall structure (section headings, line count ±3) is preserved.
