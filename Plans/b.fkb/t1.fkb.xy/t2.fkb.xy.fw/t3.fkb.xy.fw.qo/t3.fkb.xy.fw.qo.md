---
id: t3.fkb.xy.fw.qo
type: t3
title: Rewrite README.md in Claude Spawn voice (install, MCP registration, CLI usage, module paths)
down_dependencies:
- t3.fkb.xy.fw.jg
parent: t2.fkb.xy.fw
reference_materials: null
created_at: '2026-05-16T13:42:54.000786'
status: finished
schema_version: '0.1'
guid: fkbxyfwqoinnz5qextsd27c6gd4wi9bb
---

## Context
`README.md` is the public-facing introduction to the project. After Epics 1–6, the package name, CLI binary, MCP server name, env vars, label keys, tmux prefix, and sting pattern are all renamed. The README still uses "Waggle" / "waggle" throughout — installation, MCP registration, CLI usage, architecture/module-layout, and prose mentions. This subtask rewrites it in the Claude Spawn voice.

The README is ~177 lines and contains ~16 case-insensitive `waggle` references.

## What Needs to Change
- Replace every "Waggle" / "waggle" / `waggle` occurrence with the appropriate Claude Spawn form per the PRD identifier table:
  - Title-Case prose name: "Claude Spawn".
  - Distribution / CLI / `mcpServers` key: `claude-spawn` (kebab-case).
  - Python package / module path: `claude_spawn` (snake_case).
  - MCP `serverInfo.name`: `claude-spawn-stdio`.
- Update the installation snippet to read `pip install . && claude-spawn install` (or whatever the current README's install snippet shape is, with the binary renamed).
- Update the MCP-registration example so the recommended `mcpServers.<KEY>` is `claude-spawn` — e.g. `claude mcp add claude-spawn -- claude-spawn mcp` (preserve the existing command shape; only the names flip).
- Update CLI-usage examples to read `claude-spawn sting`, `claude-spawn mcp`, `claude-spawn install`.
- Update the architecture/module-layout section so module paths read `src/claude_spawn/__init__.py`, `src/claude_spawn/cli.py`, etc.
- Update any project-name prose (e.g. "Waggle is a stateless stdio MCP server...") to "Claude Spawn is a stateless stdio MCP server..."
- DO NOT introduce new content beyond name/path replacements (no new sections, no new examples).

## Key Files
- `README.md` — full pass replacing waggle/Waggle with the appropriate Claude Spawn / claude-spawn / claude_spawn form depending on context.

## Acceptance Criteria
- `grep -i "waggle" README.md` returns zero matches.
- `grep -n "claude-spawn" README.md` returns matches for the binary name in the install, MCP-registration, and CLI-usage examples.
- `grep -n "claude_spawn" README.md` returns matches for the Python package/module path references.
- `grep -n "Claude Spawn" README.md` returns matches for the project name in prose.
- The README's overall structure (sections, code-fence shapes, line count ±5) is preserved.
