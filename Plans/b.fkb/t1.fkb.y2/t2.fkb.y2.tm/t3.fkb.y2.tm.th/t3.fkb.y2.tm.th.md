---
id: t3.fkb.y2.tm.th
type: t3
title: Run full unit-test suite; verify 212 collected, 211/1/1 green
up_dependencies:
- t3.fkb.y2.tm.5x
- t3.fkb.y2.tm.ki
parent: t2.fkb.y2.tm
reference_materials: null
created_at: '2026-05-16T13:35:26.810151'
status: done
schema_version: '0.1'
guid: fkby2tmthn4h1feyze1cjj9v74teifcv
---

## Context
After Subtasks A (source) and B (tests), the default tmux session prefix is renamed across `claude_spawn.spawn` and its test file. This subtask is the verification gate. It must run AFTER A and B.

## What Needs to Change
No source or test edits.
- Run `poetry run pytest --collect-only` from the repo root; confirm clean collection.
- Run `poetry run pytest` from the repo root; confirm the expected pass/skip/fail counts.
- Confirm the single expected failure is the pre-existing environmental `tests/spikes/test_ask_relay_spike.py::TestAskRelayEndToEnd::test_sentinel_injected_via_hook` and no other test regresses.

## Key Files
- (verification only — no code changes expected unless a new failure surfaces)

## Acceptance Criteria
- `poetry run pytest --collect-only` exits 0 and reports exactly 212 tests collected.
- `poetry run pytest` final summary reads `211 passed, 1 skipped, 1 failed`.
- The single failing test is the pre-existing environmental `tests/spikes/test_ask_relay_spike.py::TestAskRelayEndToEnd::test_sentinel_injected_via_hook` — no new failures.
- `poetry run pytest tests/test_engine_spawn_worker.py -v -k "session_name"` shows the prefix and pass-through assertions pass with `spawn-` prefixes.
- If a new failure surfaces, it is either fixed in this subtask (test-side edit only) or escalated to the team lead.
