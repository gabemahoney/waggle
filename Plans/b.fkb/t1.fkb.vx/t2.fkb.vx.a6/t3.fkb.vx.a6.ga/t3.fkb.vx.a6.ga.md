---
id: t3.fkb.vx.a6.ga
type: t3
title: Remove TestCliReference class and _CLI_REFERENCE import from tests/test_sting.py
down_dependencies:
- t3.fkb.vx.a6.em
parent: t2.fkb.vx.a6
reference_materials: null
created_at: '2026-05-16T13:20:39.724557'
status: finished
schema_version: '0.1'
guid: fkbvxa6ganzo2hx3zjd2aajj1jxfxdcm
---

## Context
Subtask 1.2 removes the `_CLI_REFERENCE` constant from `src/waggle/sting.py`. Once that constant is gone, the `_CLI_REFERENCE` name in the `from waggle.sting import (...)` block at the top of `tests/test_sting.py` is dangling, and the sole consumer `class TestCliReference` becomes unrunnable. This subtask removes both. The two changes (source-side constant removal in 1.2; test-side cleanup here) must land in the same commit because pytest collection imports the test file at module level.

The other test classes in `tests/test_sting.py` (`TestWagglePattern`, `TestDetectMcp`, `TestCheckClaudeStatusHealth`, `TestHandleSting`) are unaffected. `TestLegacyTemplateRemoval` lives in `tests/test_install_hooks.py` and is preserved.

## What Needs to Change
- In `tests/test_sting.py`: remove the `_CLI_REFERENCE,` entry from the multi-name `from waggle.sting import (...)` import block at the top. The other names in that import block (`_WAGGLE_PATTERN`, `_detect_mcp`, `_has_waggle_in_mcp_servers`, `_key_matches_waggle`, `check_claude_status_health`, `handle_sting`) must remain.
- In `tests/test_sting.py`: remove the entire `class TestCliReference` block (its sole method is `test_contains_all_subcommands`).
- No edits to any other class, import, or helper in the file. No edits to any other test file.

## Key Files
- `tests/test_sting.py` — drop the `_CLI_REFERENCE` import name and delete `class TestCliReference`.

## Acceptance Criteria
- `grep -n 'class TestCliReference' tests/test_sting.py` returns no matches.
- `grep -n '_CLI_REFERENCE' tests/test_sting.py` returns no matches.
- `grep -rn '_CLI_REFERENCE' tests/` returns no matches across the entire test tree.
- `poetry run python -c "import tests.test_sting"` succeeds with no `ImportError` (after Subtask 1.2 has also landed).
- The remaining test classes in `tests/test_sting.py` (`TestWagglePattern`, `TestDetectMcp`, `TestCheckClaudeStatusHealth`, `TestHandleSting`) are present and unmodified.
