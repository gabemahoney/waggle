---
id: t3.fkb.vx.a6.ev
type: t3
title: Remove _CLI_REFERENCE constant from src/waggle/sting.py
down_dependencies:
- t3.fkb.vx.a6.em
parent: t2.fkb.vx.a6
reference_materials: null
created_at: '2026-05-16T13:20:38.642555'
status: finished
schema_version: '0.1'
guid: fkbvxa6evqrybzn6vdezfoooq5bzgt1a
---

## Context
`_CLI_REFERENCE` is a module-level triple-quoted string constant in `src/waggle/sting.py` describing the legacy daemon CLI (mentions `serve`, `set-state`, `sting`). It is dead — `handle_sting()` does not print it, and nothing else in production code reads it. Its sole consumer is `TestCliReference` in `tests/test_sting.py`, which is removed in Subtask 1.3 (the two removals must land in the same commit, since this Task is one commit).

The Epic explicitly preserves `_remove_legacy_hook_template()` in `src/waggle/installer.py` and its `TestLegacyTemplateRemoval` tests — do NOT touch those. Identifier renames (e.g. `_WAGGLE_PATTERN`, `_key_matches_waggle`) belong to Epic `t1.fkb.jg`, not this Epic.

## What Needs to Change
In `src/waggle/sting.py`: remove the `_CLI_REFERENCE` module-level constant (the triple-quoted string assignment near the top of the file, between the `_WAGGLE_PATTERN` definition and the helper functions). Leave every surrounding line untouched — module docstring, other imports, `_WAGGLE_PATTERN`, `_key_matches_waggle()`, `_has_waggle_in_mcp_servers()`, `_detect_mcp()`, `check_claude_status_health()`, `handle_sting()` all remain.

## Key Files
- `src/waggle/sting.py` — remove the `_CLI_REFERENCE` constant only.

## Acceptance Criteria
- `grep -n '_CLI_REFERENCE' src/waggle/sting.py` returns no matches.
- `src/waggle/sting.py` still defines (intact): `_WAGGLE_PATTERN`, `_key_matches_waggle`, `_has_waggle_in_mcp_servers`, `_detect_mcp`, `check_claude_status_health`, `handle_sting`.
- The module still imports successfully when the test-side cleanup (Subtask 1.3) also lands: `poetry run python -c "import waggle.sting"` exits 0.
