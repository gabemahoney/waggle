---
id: t3.fkb.vx.a6.em
type: t3
title: Run full unit-test suite; verify 212 collected, 211/1/1 green
up_dependencies:
- t3.fkb.vx.a6.xc
- t3.fkb.vx.a6.ev
- t3.fkb.vx.a6.ga
parent: t2.fkb.vx.a6
reference_materials: null
created_at: '2026-05-16T13:20:43.656557'
status: finished
schema_version: '0.1'
guid: fkbvxa6emu6fdn21qdt78mryyckcoagj
---

## Context
Final verification gate for Task 1. After Subtasks 1.1 (filesystem deletions), 1.2 (constant removal), and 1.3 (test-class removal) have landed, the full pytest collection should drop by exactly one test (the deleted `test_contains_all_subcommands`) and the suite should remain otherwise green. This subtask verifies that and investigates any new failures the deletions may have surfaced. This subtask depends on 1.1, 1.2, and 1.3 — it must run AFTER all three.

## What Needs to Change
- Run `poetry run pytest --collect-only` from the repo root and confirm clean collection (no `ImportError`, no `ModuleNotFoundError`).
- Run `poetry run pytest` from the repo root and confirm the expected pass/skip/fail counts.
- Verify the single expected failure is `tests/spikes/test_ask_relay_spike.py::TestAskRelayEndToEnd::test_sentinel_injected_via_hook` — the pre-existing environmental failure (the spike launches the real `claude` CLI and Claude refuses the prompt as a prompt-injection attempt). Any other failure is a regression introduced by this Task and must be fixed before closing the subtask.
- Do NOT modify `tests/spikes/test_ask_relay_spike.py::TestAskRelayEndToEnd::test_sentinel_injected_via_hook` — that failure is environmental and out of scope.

## Key Files
- (verification only — no code changes expected unless a new failure surfaces that the Engineer/Test Writer subtasks missed)

## Acceptance Criteria
- `poetry run pytest --collect-only` exits 0 and reports exactly 212 tests collected (down from 213).
- `poetry run pytest` final summary reads `211 passed, 1 skipped, 1 failed`.
- The single failing test is `tests/spikes/test_ask_relay_spike.py::TestAskRelayEndToEnd::test_sentinel_injected_via_hook` — no new failures introduced by this Task.
- If a new failure surfaces, it is either fixed in this subtask (test-side edit only — production code is owned by the Engineer) or escalated to the team lead with a clear root-cause note.
