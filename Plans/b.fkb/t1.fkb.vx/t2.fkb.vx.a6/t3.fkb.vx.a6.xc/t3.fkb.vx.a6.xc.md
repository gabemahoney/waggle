---
id: t3.fkb.vx.a6.xc
type: t3
title: Delete obsolete daemon-era filesystem paths
down_dependencies:
- t3.fkb.vx.a6.em
parent: t2.fkb.vx.a6
reference_materials: null
created_at: '2026-05-16T13:20:37.118181'
status: finished
schema_version: '0.1'
guid: fkbvxa6xcvragmyrta5d2exg3sm6z6ts
---

## Context
Five filesystem paths target the removed daemon surface (`waggle serve`, the docker CI flow, the opencode state-tracker plugin, the in-tree CI/release-test skills, and the operator install script). None are referenced by surviving Python, `pyproject.toml`, `README.md`, or CI config — the only inbound references are internal to the deletion set itself. They can be removed wholesale as a single filesystem change with no code-side edits.

## What Needs to Change
Delete these paths from the working tree:
- `install.sh` (single file).
- `docker/` (directory; contains `Dockerfile`, `entrypoint.sh`, `phase1.sh`, `phase2.sh`, `auto_approve.sh`).
- `plugins/` (directory; the only child is `plugins/opencode/state-tracker.ts`).
- `.claude/skills/ci/` (directory; contains `SKILL.md`).
- `.claude/skills/release-test/` (directory; contains `SKILL.md`).

No edits to `pyproject.toml`, `.gitignore`, `README.md`, or any CI config are required — none reference these paths.

## Key Files
- `install.sh` — deleted in full.
- `docker/` — directory deleted in full.
- `plugins/` — directory deleted in full.
- `.claude/skills/ci/` — directory deleted in full.
- `.claude/skills/release-test/` — directory deleted in full.

## Acceptance Criteria
- `ls install.sh docker plugins .claude/skills/ci .claude/skills/release-test 2>&1 | grep -c "No such"` returns `5`.
- `grep -rn -E '(install\.sh|state-tracker|docker/Dockerfile|skills/ci|skills/release-test)' --include='*.py' --include='*.toml' --include='*.md' .` returns no matches outside the `tickets/` tree.
- `poetry run pytest --collect-only` still exits 0 (collection count unchanged by this subtask alone — collection drops to 212 after Subtask 1.3).
