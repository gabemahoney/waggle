---
id: t3.fkb.zr.rz.kt
type: t3
title: Run full unit-test suite; verify 212 collected, 211/1/1 green
up_dependencies:
- t3.fkb.zr.rz.uy
- t3.fkb.zr.rz.er
parent: t2.fkb.zr.rz
reference_materials: null
created_at: '2026-05-16T13:34:03.894639'
status: finished
schema_version: '0.1'
guid: fkbzrrzktti3kdubd3bv25ri8kzfqmez
---

## Context
After Subtasks A (source) and B (tests), the env-var prefix and label-key namespace are renamed across `claude_spawn.spawn` and the seven affected test files. This subtask is the verification gate. It must run AFTER A and B.

## What Needs to Change
No source or test edits.
- Run `poetry run pytest --collect-only` from the repo root; confirm clean collection.
- Run `poetry run pytest` from the repo root; confirm the expected pass/skip/fail counts.
- Confirm the single expected failure is the pre-existing environmental `tests/spikes/test_ask_relay_spike.py::TestAskRelayEndToEnd::test_sentinel_injected_via_hook` and no other test regresses.
- DO NOT modify `tests/spikes/test_ask_relay_spike.py::TestAskRelayEndToEnd::test_sentinel_injected_via_hook` — that failure is environmental and out of scope.

## Key Files
- (verification only — no code changes expected unless a new failure surfaces)

## Acceptance Criteria
- `poetry run pytest --collect-only` exits 0 and reports exactly 212 tests collected.
- `poetry run pytest` final summary reads `211 passed, 1 skipped, 1 failed` (or equivalent ordering).
- The single failing test is `tests/spikes/test_ask_relay_spike.py::TestAskRelayEndToEnd::test_sentinel_injected_via_hook` — no new failures introduced by this Task.
- `grep -RIn "CLAUDE_STATUS_LABEL_WAGGLE\|waggle_owned=\|waggle_session_name\|waggle_model\|waggle_repo" src/claude_spawn/ tests/` returns no matches as data values.
- If a new failure surfaces, it is either fixed in this subtask (test-side edit only — production code is owned by the Engineer) or escalated to the team lead with a clear root-cause note.
