---
id: t3.6UNsyn
type: t3
title: Add CLAUDE_CODE_AGENT_TYPE gating tests to test_hooks.py
status: larva
parent: t2.QpxV9
up_dependencies:
- t3.kp5ksd
down_dependencies:
- t3.P3tzD3
guid: 6UNsynML4C4LXECorQFAG4
schema_version: 1.0.0
---

## Context
The existing test helper `run_set_state_hook()` already accepts an `env` parameter for additional environment variables, making it straightforward to test the gating logic. Follow the existing test patterns: nested test classes, descriptive test names, use existing fixtures (temp_home, waggle_dir, db_path, set_state_hook).

## What Needs to Change
- In `tests/test_hooks.py`:
  - Add a new test class (e.g., `TestAgentTypeGating`) with tests covering:
    - `CLAUDE_CODE_AGENT_TYPE=teammate` → no DB entry created (pass via `env` param)
    - `CLAUDE_CODE_AGENT_TYPE=team-lead` → DB entry created normally
    - `CLAUDE_CODE_AGENT_TYPE` not in env → DB entry created normally (backward compat)
    - `CLAUDE_CODE_AGENT_TYPE=""` (empty) → DB entry created normally (treat empty as unset)
    - `CLAUDE_CODE_AGENT_TYPE=teammate` → exit code is 0 (silent skip)
    - `CLAUDE_CODE_AGENT_TYPE=teammate` → no stderr output
    - Multiple agent types: verify only `team-lead` and unset result in writes
  - Use `run_set_state_hook(env={"CLAUDE_CODE_AGENT_TYPE": "teammate"})` pattern
  - Verify DB state using existing `get_db_value()` helper

## Key Files
- `tests/test_hooks.py`

## Acceptance Criteria
- All new gating tests pass
- Tests verify both positive (write happens) and negative (write skipped) cases
- Tests verify silent behavior (no stderr, exit 0) for skipped writes
- Backward compatibility explicitly tested (env var unset)
