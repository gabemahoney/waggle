---
id: t3.5ymCbd
type: t3
title: Update tests/test_tmux.py with session creation and resolution tests
status: finished
parent: t2.7GXw6
up_dependencies:
- t3.Z8afVc
down_dependencies:
- t3.CvC4WH
guid: 5ymCbdm5jNhZ5ySQViTrpu
schema_version: 1.0.0
---

## Context
The new tmux.py functions (create_session, launch_agent_in_pane, resolve_session) need test coverage. Tests mock libtmux — no running tmux required.

## What Needs to Change
Add test classes to `tests/test_tmux.py`:
- **Session creation tests**: successful creation, TmuxSessionExists error, tmux unavailable
- **Agent launch tests**: correct CLI command for claude with model flag, opencode without model, settings appended
- **Session resolution 4 cases** (SR-6.2):
  1. Session doesn't exist → returns {action: "create"}
  2. Session exists + LLM running → returns error
  3. Session exists + no LLM + same repo → returns {action: "reuse"}
  4. Session exists + no LLM + different repo → returns error
- **LLM detection**: claude/opencode detected (case-insensitive), non-LLM commands (bash, vim) return false
- **Error handling**: libtmux exceptions converted to structured error dicts

## Key Files
- `tests/test_tmux.py` — add test classes

## Acceptance Criteria
- All 4 session resolution cases have dedicated tests
- LLM detection tests for claude, opencode, and non-LLM commands
- Error cases return structured dicts, not exceptions
- Tests pass: `poetry run pytest tests/test_tmux.py -v`
- All libtmux calls mocked
