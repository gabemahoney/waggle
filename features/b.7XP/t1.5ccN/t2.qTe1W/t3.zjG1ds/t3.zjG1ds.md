---
id: t3.zjG1ds
type: t3
title: Document all 4 new MCP tools in README with parameters and examples
status: pupa
parent: t2.qTe1W
up_dependencies:
- t3.A1hprt
down_dependencies:
- t3.dseRRP
guid: zjG1dsbVWMLnS6JQExZvic
schema_version: 1.0.0
---

## Context
Four new MCP tools have been added to waggle. Each needs user-facing documentation with parameters, return contracts, and example usage scenarios per SR-12.1.

## What Needs to Change
Add documentation sections for each tool:
- **read_pane**: params (session_id, pane_id, scrollback), return contract ({status, agent_state, content, prompt_data}), agent_state values (working/done/ask_user/check_permission/unknown), example showing state detection
- **send_command**: params (session_id, command, pane_id), state-aware behavior (refuses working/unknown), formatting rules (done=freeform, ask_user=option number, check_permission=1 or 2), return contract ({status, message}), examples for different states
- **spawn_agent**: params (repo, session_name, agent, model, command, settings), session resolution logic (4 cases), command delivery behavior (immediate vs wait), return contract ({status, session_id, session_name, message}), example with initial command
- **close_session**: params (session_id, session_name, force), force protection behavior, return contract ({status, message}), example with force flag

## Key Files
- `README.md` — add tool documentation sections

## Acceptance Criteria
- All 4 tools documented with complete parameter descriptions
- Each tool has at least one example usage scenario
- Return contracts clearly specified
- State-aware behaviors explained for send_command
- Session resolution logic explained for spawn_agent
- Force protection explained for close_session
