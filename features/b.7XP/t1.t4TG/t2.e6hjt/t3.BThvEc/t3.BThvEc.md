---
id: t3.BThvEc
type: t3
title: Create tests/test_state_parser.py with full test coverage
status: finished
parent: t2.e6hjt
up_dependencies:
- t3.BN41Wj
down_dependencies:
- t3.GSTsgC
guid: BThvEc4cmQLhMau5tAM1ma
schema_version: 1.0.0
---

## Context
All parser tests must load fixture files from `tests/fixtures/pane_snapshots/` — no inline string literals for pane content (SR-11.3). Follow existing test patterns: nested test classes with descriptive names, pytest fixtures, `unittest.mock`.

## What Needs to Change
- Create `tests/test_state_parser.py` with:
  - A pytest fixture that loads snapshot files from `tests/fixtures/pane_snapshots/`
  - **Classification tests**: verify each state is correctly identified from its corresponding fixture
    - working.txt → `"working"` state
    - done.txt → `"done"` state
    - ask_user.txt → `"ask_user"` state
    - check_permission.txt → `"check_permission"` state
    - unknown.txt → `"unknown"` state
  - **Prompt data extraction tests**:
    - ask_user: verify question text extracted, options list contains correct number/label/description for each option
    - check_permission: verify tool type, command text, and description extracted
    - working/done/unknown: verify prompt_data is None
  - **False positive prevention tests**:
    - A `>` character in agent output text (e.g., blockquote) must NOT trigger done state
    - A numbered list in agent output must NOT trigger ask_user state
    - An empty/blank pane must return unknown, not done
  - **Edge cases**: empty string input, very long content

## Key Files
- `tests/test_state_parser.py` (new)

## Acceptance Criteria
- `poetry run pytest tests/test_state_parser.py` passes
- Each of the 4 states correctly classified from fixture snapshots
- prompt_data correctly extracted for ask_user and check_permission
- No false positives from numbered lists or `>` in output text
- unknown returned for unrecognized content with no crash
