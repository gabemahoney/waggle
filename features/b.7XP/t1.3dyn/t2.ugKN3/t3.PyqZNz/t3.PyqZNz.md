---
id: t3.PyqZNz
type: t3
title: Create tests/test_tmux.py for the new tmux module
status: finished
parent: t2.ugKN3
up_dependencies:
- t3.JcGNqS
down_dependencies:
- t3.jAHWua
guid: PyqZNzhHkDGaE76A7BxPQd
schema_version: 1.0.0
---

## Context
The new tmux module needs its own test file. Tests must mock libtmux objects (Server, Session, Window, Pane) — no running tmux server required. Follow the existing test patterns in the repo: nested test classes with descriptive names, pytest fixtures, `unittest.mock` (patch, MagicMock).

## What Needs to Change
- Create `tests/test_tmux.py` with test classes covering:
  - **Session enumeration**: correct data extraction from mocked sessions, empty server returns empty list, error handling
  - **Active session keys**: correct composite key format `{name}+{session_id}+{created}`, empty server returns empty set
  - **LLM detection**: `claude` → True, `opencode` → True, `Claude` → True (case-insensitive), `zsh` → False, `bash` → False, `node` → False
  - **Error handling**: `LibTmuxException` caught and returns structured error response
  - **Tmux unavailable**: Server() fails → returns empty results gracefully (no crash)

## Key Files
- `tests/test_tmux.py` (new)

## Acceptance Criteria
- `poetry run pytest tests/test_tmux.py` passes
- All main branches covered: success path, tmux unavailable, libtmux exceptions
- Tests use mocked libtmux objects only — no real tmux server needed
