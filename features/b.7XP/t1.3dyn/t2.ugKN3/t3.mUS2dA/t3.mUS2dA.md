---
id: t3.mUS2dA
type: t3
title: Add libtmux dependency to pyproject.toml
status: finished
parent: t2.ugKN3
down_dependencies:
- t3.JcGNqS
guid: mUS2dA4jdkweme3cRmpL6D
schema_version: 1.0.0
---

## Context
libtmux is pre-1.0 (v0.53.0 as of Dec 2025) with potential breaking changes. Must be pinned to minor version range for stability per SRD SR-1.1. This is the first step before any libtmux code can be written.

## What Needs to Change
- Add `libtmux = "^0.53"` under `[tool.poetry.dependencies]` in `pyproject.toml`
- Run `poetry lock` and `poetry install` to update the environment

## Key Files
- `pyproject.toml`

## Acceptance Criteria
- `poetry install` succeeds without errors
- `python -c "import libtmux"` works in the poetry environment
