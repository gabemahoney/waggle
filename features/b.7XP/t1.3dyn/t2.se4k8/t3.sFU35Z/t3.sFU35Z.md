---
id: t3.sFU35Z
type: t3
title: Update test_server.py list_agents tests to mock libtmux
status: finished
parent: t2.se4k8
up_dependencies:
- t3.d2k1pP
down_dependencies:
- t3.rRPG17
guid: sFU35ZkcjD8gxHv8NSgVbC
schema_version: 1.0.0
---

## Context
Existing tests in `TestListAgents`, `TestListAgentsCustomStates`, `TestListAgentsCleanupIntegration`, and `TestCustomStateEndToEnd` classes mock `subprocess.run` and build formatted stdout strings using `SEP` (tab separator). After migration, these must mock `waggle.tmux` functions instead.

## What Needs to Change
- In `tests/test_server.py`:
  - Update or replace the `mock_tmux_subprocess` fixture to mock `waggle.tmux` functions instead of `subprocess.run`
  - Update all tests in `TestListAgents` class: replace formatted subprocess stdout mocks with mocked tmux.py return values
  - Update `TestListAgentsCustomStates` tests similarly
  - Update `TestListAgentsCleanupIntegration` and `TestCustomStateEndToEnd` integration tests — these currently use `mock_tmux_run` side_effect functions that parse format strings; replace with tmux.py mocks
  - Preserve ALL behavioral assertions — only the mocking layer changes
  - Keep `SEP` / `TMUX_FIELD_SEP` imports for now if still needed by cleanup_dead_sessions tests (removed in Task 3)

## Key Files
- `tests/test_server.py`

## Acceptance Criteria
- All list_agents test classes pass with updated mocks
- Same test count and behavioral coverage as before migration
- No tests still mocking `subprocess.run` for list_agents code paths
