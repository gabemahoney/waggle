---
id: t3.VbVZ5f
type: t3
title: Update tests/test_server.py with TestSpawnAgent tests
status: finished
parent: t2.s7ang
up_dependencies:
- t3.6JC42z
- t3.THuqsA
down_dependencies:
- t3.BP9fFt
guid: VbVZ5fBThkDm8pVRZDJ9UX
schema_version: 1.0.0
---

## Context
The spawn_agent MCP tool needs comprehensive test coverage following existing patterns in test_server.py (mock_ctx, temp_db fixtures, mocked tmux layer).

## What Needs to Change
Add `TestSpawnAgent` class to tests/test_server.py:
- Basic spawn without command: creates session, registers DB, returns immediately
- Spawn with command: polls until done, delivers command (mock read_pane sequence: working → done)
- Readiness timeout: agent never reaches done within 60s, returns error with last state
- DB registration: entry created immediately with correct composite key and "working" status
- Session resolution: all 4 cases via mocked tmux.py responses
  - New session created successfully
  - LLM already running → error
  - Reuse existing session (same repo) → success
  - Wrong repo → error
- Parameter validation: invalid agent value rejected
- Return contract: all paths return {status, session_id, session_name, message}
- Use existing fixtures: mock_ctx, temp_db/mock_db_path, mock libtmux via waggle.tmux patches

## Key Files
- `tests/test_server.py` — add TestSpawnAgent class

## Acceptance Criteria
- All spawn_agent scenarios covered (happy paths, errors, timeout)
- Both with-command and without-command paths tested
- Session resolution 4 cases tested
- DB registration verified
- Tests mock tmux layer, not subprocess
- Tests pass: `poetry run pytest tests/test_server.py::TestSpawnAgent -v`
