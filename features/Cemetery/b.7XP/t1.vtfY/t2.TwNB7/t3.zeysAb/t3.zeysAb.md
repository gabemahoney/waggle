---
id: t3.zeysAb
type: t3
title: Update tests/test_server.py with send_command tests
status: finished
parent: t2.TwNB7
up_dependencies:
- t3.q27ZGP
down_dependencies:
- t3.G5dkrc
guid: zeysAbiv1xWxsHA6UbjAPj
schema_version: 1.0.0
---

## Context
The send_command MCP tool needs comprehensive test coverage in test_server.py. Tests should follow existing patterns (mock_ctx, mock DB, mock tmux layer).

## What Needs to Change
- Add TestSendCommand class to tests/test_server.py with tests for:
  - Happy path: deliver text to done-state agent, verify success return
  - State rejection: working state returns "agent is busy" error
  - State rejection: unknown state returns "agent state unknown, cannot safely send" error
  - DB validation: unregistered session_id returns error
  - ask_user formatting: valid option number accepted and sent as keystroke
  - ask_user formatting: invalid option number rejected with descriptive error
  - check_permission formatting: "1" and "2" accepted
  - check_permission formatting: invalid value (e.g., "3", "yes") rejected
  - Delivery verification: successful transition detected within timeout
  - Delivery verification: timeout after 5s returns error without retry
  - Pane targeting: specific pane_id passed through correctly
  - Pane targeting: omitted pane_id falls back to active pane
- Mock the tmux.py functions (send_keys_to_pane, clear_pane_input, read_pane_content) and state_parser
- Use existing test fixtures (mock_ctx, temp_db, etc.)

## Key Files
- `tests/test_server.py` — add TestSendCommand class

## Acceptance Criteria
- All send_command scenarios covered (happy path, state rejection, formatting, verification, targeting)
- Both valid and invalid inputs tested for ask_user and check_permission states
- Delivery verification tests cover both success and timeout paths
- No-retry behavior explicitly verified (timeout returns error, no second send attempt)
- Tests mock libtmux layer, not subprocess
- Tests follow existing patterns in test_server.py
