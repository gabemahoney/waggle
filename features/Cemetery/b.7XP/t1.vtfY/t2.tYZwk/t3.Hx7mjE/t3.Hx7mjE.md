---
id: t3.Hx7mjE
type: t3
title: Update tests/test_tmux.py with send_keys and input clearing tests
status: finished
parent: t2.tYZwk
up_dependencies:
- t3.sEnhc9
down_dependencies:
- t3.1tjfHC
guid: Hx7mjEhNepbaP9EsvvqBPy
schema_version: 1.0.0
---

## Context
The tmux.py module is being extended with send_keys_to_pane() and clear_pane_input(). These need test coverage following the existing test patterns in test_tmux.py (created in t1.3dyn).

## What Needs to Change
- Add tests for `send_keys_to_pane()`:
  - Sends text with Enter to mocked pane
  - Sends text without Enter when enter=False
  - Targets specific pane_id when provided
  - Falls back to active pane when pane_id omitted
  - Returns structured error when session not in DB
  - Returns structured error when pane_id not found in session
  - Catches libtmux exceptions gracefully
- Add tests for `clear_pane_input()`:
  - Sends Ctrl+C to mocked pane
  - Targets correct pane (specific or active)
  - Error handling matches send_keys_to_pane pattern
- Mock libtmux — no running tmux required

## Key Files
- `tests/test_tmux.py` — add test classes/methods

## Acceptance Criteria
- All send_keys_to_pane scenarios tested (happy path, pane targeting, error cases)
- All clear_pane_input scenarios tested
- Tests mock libtmux, not subprocess
- Tests follow existing patterns in test_tmux.py
