---
id: t3.e6w5vB
type: t3
title: Update tests/test_server.py with TestCloseSession tests
status: finished
parent: t2.hRbbf
up_dependencies:
- t3.6cE4r7
down_dependencies:
- t3.ad3UqX
guid: e6w5vBaAaAbyi1D5Cfv8Hv
schema_version: 1.0.0
---

## Context
close_session needs comprehensive test coverage following existing test_server.py patterns.

## What Needs to Change
Add `TestCloseSession` class to tests/test_server.py:
- Success case: registered session, no LLM, kills and removes DB entry
- DB validation error: unregistered session_id returns error
- Name/ID mismatch error: session_name and session_id refer to different sessions
- LLM protection without force: LLM running + force=false → error with "force=true" message
- LLM protection with force: LLM running + force=true → proceeds with kill
- Partial failure: DB removed but tmux kill fails → specific error message
- DB-first ordering: verify DB delete happens before tmux kill
- Return contract: all paths return {status, message}
- Use existing fixtures: mock_ctx, temp_db, mock libtmux via waggle.tmux patches

## Key Files
- `tests/test_server.py` — add TestCloseSession class

## Acceptance Criteria
- All close_session scenarios covered
- LLM protection tested both with and without force
- DB-first ordering verified
- Partial failure path tested
- Tests mock tmux layer, not subprocess
- Tests pass: `poetry run pytest tests/test_server.py::TestCloseSession -v`
