---
id: t3.NaHqhA
type: t3
title: Add read_pane tests to test_server.py
status: larva
parent: t2.gvS35
up_dependencies:
- t3.s1uTGv
down_dependencies:
- t3.bpcc3N
guid: NaHqhA3dcMoi9u25P1tLi9
schema_version: 1.0.0
---

## Context
read_pane tests follow the existing pattern in test_server.py: mock the tmux layer and DB, test validation logic and return contracts. Use pytest.mark.asyncio for async tests.

## What Needs to Change
- In `tests/test_server.py`, add test class `TestReadPane` (or similar) covering:
  - Returns correct state for each of 5 agent states (mock tmux to return fixture-like content, mock state_parser to return expected state)
  - content field always populated
  - prompt_data populated for ask_user/check_permission, None for others
  - Unregistered session_id returns error
  - Invalid pane_id returns descriptive error
  - Scrollback parameter passed through to tmux layer
  - pane_id omitted → active pane targeted
  - pane_id provided → specific pane targeted
  - tmux capture failure returns error (not crash)

## Key Files
- `tests/test_server.py`

## Acceptance Criteria
- All read_pane tests pass
- Validation, return contract, and error paths all covered
- Mocking approach consistent with existing test patterns
