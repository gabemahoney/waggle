---
id: t3.dSZNT4
type: t3
title: Update test_server.py cleanup tests and remove legacy test infrastructure
status: finished
parent: t2.j3MZX
up_dependencies:
- t3.ayg41E
- t3.qeYHa5
down_dependencies:
- t3.WwrTbd
guid: dSZNT4kNh46hs2uLQXXFLF
schema_version: 1.0.0
---

## Context
Tests in `TestCleanupDeadSessions` class mock `subprocess.run` and build formatted stdout strings. After migration, these must mock `waggle.tmux` functions instead. This is also the place to remove all remaining legacy test infrastructure.

## What Needs to Change
- In `tests/test_server.py`:
  - Update all tests in `TestCleanupDeadSessions` class to mock tmux.py's active session key function instead of `subprocess.run`
  - Remove `from waggle.server import TMUX_FIELD_SEP` import
  - Remove `SEP = TMUX_FIELD_SEP` shorthand constant
  - Remove `mock_tmux_subprocess` fixture (should no longer be used by any test)
  - Preserve all behavioral assertions: orphan removal, no-tmux handling, DB error handling, active session preservation, batch deletion

## Key Files
- `tests/test_server.py`

## Acceptance Criteria
- All cleanup_dead_sessions tests pass with updated mocks
- No references to `TMUX_FIELD_SEP`, `mock_tmux_subprocess`, or `subprocess.run` for tmux calls remain in test file
- Same behavioral coverage as before migration
