---
id: t3.ayg41E
type: t3
title: Refactor cleanup_dead_sessions to use tmux.py
status: finished
parent: t2.j3MZX
down_dependencies:
- t3.qeYHa5
- t3.dSZNT4
guid: ayg41EyyvRhERZEBG6TSvP
schema_version: 1.0.0
---

## Context
`cleanup_dead_sessions` (~lines 314-369 of `server.py`) uses `subprocess.run(["tmux", "list-sessions", "-F", TMUX_FMT_KEYS_ONLY])` to enumerate sessions and build a set of active composite keys for orphan detection. Replace with tmux.py's active session key function.

## What Needs to Change
- In `src/waggle/server.py`:
  - Import the active session key function from `waggle.tmux`
  - Replace the `subprocess.run(["tmux", "list-sessions", "-F", TMUX_FMT_KEYS_ONLY])` block with a call to tmux.py
  - Use the returned set of composite keys directly for orphan detection (no parsing needed)
  - Preserve: orphan detection logic (DB keys not in active set), batch deletion with IN clause, silent failure (all exceptions caught, never raises)
  - Note: `cleanup_dead_sessions` is currently synchronous. If tmux.py's function is async-only, either expose a synchronous inner function from tmux.py or make cleanup_dead_sessions async. The caller (`list_agents`) is already async so this is flexible.

## Key Files
- `src/waggle/server.py`

## Acceptance Criteria
- cleanup_dead_sessions correctly removes orphaned DB entries
- Silent failure on all errors (never raises exceptions)
- No subprocess calls in the function
- Orphan detection and batch deletion logic preserved
