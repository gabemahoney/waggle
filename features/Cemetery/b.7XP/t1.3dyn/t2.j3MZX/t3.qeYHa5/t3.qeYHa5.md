---
id: t3.qeYHa5
type: t3
title: Remove legacy tmux subprocess infrastructure from server.py
status: finished
parent: t2.j3MZX
up_dependencies:
- t3.ayg41E
down_dependencies:
- t3.dSZNT4
guid: qeYHa5hVg8DvUuTqGe5C7H
schema_version: 1.0.0
---

## Context
After both list_agents and cleanup_dead_sessions are migrated, the legacy tmux constants and subprocess import are dead code. Removing them completes the migration and ensures no accidental regression to subprocess patterns.

## What Needs to Change
- In `src/waggle/server.py`:
  - Remove `import subprocess`
  - Remove `TMUX_FIELD_SEP` constant (line 19)
  - Remove `TMUX_FMT_WITH_PATH` constant (lines 23-25)
  - Remove `TMUX_FMT_KEYS_ONLY` constant (lines 27-29)
  - Remove the comment block about the tab delimiter (lines 17-18)

## Key Files
- `src/waggle/server.py`

## Acceptance Criteria
- `grep -r "subprocess.run.*tmux" src/` returns zero results
- `grep "TMUX_FMT\|TMUX_FIELD" src/waggle/server.py` returns zero results
- `import subprocess` no longer in server.py
- Module still imports and runs correctly
