---
id: t1.dqt.da
type: t1
title: SRD
parent: b.dqt
reference_materials: null
created_at: '2026-05-16T14:33:30.884102'
status: pupa
schema_version: '0.1'
guid: dqtda4mrq86s74ojjsygwb6u2cztxoig
---

# SRD: Spawn Options and Templates for Claude Spawn Workers

Companion software requirements for PRD `t1.dqt.zb`. Defines what must be true about the implementation; does not prescribe how to build it.

This SRD is written against the **post-rename** identifier shape (`claude_spawn` Python package, `claude-spawn` CLI binary, `~/.claude-spawn/templates/`, `CLAUDE_STATUS_LABEL_CLAUDE_SPAWN_*` env-var prefix, `claude-spawn-<id>` tmux session prefix, `"claude-spawn-stdio"` FastMCP server name). The rename is the work of Plan Bee `b.fkb` and is a hard prerequisite — every name referenced below assumes that work has merged. File paths under `src/waggle/` and `tests/` in the current tree will live under `src/claude_spawn/` and `tests/` after the rename; this SRD references the post-rename paths.

## SR-1: Spawn options surface

### SR-1.1: Tool signature

- `spawn_worker` accepts the twelve options enumerated in the PRD "Invocation shape" section: `template`, `model`, `thinking`, `cwd`, `tmux_session_name`, `instance_id`, `claude_home`, `claude_settings`, `extra_env`, `claude_status_labels`, `claude_args`, `permissions`.
- The MCP tool definition in `src/claude_spawn/mcp_stdio.py` exposes every option as a named keyword parameter.
- The stateless implementation in `src/claude_spawn/spawn.py` (`spawn_worker_impl`) accepts the same named parameters.
- The old positional/keyword pair `(model, repo, session_name)` from the pre-feature signature is removed. There are no compatibility shims — callers passing the old positional `repo` argument or the old `session_name` keyword get an MCP-level parameter-mismatch error from FastMCP, not a silent translation.

### SR-1.2: Required vs. optional options

- `cwd` is the only required option, and its requirement is satisfied if either the per-call argument supplies it or the named template (when `template=<name>`) supplies it.
- Every other option is optional and falls back per SR-1.3.

### SR-1.3: Per-option default categories

The fallback table is normative. Each option falls in exactly one of three categories.

**Required (no default; spawn fails if unsupplied):**
- `cwd`

**Hardcoded-by-Claude-Spawn defaults:**
- `tmux_session_name` → `<sanitized-folder-name>-<first 8 characters of the resolved `instance_id` (no parsing, no UUID-shape assumption)>`
- `instance_id` → a bare UUIDv4 (e.g. `c2157396-d7fb-471c-815c-fbe9db0f5ab4`), with no folder prefix
- `extra_env` → empty map
- `claude_status_labels` → empty map
- `claude_args` → empty list
- `permissions` → empty map (no `allow`, `deny`, or `ask` keys injected into the settings overlay)

**Inherits-Claude-Code-default (Claude Spawn does not set):**
- `model` — no `--model` flag passed; Claude Code picks its own default
- `thinking` — no effort level injected
- `claude_home` — worker inherits the spawn process's `HOME`
- `claude_settings` — no `--settings` overlay file written or passed

### SR-1.4: Return shape

- A successful `spawn_worker` returns exactly `{"instance_id": str, "tmux_session_name": str}`. No additional keys.
- The previous return key `session_name` is renamed to `tmux_session_name` to match the new parameter name. The MCP contract change is part of this feature; no compatibility alias.
- Failure returns the operation-failed dict shape established by the existing stdio MCP module: `{"ok": False, "operation": "spawn_worker", "err_name": "<ErrName>", "err_description": "<detail>"}`. The implementation continues to use the FastMCP-level try/except wrapper in `src/claude_spawn/mcp_stdio.py` that maps unexpected exceptions to `err_name="ErrUnexpected"`. (Note: the existing module docstrings tag this pattern as "SR-7.1 error wrapping" referring to a *historical* SRD's section numbering. References to that historical tag are distinct from this SRD's own SR-7.1, which describes the new `list_templates` MCP tool.)

## SR-2: Option resolution and merge semantics

### SR-2.1: Resolution order

For each option, the effective value is determined by walking these sources in order and stopping at the first one that provides the key:
1. Per-call argument (any explicit non-sentinel value passed to `spawn_worker`).
2. Template field (only consulted when the caller supplied `template=<name>`).
3. The option's documented default per SR-1.3.

### SR-2.2: Map-valued merge

- The three map-valued options — `extra_env`, `claude_status_labels`, `permissions` — merge across the per-call and template sources by union, not by replacement.
- On colliding keys within a map, per-call entries win over template entries.
- The merge is shallow: a top-level key present in both sources resolves to the per-call value as a whole. The three `permissions` category keys (`allow`, `deny`, `ask`) are treated as the merge keys — a per-call `permissions.allow=[A]` against a template `permissions.allow=[B]` resolves to `permissions.allow=[A]` (replacement, not list union); a per-call `permissions.allow=[A]` against a template `permissions.deny=[D]` produces `{allow:[A], deny:[D]}` (independent keys preserved).
- Operators that want to "add to" a template's permission list pass the merged list explicitly per-call. The SRD-finalized policy chooses simplicity over deep-merge ergonomics.

### SR-2.3: List-valued and scalar sources

- `claude_args` is a list. It is **not** one of the three map-valued options in SR-2.2; per-call replaces template wholesale. Operators that want to extend a template's `claude_args` pass the full extended list per-call.
- Every scalar option (`model`, `thinking`, `cwd`, `tmux_session_name`, `instance_id`, `claude_home`, `claude_settings`) follows the same rule: per-call replaces template wholesale.

### SR-2.4: `template` is non-recursive

- A `template` field cannot itself contain `template`. Templates load as flat option bundles. SR-7.3 enforces the prohibition at write time; SR-6.4 enforces it at load time.

## SR-3: Folder-name sanitize helper

### SR-3.1: Behavior

A single helper function in `src/claude_spawn/spawn.py` takes a `cwd` path string and returns a tmux-safe, non-empty identifier. The helper is used by the default-value computation for `tmux_session_name` only. The default for `instance_id` is a bare UUIDv4 and does not consult the helper.

### SR-3.2: Algorithm requirements

The helper must satisfy these requirements in order:
1. Normalize the input path so that trailing slashes and redundant separators do not affect the result.
2. Extract the final path segment.
3. If the segment is empty (e.g. `"/"`) or equal to `"."`, substitute the literal string `root`.
4. Replace every character that is illegal in a tmux session name (at minimum `.` and `:`) with `-`.
5. If after replacement the result is empty or contains only punctuation, substitute the literal string `root`.

The output must always be non-empty and consist only of `[A-Za-z0-9_-]` characters.

### SR-3.3: Example mapping

The helper must produce the table in the PRD verbatim. Equivalents:
- `/home/horde/projects/waggle-project/waggle-main` → `waggle-main`
- `/home/horde/projects/waggle-project/waggle-main/` → `waggle-main`
- `/path/foo.bar.tar.gz` → `foo-bar-tar-gz`
- `/path/with:colon` → `with-colon`
- `/` → `root`
- `.` → `root`

### SR-3.4: Reuse

The `tmux_session_name` default-value computation is the only consumer of this helper. It composes `<helper(cwd)>-<resolved_instance_id[:8]>`. The 8-character slice operates on the resolved `instance_id` as a whole — the raw string, regardless of whether it is the auto-generated bare UUIDv4 (e.g. `c2157396-…`), a template-supplied value, or an operator-supplied value. The implementation does not parse `instance_id` to find a "UUID portion"; no folder prefix is assumed or stripped. If the resolved `instance_id` is shorter than 8 characters, the slice yields the entire string (no padding, no error).

## SR-4: Claude Code launch composition

### SR-4.1: Tmux session creation

- `spawn_worker_impl` continues to use the existing `_tmux` subprocess seam in `src/claude_spawn/spawn.py`. No new subprocess seam is introduced.
- The tmux `new-session -d -s <tmux_session_name>` invocation receives `-e KEY=VALUE` arguments for every environment variable in SR-4.2 and SR-4.3.
- The tmux session's working directory is set to the resolved `cwd` so that the Claude Code process launched in window 0, pane 0 inherits `cwd` as its working directory. This is the load-bearing realization of the PRD acceptance criterion that Claude Status's `cwd` field reflect the supplied `cwd`. The implementation must use tmux's start-directory flag on the `new-session` invocation rather than relying on a `cd` send-keys preamble.

### SR-4.2: Built-in environment variables

The required environment-variable set continues to include the existing baseline (`CLAUDE_STATUS_INSTANCE_ID`, `CLAUDE_STATUS_RELAY_MODE=on`, `CLAUDE_STATUS_AUQ_MODE=record`, and three or four `CLAUDE_STATUS_LABEL_CLAUDE_SPAWN_*` labels). The label env vars are:
- `CLAUDE_STATUS_LABEL_CLAUDE_SPAWN_OWNED=1` — always present.
- `CLAUDE_STATUS_LABEL_CLAUDE_SPAWN_SESSION_NAME=<tmux_session_name>` — always present.
- `CLAUDE_STATUS_LABEL_CLAUDE_SPAWN_CWD=<resolved cwd>` — always present.
- `CLAUDE_STATUS_LABEL_CLAUDE_SPAWN_MODEL=<resolved model, lowercased>` — only present when `model` was resolved to a non-empty value; omitted entirely otherwise.

The old `CLAUDE_STATUS_LABEL_WAGGLE_REPO` label is replaced by `CLAUDE_STATUS_LABEL_CLAUDE_SPAWN_CWD` (semantics: the worker's working directory, not a cloned repo). The implementation must update the `_REQUIRED_ENV_VARS` tuple in `src/claude_spawn/spawn.py` to reflect the new label set. Because the `MODEL` label is conditional, the tuple is the *unconditionally-required* subset (the three always-present labels plus the three non-label env vars), and the `MODEL` label is asserted separately when `model` is supplied.

### SR-4.3: Caller-supplied environment

- Every entry in the merged `extra_env` map (SR-2.2) is added to the `-e KEY=VALUE` list passed to `tmux new-session`.
- Every entry in the merged `claude_status_labels` map is added as `-e CLAUDE_STATUS_LABEL_<KEY>=<VALUE>`. The implementation must uppercase the `<KEY>` for env-var conformance; the caller-facing label key in templates and tool input is the post-transform key (operators pass the bare label name without the `CLAUDE_STATUS_LABEL_` prefix and without forcing uppercase).
- `claude_home`, when supplied, is added as `-e HOME=<claude_home>` so the worker's `HOME` is rewritten before Claude Code starts.
- No `extra_env` key is special-cased, sanitized, or rejected on value grounds. `extra_env={"PATH":""}` is the operator's choice and reaches the worker unchanged.
- Conflict policy: if a caller-supplied `extra_env` key collides with one of the SR-4.2 built-in env vars (`CLAUDE_STATUS_INSTANCE_ID`, `CLAUDE_STATUS_RELAY_MODE`, `CLAUDE_STATUS_AUQ_MODE`, any `CLAUDE_STATUS_LABEL_CLAUDE_SPAWN_*` key, or `HOME` when `claude_home` is also supplied), the spawn fails with `err_name=ErrReservedEnvKey` before any tmux call is made; the error description names the offending key. Claude Spawn never lets a caller silently clobber the built-in env set.

### SR-4.4: Claude Code command composition

The `tmux send-keys` invocation that launches Claude in window 0, pane 0 composes a command line from these pieces, in this order:
1. The literal `claude` invocation.
2. `--model <model>` if and only if `model` was resolved to a non-empty value.
3. `--effort <level>` if and only if `thinking` was resolved to a non-empty value. Claude Code exposes effort as a launch-time CLI flag (`--effort low|medium|high|xhigh`, supported since Claude Code v2.1.113). The implementation must use this CLI flag rather than the `CLAUDE_CODE_EFFORT_LEVEL` env var or the `effortLevel` `settings.json` field — both also exist but provide weaker isolation from the spawn-process's broader env / settings. The four accepted values match SR-6.4's `thinking` enum.
4. `--settings <path>` if `claude_settings` is supplied **or** if the resolved `permissions` map is non-empty (see SR-4.5).
5. Every entry of the resolved `claude_args` list, appended verbatim with no quoting transformation beyond what `tmux send-keys` already does for a single argv string.

The final argument to `send-keys` remains the literal `Enter` keysym, matching the existing pattern.

### SR-4.5: Settings overlay synthesis

- When the caller supplies `claude_settings=<path>`, that file is passed to Claude Code via `--settings`.
- When the merged `permissions` map is non-empty and no `claude_settings` is supplied, Claude Spawn synthesizes an overlay file in a per-spawn temporary location, writes the `permissions` block into it under Claude Code's `settings.permissions` schema, and passes that path via `--settings`. The temp file's lifetime is bounded to the worker's tmux session; cleanup on termination is best-effort and not part of this feature's scope.
- When **both** `claude_settings=<path>` and a non-empty `permissions` map are supplied, Claude Spawn produces a single composite overlay by reading the caller's file, layering the resolved `permissions` map's `allow`/`deny`/`ask` keys on top of any same-named keys already present in the file, and writing the composite to a per-spawn temp file. The composite path (not the caller's original path) is passed via `--settings`. This is the mechanical realization of "first-class `permissions` wins" from the PRD.

### SR-4.6: `claude_args` conflict guard

- The implementation must inspect `claude_args` before constructing the Claude command line. If `claude_args` contains a `--settings` flag (either `--settings <value>` or `--settings=<value>` form) **and** `claude_settings` is also resolved to a non-empty value, the spawn fails with `err_name=ErrClaudeArgsSettingsConflict` before any tmux call is made.
- The implementation must not silently strip or rewrite `--settings` from `claude_args`.
- The guard applies only to `--settings`. Duplicates of the other first-class flags Claude Spawn injects (`--model`, `--effort`) are intentionally not guarded: Claude Code defines last-flag-wins semantics for those, so `claude_args=["--model","haiku"]` with `model="opus"` resolves predictably to `haiku` at the worker. `--settings` is guarded because Claude Code's behavior when passed two `--settings` flags is ambiguous (load-both, ignore-one, or error are all plausible), and Claude Spawn refuses to emit a command line that depends on that resolution.

## SR-5: Spawn readiness

### SR-5.1: Blocking semantics

- `spawn_worker_impl` returns only after the worker has appeared in Claude Status's `instances` table under the worker's `instance_id`, or after the readiness timeout expires.
- The readiness check polls Claude Status via the existing `src/claude_spawn/claude_status.py` `workers(label=...)` verb (or equivalent), reusing the existing `_run` subprocess seam. No new client surface is introduced.
- The poll interval is short enough that the happy-path return latency is bounded by the worker's actual registration time plus at most one poll-interval round-trip.

### SR-5.2: Timeout default

- The readiness timeout is a module-level constant in `src/claude_spawn/spawn.py`. **The SRD commits to a default of 15 seconds**, matching the PRD-author pick.
- v1 does not expose the timeout to callers or templates. Operators cannot override it per-call.

### SR-5.3: Timeout cleanup

- If the worker does not register within the timeout, `spawn_worker` returns `err_name=ErrSpawnReadinessTimeout` and a description that names the `tmux_session_name` and the timeout value.
- Before returning the timeout error, the implementation must issue `tmux kill-session -t <tmux_session_name>` to avoid leaking an unregistered session. The kill is best-effort; if `tmux kill-session` fails (e.g. session already gone), the timeout error is still returned with an unchanged `err_name`.

### SR-5.4: Worker-exited-early detection

- During the readiness wait, the implementation must detect when the worker's tmux session has terminated before registration. The detection method is a `tmux has-session -t <tmux_session_name>` probe interleaved with the poll, or equivalent.
- On detected early exit, `spawn_worker` returns `err_name=ErrSpawnWorkerExitedEarly`. The `err_description` must include the last captured pane content (via `tmux capture-pane` against the same session before kill, if still capturable) for diagnostics.

## SR-6: Templates configuration layer

### SR-6.1: Storage layout

- Templates live at `~/.claude-spawn/templates/`. One file per template.
- The template name is the filename stem (i.e. file basename without the format-specific extension).
- The directory may be absent on a fresh install. Code paths that read the directory must treat absence as "zero templates" rather than an error.

### SR-6.2: File format

**The SRD commits to TOML.** This finalizes the open question in the PRD. Rationale:
- TOML supports nested maps natively (required for `extra_env`, `claude_status_labels`, `permissions`).
- TOML parsing is in the Python standard library as `tomllib` from 3.11 onwards. The current `pyproject.toml` declares `python = "^3.10"`; the SRD requires that the package floor be lifted to `^3.11` as part of this feature, so the implementation can depend on `tomllib` without a backport dependency.
- TOML is human-readable, hand-editable, and uses the same syntax operators already see in `pyproject.toml`.
- Template files use the `.toml` extension. The on-disk path is `~/.claude-spawn/templates/<name>.toml`.

For writing TOML, the implementation depends on the `tomli-w` package (the read-only counterpart of `tomllib`). `tomli-w` is added as a runtime dependency in `pyproject.toml`.

### SR-6.3: Schema

- The top-level TOML document is a table whose keys are a subset of the eleven option names defined in SR-1.1, excluding `template`. (Templates cannot reference templates — SR-2.4.)
- Map-valued options (`extra_env`, `claude_status_labels`, `permissions`) are TOML inline tables or table sections.
- List-valued options (`claude_args`, the three sub-lists of `permissions`) are TOML arrays.
- Scalar options (`model`, `thinking`, `cwd`, `tmux_session_name`, `instance_id`, `claude_home`, `claude_settings`) are TOML strings.

### SR-6.4: Load-time validation

A template file is considered **malformed** and must be rejected at load time if any of these hold:
- The file fails to parse as TOML.
- The top-level table contains a key not in the schema (SR-6.3).
- The top-level table contains a `template` key (SR-2.4).
- `thinking` is present and is not one of the four documented levels (`low`, `medium`, `high`, `xhigh`).
- A scalar option's value is not a string.
- A map option's value is not a TOML table (inline or section).
- A list option's value is not a TOML array of strings.

A malformed template produces a per-file `ErrTemplateMalformed` result with `err_description` naming the file path and the parse problem.

### SR-6.5: Loader module

- Template loading lives in a new module, `src/claude_spawn/templates.py`, alongside the existing `spawn.py` and `claude_status.py`.
- The loader exposes at least these functions: one to resolve and read a single template by name (used by `spawn_worker_impl`) and one to enumerate all templates (used by `list_templates_impl`).
- All filesystem reads go through a single, patchable seam (e.g. `templates._read_template_file(path)`) so tests never touch the real `~/.claude-spawn/templates/` directory.
- Import of `src/claude_spawn/templates.py` performs no filesystem reads, matching the import-inertia convention already documented in `src/claude_spawn/claude_status.py`.

### SR-6.6: Lookup semantics

- A bare `spawn_worker(cwd=…)` call (no `template=<name>`) must not read the templates directory at all. The loader is only invoked when `template` is supplied.
- Templates are not cached between calls within a single MCP server process. Each `spawn_worker(template=<name>)` invocation re-reads the file, so hand-edits take effect on the next spawn without reloading the server.
- `spawn_worker(template=<missing>, …)` returns `err_name=ErrTemplateNotFound` with an `err_description` naming both the template name and the directory searched.

## SR-7: Template authoring tools

### SR-7.1: `list_templates` MCP tool

- A new tool `list_templates` is registered in `src/claude_spawn/mcp_stdio.py`, alongside the six existing tools. The total tool count rises from six to eight (the eighth is `write_template`, SR-7.3).
- The tool takes no parameters.
- On success the tool returns a dict with two keys: `templates` (a list of entries) and `skipped` (a list of malformed-template diagnostics).
- Each `templates[]` entry is a dict with at least these keys: `name` (filename stem), `path` (absolute path on disk), and `options` (the resolved option map as it would be applied to a spawn). The option map keys are the canonical option names from SR-1.1 (excluding `template`).
- Each `skipped[]` entry is a dict with at least these keys: `path` (absolute path), `err_name` (e.g. `ErrTemplateMalformed`), and `err_description` (the parser detail).
- Missing templates directory is operation-success with `templates=[]` and `skipped=[]`. It is not an error.
- The implementation goes through the existing FastMCP try/except error-wrapping pattern documented in `mcp_stdio.py`; an unexpected exception maps to `ErrUnexpected`.

### SR-7.2: `list_spawned_workers` is unaffected

- The existing `list_spawned_workers` MCP tool retains its current contract and is not renamed, merged with, or shadowed by `list_templates`. The two tools query different sources (Claude Status vs. the templates directory) and return different shapes.

### SR-7.3: `write_template` MCP tool

- A new tool `write_template` is registered in `src/claude_spawn/mcp_stdio.py`. It accepts three named parameters: `name` (string), `options` (dict matching `spawn_worker`'s named parameters minus `template`), and `force` (boolean, default false).
- On success the tool returns a dict with at least: `path` (absolute on-disk path of the written file) and `options` (the option map as written, after server-side normalization).
- The tool validates `name` against SR-7.4 before writing.
- The tool validates `options` against the same schema rules as load-time validation (SR-6.4). Specifically: unknown keys, a `template` key, an invalid `thinking` level, or a type mismatch return `ErrTemplateOptionsInvalid` with `err_description` enumerating the rejected keys or values. No file is written or modified on validation failure.
- The tool checks for an existing template with the same name. If one exists and `force=False`, the tool returns `ErrTemplateExists` with `err_description` naming the existing file path; the existing file is not modified. If `force=True`, the file is overwritten atomically (write to temp + rename within the same directory) so that a crash mid-write does not leave a half-written file at the canonical path.
- The tool creates `~/.claude-spawn/templates/` if it does not already exist.

### SR-7.4: Template-name safety

- A template name is **unsafe** and must be rejected if any of these hold: it contains a path separator (`/` on POSIX, `\` on Windows, but Claude Spawn is POSIX-only so `/` is the focus); it starts with `.`; it equals `..`; it contains `..` as a path-component substring; it is empty.
- Unsafe names return `ErrTemplateNameUnsafe`. No file is written.
- The implementation must construct the on-disk path by joining the templates directory and the sanitized name in a way that does not allow the joined path to escape the templates directory. A defensive check after path construction (e.g. resolving the parent and confirming it equals the templates directory) is required.

### SR-7.5: `claude-spawn write-template` CLI subcommand

- A new subcommand `write-template <name>` is added to `src/claude_spawn/cli.py` via the existing argparse `subparsers` registry. The CLI module continues to use the `ClaudeSpawnArgumentParser` subclass (the post-rename version of `WaggleArgumentParser`) with its JSON-error-on-stderr convention.
- Two mutually exclusive modes are supported:
  - **Interactive mode** (`--interactive`): the subcommand walks through every option in SR-1.1 (except `template`) one prompt at a time. Each prompt displays the option name, a short human description, and (if a template with that name already exists) the current resolved value. The operator types a value or the literal token `skip` to leave the option unset. After the last prompt, the subcommand delegates to `write_template_impl` with the collected options.
  - **Flag-driven mode** (the default): every option in SR-1.1 (except `template`) is exposed as a CLI flag. Scalar options: `--model`, `--thinking`, `--cwd`, `--tmux-session-name`, `--instance-id`, `--claude-home`, `--claude-settings`. List-valued `claude_args` is exposed as a repeatable `--claude-arg VALUE` flag (singular; repeat to add multiple entries). Map-valued options accept repeatable entry-form flags: `--extra-env-entry KEY=VALUE`, `--claude-status-labels-entry KEY=VALUE`, `--permissions-allow PATTERN`, `--permissions-deny PATTERN`, `--permissions-ask PATTERN`.
- Both modes accept `--force`; without it, a name collision with an existing template fails with the same `ErrTemplateExists` surface as the MCP tool.
- The CLI subcommand's success output is a single JSON line on stdout matching the dict shape returned by `write_template` (path + options). The CLI subcommand exits 0 on success and 2 on error, with the error JSON line on stdout (matching the existing `ClaudeSpawnArgumentParser.error` convention).
- Cancellation: a user interrupting interactive mode (Ctrl-C, EOF on stdin) causes the subcommand to exit non-zero with a JSON error line `{"status": "error", "message": "write-template cancelled"}` and to write no file.
- Both the MCP tool and the CLI subcommand delegate to a single `write_template_impl` function so the two surfaces are guaranteed to produce identical files for identical inputs.

### SR-7.6: Hand-editing is preserved

- Operators may continue to author template files by directly editing `~/.claude-spawn/templates/<name>.toml`. The loader (SR-6.5) treats hand-written files identically to tool-written files.

## SR-8: Existing-behavior preservation

### SR-8.1: Removed surfaces

- The old positional `repo` parameter is **removed**, not deprecated. There is no compatibility shim that accepts an HTTPS URL and emits a warning. Callers that pass HTTPS URLs as `cwd` get the SR-9.2 path-validation error.
- The old `session_name` parameter name is **renamed** to `tmux_session_name`. There is no positional fallback or alias.

### SR-8.2: Preserved surfaces

The following must remain functionally identical for callers that do not exercise the new options:
- `send_input`, `get_output`, `terminate_worker`, `answer_question`, `list_spawned_workers` — all unchanged in contract and error surface.
- The existing FastMCP error-wrapping pattern in `src/claude_spawn/mcp_stdio.py` (every tool wrapped in try/except, mapping to `ErrUnexpected`) — unchanged.
- The `_tmux` and `_run` subprocess seams in `src/claude_spawn/spawn.py` and `src/claude_spawn/claude_status.py` — unchanged in name and signature, so existing tests continue to patch them.
- The Claude Status consumer-CLI subprocess contract (the `_TIMEOUT_SECONDS=10` bound, the stderr envelope grammar, the `capabilities()` contract-version pin) — unchanged.
- The `waggle install` → `claude-spawn install` subcommand and its hook-wiring behavior — unchanged in behavior; rename-only.
- The `waggle sting` → `claude-spawn sting` subcommand — unchanged in behavior; rename-only.

## SR-9: Validation and error catalogue

### SR-9.1: New error names

The implementation must surface the following `err_name` values from `spawn_worker`, `list_templates`, and/or `write_template`. Each must produce a description that names the offending input (path, key, name, etc.) so the LLM can act on it without consulting source code.

| `err_name` | Surface(s) | Trigger |
|---|---|---|
| `ErrCwdMissing` | `spawn_worker` | Neither per-call nor template supplied `cwd` |
| `ErrCwdNotFound` | `spawn_worker` | Resolved `cwd` does not exist on the filesystem |
| `ErrCwdNotAPath` | `spawn_worker` | Resolved `cwd` is an HTTPS/SSH URL or any string that is not an absolute or `~`-prefixed local filesystem path |
| `ErrClaudeSettingsNotFound` | `spawn_worker` | Resolved `claude_settings` path does not exist or is not readable |
| `ErrInstanceIdCollision` | `spawn_worker` | Operator-supplied `instance_id` matches an active worker in Claude Status |
| `ErrClaudeArgsSettingsConflict` | `spawn_worker` | `claude_args` contains `--settings` while `claude_settings` is also set |
| `ErrReservedEnvKey` | `spawn_worker` | `extra_env` supplies a key that collides with the built-in env-var set (SR-4.2) |
| `ErrThinkingInvalid` | `spawn_worker`, `write_template` | `thinking` value is not one of `low`/`medium`/`high`/`xhigh` |
| `ErrTemplateNotFound` | `spawn_worker` | Caller supplied `template=<name>` and no such file exists |
| `ErrTemplateMalformed` | `spawn_worker`, `list_templates` (in `skipped[]`) | Template file fails parse or schema validation per SR-6.4 |
| `ErrTemplateExists` | `write_template` | Template name collides with existing file and `force=False` |
| `ErrTemplateNameUnsafe` | `write_template` | Template `name` fails SR-7.4 safety check |
| `ErrTemplateOptionsInvalid` | `write_template` | `options` dict fails schema validation per SR-6.4 (unknown keys, type mismatch, `template` key) |
| `ErrSpawnReadinessTimeout` | `spawn_worker` | Worker did not register with Claude Status within the SR-5.2 timeout |
| `ErrSpawnWorkerExitedEarly` | `spawn_worker` | Worker's tmux session ended before Claude Status registration |

### SR-9.2: Path validation rules

For both `cwd` and `claude_settings`, the validation must reject HTTPS URLs, SSH URLs, and any string that does not resolve via `os.path.expanduser` + `os.path.isabs` (or `os.path.realpath`) to an existing filesystem entry. The implementation must not create directories or files to satisfy these checks.

### SR-9.3: `permissions`-vs-`claude_settings` precedence

Per SR-4.5, when both a `claude_settings` file and a non-empty `permissions` map are supplied, the first-class `permissions` map wins on the keys it specifies (`allow`, `deny`, `ask`). All other top-level keys in `claude_settings` (e.g. `theme`, `defaultMode`, `mcpServers`) pass through unmodified into the composite overlay.

### SR-9.4: `--dangerously-skip-permissions` interaction

The implementation must not reject the combination of `claude_args=["--dangerously-skip-permissions"]` and a non-empty `permissions` map. Claude Code's CLI-level bypass wins at runtime; Claude Spawn passes both through and lets the resulting worker's behavior reflect Claude Code's precedence. The behavior is documented in the README per SR-12.

## SR-10: Claude Status integration

### SR-10.1: Readiness query

The readiness wait in SR-5 uses `claude_status.workers(label=<owner-label>)` where `<owner-label>` is the post-rename `claude_spawn_owned=1` label. The result is scanned for a row whose `instance_id` matches the spawned worker's `instance_id`. The matching is exact-equality on `instance_id`, not on `tmux_session_name`.

### SR-10.2: Existing `workers(label=…)` usages

- Every existing call site that filters by `label="waggle_owned=1"` (in `spawn.py` `list_spawned_workers_impl`, in `spawn.py` `answer_question_impl`) is updated to `label="claude_spawn_owned=1"` as part of the Plan Bee `b.fkb` rename. This SRD assumes those updates have already landed; it requires only that the readiness check in `spawn_worker_impl` (new) uses the same post-rename label.
- The `labels.get("waggle_session_name", …)` and `labels.get("waggle_repo", …)` lookups in `spawn.py` are updated by `b.fkb` to `claude_spawn_session_name` and `claude_spawn_repo` respectively (b.fkb is a pure rename per its Epic 3 scope; it does not change the `repo`-to-`cwd` semantics). This SRD's b.dqt work performs the further rename from `claude_spawn_repo` to `claude_spawn_cwd` in lockstep with the SR-4.2 label-key change — both at the env-var emit site (in `spawn_worker_impl`) and at the label-read site (in `list_spawned_workers_impl`'s projection of `session_name`, and anywhere else b.fkb leaves `claude_spawn_repo` after merging).
- The new readiness check does **not** depend on label lookups — it filters the worker list by `instance_id` directly.

### SR-10.3: Capabilities pin

The `claude_status.capabilities()` contract-version pin (major == 1) continues to enforce at the MCP server layer. No changes to the pin logic in `src/claude_spawn/claude_status.py`.

## SR-11: Test architecture and fixtures

### SR-11.1: Test layout

Tests live in the existing flat `tests/` directory. There is no `conftest.py` and one is not added. Shared helpers go in `tests/helpers.py`. Canonical constants go in `tests/sample_payloads.py`.

### SR-11.2: Mandated helper reuse

Implementors must reuse the following existing helpers instead of constructing equivalents inline:
- `tests.helpers.fake_claude_status(triples)` — context manager that patches the `claude_status._run` seam. Use this for every test that asserts on Claude Status interactions (including the new readiness check and the new `claude_spawn_owned=1` queries).
- `tests.helpers.fake_worker_record(instance_id, status, labels=None, pending=None)` — builder for Worker dicts. The default labels in this helper currently embed `waggle_owned`, `waggle_session_name`, `waggle_model`, `waggle_repo` keys; these are renamed by `b.fkb` to `claude_spawn_owned`, `claude_spawn_session_name`, `claude_spawn_model`, `claude_spawn_repo` (pure rename, no semantic change). This SRD's b.dqt work then renames `claude_spawn_repo` to `claude_spawn_cwd` in the helper defaults, in lockstep with the SR-4.2 label-key rename. New tests must use the helper, not hardcode label keys.
- `tests.helpers.fake_workers_response(records, skipped=())` — envelope wrapper. New tests must use this rather than constructing the `{"workers": …, "skipped": …}` dict inline.
- `tests.sample_payloads.WORKER_WORKING`, `WORKER_ASK_USER_SINGLE`, `WORKER_ASK_USER_MULTI`, and the stderr-payload constants — use these whenever a test needs a canonical Worker shape or a canonical Claude-Status stderr envelope.

### SR-11.3: Helper extensions

The following extensions to existing helpers are mandatory:
- `fake_worker_record` gains an `instance_id`-only fast path (already present) plus a new keyword `cwd` so tests asserting on the new `claude_spawn_cwd` label can override the default without constructing a labels dict by hand. The implementation must keep the existing label defaults for tests that pass no `labels=` argument; the new keyword is additive.
- `fake_claude_status` requires no signature change — it already accepts an arbitrary sequence of `(stdout, stderr, exit_code)` triples, which is sufficient for the multi-poll readiness sequence (callers append one triple per expected poll).

### SR-11.4: New helpers

A new helper `fake_templates_dir` is added to `tests/helpers.py`. It is a context manager that takes a mapping of template-name strings to TOML body strings and:
- Creates a temp directory and returns its path as a string.
- Writes each `name -> content` entry as a `.toml` file under the temp dir.
- Patches the templates-directory resolution seam in `src/claude_spawn/templates.py` (whatever name it ends up with — e.g. `_templates_dir()`) to return the temp dir for the duration of the context.
- Cleans up the temp dir on exit.

No test in the new feature may touch the real `~/.claude-spawn/templates/` directory.

### SR-11.5: New constants

The following constants are added to `tests/sample_payloads.py`:
- `TEMPLATE_TOML_MINIMAL` — a canonical valid template body with only `cwd` set.
- `TEMPLATE_TOML_FULL` — a canonical valid template body exercising every supported option (`model`, `thinking`, `cwd`, `tmux_session_name`, `instance_id`, `claude_home`, `claude_settings`, `extra_env`, `claude_status_labels`, `claude_args`, `permissions`).
- `TEMPLATE_TOML_MALFORMED_UNKNOWN_KEY` — a body that fails SR-6.4 by including an unknown top-level key.
- `TEMPLATE_TOML_MALFORMED_PARSE` — a body that fails TOML parsing entirely.
- `TEMPLATE_TOML_RECURSIVE` — a body that fails SR-6.4 by including a top-level `template` key.

No test may hardcode a TOML string literal; every test uses one of the above constants or a programmatic construction via `tomli_w`.

### SR-11.6: New test files

The following new test files are added under `tests/`:
- `tests/test_engine_spawn_worker_options.py` — exercises the new spawn-option resolution, the SR-2.2 map-merge rules, the SR-3 sanitize helper, the SR-4 launch composition, and the SR-9 validation errors. Patches `claude_spawn.spawn._tmux` (and `claude_spawn.claude_status._run` for readiness assertions).
- `tests/test_engine_spawn_worker_readiness.py` — exercises SR-5 paths: timely registration, registration timeout (with `tmux kill-session` verification), worker-exited-early.
- `tests/test_engine_templates_load.py` — exercises SR-6 loader paths against `fake_templates_dir`: missing directory, valid templates, every SR-6.4 malformed shape.
- `tests/test_engine_templates_write.py` — exercises `write_template_impl`: success, collision-without-force, collision-with-force, unsafe-name, unknown-key, recursive-template.
- `tests/test_mcp_stdio_list_templates.py` — exercises the `list_templates` MCP tool via the FastMCP test surface, asserting the SR-7.1 result shape (with `skipped[]` channel).
- `tests/test_mcp_stdio_write_template.py` — exercises the `write_template` MCP tool, asserting SR-7.3.
- `tests/test_cli_write_template.py` — exercises the new CLI subcommand via direct `main()` calls with `monkeypatch`-set `sys.argv` (matching the existing pattern in `tests/test_cli.py`). Covers both interactive and flag-driven modes; interactive mode uses `monkeypatch.setattr` on `builtins.input` to drive the prompts.

### SR-11.7: Test extensions to existing files

- `tests/test_engine_spawn_worker.py` — existing tests are updated by `b.fkb` to use post-rename label keys via `fake_worker_record`. This SRD adds: a small number of tests that verify the new signature (`tmux_session_name` return key, `cwd`-instead-of-`repo` parameter) but leaves the bulk of new spawn-option testing to `tests/test_engine_spawn_worker_options.py`.
- `tests/test_mcp_stdio_server.py` — the tool-count assertion is updated to expect eight tools (six existing + `list_templates` + `write_template`). The error-wrapping assertion (existing pattern, documented inline as "SR-7.1" referring to a historical SRD) is extended to cover the two new tools.

## SR-12: Documentation

After implementation, the following documentation updates are mandatory:

### SR-12.1: README

`README.md` documents:
- The full v1 option list (all twelve parameters) and the per-option fallback table from SR-1.3.
- Claude Code's 4-layer settings stack and how `claude_home`, `claude_settings`, and `permissions` map onto it.
- The template file format (TOML), with at least one worked example template under `~/.claude-spawn/templates/`.
- The merge rules from SR-2 (per-call wins on scalar collisions; map fields union with per-call winning).
- The `list_templates` MCP tool's output shape, including the `skipped[]` channel.
- The `write_template` MCP tool and the `claude-spawn write-template` CLI subcommand, with both interactive and flag-driven mode examples and the repeatable-flag forms for map-valued options.
- The spawn-readiness blocking behavior and the 15-second timeout.
- The behavioral change from the old `repo` parameter: `cwd` must be a local path that already exists; Claude Spawn never clones from HTTPS URLs.
- The `--dangerously-skip-permissions` precedence note (SR-9.4).

### SR-12.2: Architecture docs

A new file `docs/architecture/spawn_options_and_templates.md` documents the data flow for the new feature, in the same style as the existing `docs/architecture/spawn_worker.md`, `docs/architecture/send_input.md`, and `docs/architecture/terminate_worker.md`. It covers: the option-resolution order, the settings-overlay synthesis, the readiness wait, the templates loader, and the two new authoring surfaces.

The existing `docs/architecture/spawn_worker.md` is updated by this feature to reflect the new options and the readiness wait; the old `repo`-centric phrasing is replaced.

### SR-12.3: Acceptance bar

An operator must be able to read the README, author a template (by hand, by `claude-spawn write-template`, or by `write_template` MCP call), and spawn from it — all without reading source code. This is the same bar the PRD states.

## SR-13: Out-of-scope confirmations

The SRD does not require any of the following, mirroring the PRD's non-goals:
- Template inheritance / `extends` chains.
- Field-level template patching via `write_template` (whole-file writes only).
- Live template reloading via a watchdog (next-spawn pickup is sufficient).
- JSON Schema or other formal validation of template contents (the SR-6.4 schema is the only requirement).
- An auto-loaded default template (`spawn_worker(cwd=…)` with no `template=` argument reads no file).
- Shipping any builtin templates in `~/.claude-spawn/templates/`.
- Migration tooling from a prior spawn-options system.
- The `initial_prompt` option (operators chain `send_input` after spawn).
- Per-call or template-level override of the SR-5.2 readiness timeout.

## Open questions and research items

All open questions in the PRD are resolved by this SRD:
- Template file format → TOML (SR-6.2).
- Spawn-readiness timeout default → 15 seconds, hardcoded, no per-call override (SR-5.2).
- `thinking` launch-time surface → `--effort <level>` CLI flag (SR-4.4), confirmed available since Claude Code v2.1.113. The previously flagged `ErrThinkingNotLaunchable` contingency is no longer needed and has been dropped from the error catalogue.
